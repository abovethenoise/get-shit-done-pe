---
phase: 14-install-validate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gsd/debug.md
  - commands/gsd/discuss-capability.md
  - commands/gsd/discuss-feature.md
  - commands/gsd/enhance.md
  - commands/gsd/init.md
  - commands/gsd/new.md
  - commands/gsd/plan.md
  - commands/gsd/refactor.md
  - commands/gsd/resume-work.md
  - commands/gsd/review.md
  - commands/gsd/progress.md
  - commands/gsd/focus.md
  - get-shit-done/workflows/
  - get-shit-done/references/
  - get-shit-done/templates/
autonomous: true
requirements: [INST-02, INST-03]

must_haves:
  truths:
    - "All deployable .md files use {GSD_ROOT}/ instead of ~/.claude/"
    - "Zero literal ~/.claude/ paths remain in source files that get deployed"
    - "All v2 agent files follow gsd-* prefix convention"
  artifacts:
    - path: "commands/gsd/*.md"
      provides: "Commands with tokenized paths"
      contains: "{GSD_ROOT}/"
    - path: "get-shit-done/workflows/*.md"
      provides: "Workflows with tokenized paths"
      contains: "{GSD_ROOT}/"
  key_links:
    - from: "commands/gsd/*.md"
      to: "get-shit-done/workflows/*.md"
      via: "@{GSD_ROOT}/ file references"
      pattern: "\\{GSD_ROOT\\}"
---

<objective>
Convert all literal ~/.claude/ paths in deployable source files to {GSD_ROOT}/ tokens.

Purpose: Source files must contain path tokens so install.js can resolve them to the actual install location at deploy time. This decouples source from any specific install path.
Output: 26 source files updated (~70 occurrences), zero literal ~/.claude/ paths remaining in deployable dirs.
</objective>

<execution_context>
@/Users/philliphall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philliphall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-install-validate/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace ~/.claude/ with {GSD_ROOT}/ in all deployable source files</name>
  <files>
    commands/gsd/*.md
    get-shit-done/workflows/*.md
    get-shit-done/references/*.md
    get-shit-done/templates/*.md
    agents/*.md
  </files>
  <action>
Perform a bulk find-and-replace of `~/.claude/` with `{GSD_ROOT}/` across all deployable source directories. The target directories are:

1. `commands/gsd/` -- 13 .md files (~35 occurrences, mostly @file references like `@~/.claude/get-shit-done/...`)
2. `get-shit-done/` -- workflows, references, templates (~35 occurrences)
3. `agents/` -- 17 .md files (research confirms 0 occurrences, but scan to be safe)
4. `hooks/` -- 2 .js files (research confirms 0 occurrences, but scan to be safe)

Use `sed -i '' 's|~/.claude/|{GSD_ROOT}/|g'` or equivalent across all .md files in these directories recursively.

**Do NOT modify:**
- `bin/install.js` (handled in Plan 02)
- `.planning/` directory (not deployed)
- `scripts/` directory (not deployed)
- `package.json` (not a deployable artifact)
- Any file outside the 4 source directories above

**Verification approach:** After replacement, grep all deployable dirs for literal `~/.claude/`. Must return zero matches.
  </action>
  <verify>
    <automated>grep -r '~/.claude/' commands/gsd/ agents/ get-shit-done/ hooks/ --include='*.md' --include='*.js' | wc -l | tr -d ' ' # must be 0</automated>
  </verify>
  <done>All deployable source files use {GSD_ROOT}/ tokens. Zero literal ~/.claude/ paths remain in commands/gsd/, agents/, get-shit-done/, or hooks/ directories.</done>
</task>

<task type="auto">
  <name>Task 2: Verify {GSD_ROOT} token count and file naming conventions</name>
  <files>
    agents/*.md
    commands/gsd/*.md
  </files>
  <action>
After token replacement, run verification checks:

1. **Token count:** `grep -r '{GSD_ROOT}/' commands/gsd/ agents/ get-shit-done/ hooks/ --include='*.md' -c` -- should show ~70 total occurrences across ~26 files. If significantly different, investigate.

2. **INST-03 prefix check:** Verify all agent files follow `gsd-*` prefix:
   - `ls agents/*.md` -- all should match `gsd-*.md` pattern
   - Hooks already confirmed as `gsd-context-monitor.js` and `gsd-statusline.js`
   - Commands are namespaced under `commands/gsd/` (no prefix needed per research)

3. **No stray tokens in non-deployed dirs:** `grep -r '{GSD_ROOT}/' .planning/ scripts/ bin/ 2>/dev/null` -- should return 0 (these dirs shouldn't have tokens).

If any agent files don't follow `gsd-*` prefix, rename them.
  </action>
  <verify>
    <automated>ls agents/*.md | xargs -I {} basename {} | grep -v '^gsd-' | wc -l | tr -d ' ' # must be 0</automated>
  </verify>
  <done>All {GSD_ROOT}/ tokens are in expected locations only. All agent files follow gsd-* prefix convention. No tokens leaked into non-deployed directories.</done>
</task>

</tasks>

<verification>
1. `grep -r '~/.claude/' commands/gsd/ agents/ get-shit-done/ hooks/ --include='*.md' --include='*.js'` returns empty
2. `grep -r '{GSD_ROOT}/' commands/gsd/ agents/ get-shit-done/ hooks/ --include='*.md' -l | wc -l` returns ~26
3. All files in agents/ match `gsd-*.md` pattern
</verification>

<success_criteria>
- Zero literal ~/.claude/ paths in any deployable source file
- ~70 {GSD_ROOT}/ tokens across ~26 files in deployable directories
- All agent files follow gsd-* prefix convention
- No {GSD_ROOT} tokens in non-deployed directories (.planning/, scripts/, bin/)
</success_criteria>

<output>
After completion, create `.planning/phases/14-install-validate/14-01-SUMMARY.md`
</output>

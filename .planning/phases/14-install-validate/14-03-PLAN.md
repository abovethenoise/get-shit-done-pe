---
phase: 14-install-validate
plan: 03
type: execute
wave: 2
depends_on: [14-01, 14-02]
files_modified:
  - scripts/validate-install.js
autonomous: true
requirements: [INST-01, INST-07, INST-08, CMD-01, VAL-01, VAL-02, VAL-03]

must_haves:
  truths:
    - "node install.js deploys all v2 artifacts to correct locations"
    - "No {GSD_ROOT} tokens remain in any installed file"
    - "Commands are discoverable after install"
    - "Framing commands load discovery files"
    - "/init on existing project (this repo) detects existing capabilities"
    - "config.json ships to ~/.claude/get-shit-done/templates/"
    - "framings/ directory deploys with all 4 subdirs"
  artifacts:
    - path: "scripts/validate-install.js"
      provides: "Dev-only install validation script"
      contains: "validateInstall"
  key_links:
    - from: "scripts/validate-install.js"
      to: "~/.claude/commands/gsd/"
      via: "file existence check"
      pattern: "commands.*gsd"
    - from: "scripts/validate-install.js"
      to: "~/.claude/get-shit-done/"
      via: "file existence check"
      pattern: "get-shit-done"
---

<objective>
Run install, create dev validation script, and execute smoke tests to prove the full install works end-to-end.

Purpose: The install must actually work. This plan runs the install, validates the deployed artifacts, and checks that the command surface is functional post-install.
Output: Validated install + dev-only validation script at scripts/validate-install.js.
</objective>

<execution_context>
@/Users/philliphall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philliphall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-install-validate/14-RESEARCH.md
@.planning/phases/14-install-validate/14-01-SUMMARY.md
@.planning/phases/14-install-validate/14-02-SUMMARY.md
@bin/install.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dev validation script and run install</name>
  <files>scripts/validate-install.js</files>
  <action>
Create `scripts/validate-install.js` -- a dev-only validation script (not shipped to users per decision).

The script checks the following (per user decisions in CONTEXT.md):

**Check 1: Expected files exist at installed paths**
Verify these files/dirs exist under `~/.claude/`:
- `commands/gsd/` -- 13 .md files (debug, discuss-capability, discuss-feature, enhance, focus, init, new, plan, progress, refactor, resume-work, review, status)
- `agents/` -- 17 gsd-*.md files
- `get-shit-done/workflows/` -- workflow .md files
- `get-shit-done/templates/` -- template files including config.json
- `get-shit-done/references/` -- reference .md files
- `get-shit-done/framings/` -- 4 subdirs (debug, enhance, new, refactor) each with anchor-questions.md
- `get-shit-done/bin/gsd-tools.cjs` -- CLI tool
- `hooks/gsd-context-monitor.js` and `hooks/gsd-statusline.js`

**Check 2: No unresolved {GSD_ROOT} tokens in installed files**
Recursively scan all installed .md, .js, .json files for literal `{GSD_ROOT}` string. Report any found.

**Check 3: Commands are discoverable**
Check that command files exist and are parseable (have frontmatter with `---` delimiters).

**Check 4: gsd-tools.cjs runs without error**
`node ~/.claude/get-shit-done/bin/gsd-tools.cjs` -- should not throw. May need a `--help` or `--version` flag, or just require and check it doesn't throw.

**Check 5: No references to deleted files or old phase paths**
Scan installed .md files for `phases/` references that would indicate old v1 path patterns. Also check for references to known-deleted files.

**Check 6: Legacy artifacts cleaned**
Verify these do NOT exist:
- `~/.claude/gsd-local-patches/`
- `~/.claude/gsd-file-manifest.json`
- `~/.claude/get-shit-done/VERSION`

Script should output PASS/FAIL per check with details on failures. Exit 0 if all pass, exit 1 if any fail.

After creating the script, run the actual install: `cd /Users/philliphall/get-shit-done-pe && node bin/install.js --global` (use --global flag to skip interactive prompt). Then run the validation script: `node scripts/validate-install.js`.

Fix any issues found by the validation script by editing the appropriate source files or install.js, then re-run install + validation until clean.
  </action>
  <verify>
    <automated>node scripts/validate-install.js # must exit 0</automated>
  </verify>
  <done>Install completes without error. Validation script passes all 6 checks. All v2 artifacts deployed to correct locations. No unresolved tokens. No legacy artifacts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verifies install on existing and fresh repos</name>
  <what-built>
Full install and dev validation. The install deploys all v2 artifacts to ~/.claude/. The validation script confirms file existence, token resolution, command discoverability, and legacy cleanup.

This covers:
- INST-07: config.json ships to templates/
- INST-08: framings/ directory deploys with all 4 subdirs
- VAL-01: Smoke test on both brownfield (this repo) AND fresh repo
- VAL-02: Framing files (anchor-questions.md) deployed for all 4 framings
- VAL-03: This repo (get-shit-done-pe) serves as the existing project test case
  </what-built>
  <how-to-verify>
**Part A: Existing project (brownfield)**
1. Open a new Claude Code session in this repo (get-shit-done-pe)
2. Run `/init` -- should detect existing project and trigger brownfield flow
3. Run `/gsd:status` -- should show current project state
4. Run `/gsd:new` or `/gsd:debug` -- should launch framing discovery (you can cancel after confirming it starts)
5. Confirm the installed commands appear in Claude Code's command list (type `/gsd:` and check autocomplete)

**Part B: Fresh repo (greenfield -- VAL-01)**
1. Create a temporary empty directory: `mkdir /tmp/gsd-fresh-test && cd /tmp/gsd-fresh-test && git init`
2. Open a Claude Code session in that directory
3. Run `/init` -- should detect fresh project and trigger greenfield setup flow
4. Confirm it creates project scaffolding without errors
5. Clean up: `rm -rf /tmp/gsd-fresh-test`

If anything doesn't work, describe the issue.
  </how-to-verify>
  <resume-signal>Type "approved" if install works correctly on both scenarios, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `node scripts/validate-install.js` exits 0
2. All 13 command files exist in ~/.claude/commands/gsd/
3. All 17 agent files exist in ~/.claude/agents/
4. Framing anchor questions exist for all 4 framings
5. No {GSD_ROOT} tokens in installed files
6. No legacy artifacts remain
7. User confirms commands work in Claude Code session (brownfield + greenfield)
</verification>

<success_criteria>
- Install completes successfully via `node bin/install.js`
- Dev validation script passes all checks (exit 0)
- User confirms /init, /status, and framing commands work on this repo (brownfield)
- User confirms /init works on a fresh empty repo (greenfield -- VAL-01)
- All v2 artifacts deployed to correct locations under ~/.claude/
</success_criteria>

<output>
After completion, create `.planning/phases/14-install-validate/14-03-SUMMARY.md`
</output>

---
phase: 03-planning-pipeline
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - agents/gsd-planner.md
  - get-shit-done/workflows/plan-phase.md
autonomous: true
requirements: [PLAN-01, PLAN-02, PLAN-03, PLAN-04, REQS-03, REQS-04]
must_haves:
  truths:
    - "Planner agent produces tasks with v2 schema: title, reqs, artifact, inputs, done — no steps, no implementation notes, no estimates"
    - "Planner runs 2-round self-critique internally: round 1 fixes silently, round 2 surfaces findings"
    - "Planner returns plan files + findings list to the workflow"
    - "Workflow presents findings one-at-a-time with 3 response options: Accept, Feedback, Research Guidance"
    - "Workflow runs gsd plan-validate after findings resolved — errors block finalization"
    - "Plan finalized only after explicit user confirmation — no auto-finalize"
    - "Every task in a generated plan references at least one REQ ID"
  artifacts:
    - path: "agents/gsd-planner.md"
      provides: "v2 planner agent with self-critique loop and v2 task schema"
      contains: "self_critique"
    - path: "get-shit-done/workflows/plan-phase.md"
      provides: "Updated workflow with Q&A loop, CLI validation, and finalization gate"
      contains: "plan-validate"
  key_links:
    - from: "get-shit-done/workflows/plan-phase.md"
      to: "agents/gsd-planner.md"
      via: "Task spawn — planner returns plan + findings"
      pattern: "gsd-planner"
    - from: "get-shit-done/workflows/plan-phase.md"
      to: "get-shit-done/bin/gsd-tools.cjs"
      via: "CLI call to plan-validate after Q&A"
      pattern: "plan-validate"
---

<objective>
Update the planner agent definition to produce v2 task format with per-task REQ tracing and internal self-critique, then update the plan-phase workflow to handle Q&A presentation of findings, CLI validation, and explicit finalization.

Purpose: The planner becomes self-correcting (catches its own coverage gaps before external review) and every task is independently traceable to requirements. The workflow ensures no plan ships without user confirmation.

Output: Updated `agents/gsd-planner.md` (v2 rewrite), updated `get-shit-done/workflows/plan-phase.md` (Q&A + validation + finalization gate).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-planning-pipeline/03-CONTEXT.md
@.planning/phases/03-planning-pipeline/03-RESEARCH.md
@.planning/phases/03-planning-pipeline/03-01-SUMMARY.md

<interfaces>
<!-- v2 task format (from CONTEXT.md — locked decision) -->
```xml
<task>
  <title>Create frontmatter validation for v2 task schema</title>
  <reqs>REQS-03, PLAN-01</reqs>
  <artifact>get-shit-done/bin/lib/verify.cjs</artifact>
  <inputs>
    - PLAN.md frontmatter (parsed by frontmatter.cjs) -> requirements field, task XML
    - FEATURE.md -> REQ ID list from trace table
  </inputs>
  <done>gsd plan validate returns 0 errors for a well-formed plan</done>
</task>
```

<!-- 5 task fields (locked from CONTEXT.md) -->
- REQs — pointers to requirement specs (prevents orphan work, scope creep)
- Artifact — exact file path to create/modify (prevents wrong file, wrong location)
- Inputs — upstream artifacts with key columns/shape (prevents wrong data dependencies)
- Done — observable exit condition (prevents gold-plating and under-building)
- Title — what the task does
Intentionally omitted: steps/substeps, implementation notes, priority/order, estimates, dependencies

<!-- Self-critique finding format (from RESEARCH.md) -->
```javascript
// Each finding returned by planner
{ category: 'coverage_gap|assumption|ambiguity', description: '...', suggestion: '...', reqs_affected: ['REQ-01'] }
```

<!-- CLI validation command (built in 03-01) -->
```bash
node gsd-tools.cjs plan-validate <req-source> <plan1> [plan2...] [--raw]
# Returns: { passed: bool, errors: [...], warnings: [...], summary: {...} }
```

<!-- Existing plan-phase workflow steps that change -->
Step 8: Spawn gsd-planner -> NOW returns plan + findings
Step 9: Handle planner return -> NOW routes to Q&A if findings exist
Step 10-12: Plan checker loop -> KEPT but scope reduced (feasibility only)
Step 13: Present status -> NOW includes validation + finalization gate
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite gsd-planner.md with v2 task schema and self-critique loop</name>
  <files>agents/gsd-planner.md</files>
  <action>
Read the existing v1 `agents/gsd-planner.md` (42KB). Rewrite it as v2 — clean break, not additive edit. Target ~35KB or less (strip v1 cruft, tighten prose).

**What stays (structural sections that work):**
- `<role>` — update for v2 responsibilities
- `<context_fidelity>` — user decision fidelity (locked decisions, deferred ideas, discretion)
- `<philosophy>` — solo developer + Claude, plans are prompts, quality degradation curve
- `<discovery_levels>` — mandatory discovery protocol
- `<dependency_graph>` — building dependency graphs, vertical slices
- `<scope_estimation>` — context budget rules, split signals
- `<goal_backward>` — goal-backward methodology
- `<checkpoints>` — checkpoint types
- Frontmatter and output format sections

**What changes:**

1. **`<task_breakdown>` section — REPLACE entirely.** New section defines v2 task anatomy:
   - Five fields: `<title>`, `<reqs>`, `<artifact>`, `<inputs>`, `<done>`
   - Drop v1 fields: `<name>` (replaced by `<title>`), `<action>` (replaced by `<inputs>` + `<done>` — executor decides how), `<verify>` (absorbed into `<done>`), `<files>` (replaced by `<artifact>`)
   - Keep `type` attribute on `<task>` element (auto, checkpoint:*)
   - Task granularity: one atomic commit per task (from CONTEXT.md)
   - Wave-based grouping (from CONTEXT.md)
   - Cross-layer constraint: "A task MUST NOT reference both EU-xx and TC-xx REQs. Bridge through FN if needed."

2. **`<plan_format>` section — UPDATE task XML examples** to use v2 format. Keep frontmatter spec unchanged (wave, depends_on, files_modified, autonomous, requirements, must_haves). Add `<reqs>` to task examples.

3. **ADD new `<self_critique>` section** after `<task_breakdown>`. Content:

```markdown
<self_critique>

## After Drafting: Self-Critique (2 rounds max)

After producing all PLAN.md files, run two critique rounds before returning.

### Round 1 — Fix Silently
For each requirement ID in the phase requirement list:
  - Is there a task with this REQ in its <reqs>? If no -> add a task covering it.
For each task in every plan:
  - Does every REQ in <reqs> exist in the phase requirements? If no -> remove the reference.
  - Does <artifact> specify an exact file path? If no -> fix it.
  - Does <inputs> name specific upstream artifacts with data shape? If no -> fix it.
  - Does <done> state an observable exit condition (not "it works")? If no -> rewrite it.

Apply all fixes. Do not surface Round 1 fixes to the user.

### Round 2 — Surface Issues
Re-scan after Round 1 fixes. For any remaining issues, create a finding:

Finding categories:
- **coverage_gap**: REQ has a task but the task's scope doesn't fully address the REQ
- **assumption**: Planner assumed an approach without explicit requirement backing (e.g., "assumed sync not async")
- **ambiguity**: REQ is interpretable multiple ways and the chosen interpretation affects implementation

Each finding:
{ category, description, suggestion, reqs_affected }

### Hard Stop
After Round 2, return plan files + findings list.
Do not attempt Round 3. Do not resolve ambiguous findings — that is the user's job.
Unresolved issues after Round 2 are surfaced to the user as-is.

</self_critique>
```

4. **UPDATE `<role>` section** — add self-critique and v2 task schema to core responsibilities. Add "returns findings to orchestrator" to the output description.

5. **ADD pipeline prerequisite** to `<role>` or `<project_context>`: "Planner requires RESEARCH.md to exist before starting — no inline research, no --skip-research."

6. **Trim aggressively:**
   - Remove redundant examples where a single example suffices
   - Remove v1-specific task format examples
   - Tighten prose — the agent is Sonnet, it doesn't need paragraphs of motivation
   - Target: every section earns its token cost

**Key constraints from CONTEXT.md:**
- Five fields per task, no more (PLAN-01)
- Self-critique is internal to planner, not a separate agent (PLAN-02)
- Hard stop at 2 rounds (PLAN-02)
- Fix obvious gaps silently, surface ambiguous issues (PLAN-02)
- Flag implementation assumptions (PLAN-02)
  </action>
  <verify>
    <automated>wc -c agents/gsd-planner.md; grep -c "self_critique" agents/gsd-planner.md; grep -c "<reqs>" agents/gsd-planner.md; grep -c "<artifact>" agents/gsd-planner.md; grep -c "Round 1\|Round 2\|Hard Stop" agents/gsd-planner.md</automated>
  </verify>
  <done>gsd-planner.md is a clean v2 rewrite under 40KB. Contains self_critique section with 2-round loop. Task examples use v2 schema (title/reqs/artifact/inputs/done). No v1 task format remains.</done>
</task>

<task type="auto">
  <name>Task 2: Update plan-phase.md workflow with Q&A loop, validation gate, and finalization confirmation</name>
  <files>get-shit-done/workflows/plan-phase.md</files>
  <action>
Read the existing `get-shit-done/workflows/plan-phase.md`. Make targeted modifications to steps 9-13. Do NOT rewrite the entire workflow — steps 1-8 and 14 stay mostly unchanged.

**Modifications:**

### Step 9 — Handle Planner Return (MODIFY)

After "PLANNING COMPLETE", add new flow before routing to plan-checker:

```markdown
## 9. Handle Planner Return

- **`## PLANNING COMPLETE`:** Extract plan file paths and findings list from planner output.

  **If findings exist:** Proceed to step 9.5 (Q&A loop).
  **If no findings:** Proceed to step 9.7 (CLI validation).

- **`## CHECKPOINT REACHED`:** (unchanged)
- **`## PLANNING INCONCLUSIVE`:** (unchanged)
```

### Step 9.5 — Q&A Loop (NEW)

```markdown
## 9.5. Present Findings to User

Display banner:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 GSD ► PLANNER FINDINGS ({count} items)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

For each finding (one at a time):

Display:
```
Finding {N}/{total}: [{category}]
{description}

Suggestion: {suggestion}
Affected REQs: {reqs_affected}

Options:
  1. Accept — planner's suggestion is good
  2. Provide feedback — tell the planner what to change
  3. Research guidance — point to what needs investigating
```

**If Accept:** Mark finding resolved. Next finding.

**If Feedback:** Record user feedback. After all findings processed, re-spawn planner in revision mode with feedback applied. Re-run self-critique. If new findings emerge, add to queue and continue.

**If Research Guidance:** Spawn researcher with guidance. Read result. Revise affected plan tasks. If finding resolved, next. If new findings/assumptions surface, add to queue.

After all findings resolved: proceed to step 9.7.
```

### Step 9.7 — CLI Validation (NEW)

```markdown
## 9.7. Run CLI Validation

```bash
VALIDATE=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" plan-validate "${REQ_SOURCE}" ${PLAN_FILES} --raw)
```

Parse JSON result.

**If `passed` is true and warnings exist:**
Display warnings. These are informational — uncovered REQs surface as planner awareness, not blockers.

**If `passed` is false (errors exist):**
Display errors with fix guidance:
```
VALIDATION ERRORS (must fix before finalizing):

  ERROR: orphan_task — Task "{title}" in {plan} has no REQ references
    Fix: Add <reqs> with at least one requirement ID

  ERROR: phantom_reference — REQ {id} in Task "{title}" not found in {source}
    Fix: Remove invalid REQ reference or add REQ to source file

  ERROR: cross_layer_mixing — Task "{title}" mixes EU and TC layers
    Fix: Split task or bridge through FN-layer REQ
```

Re-spawn planner in revision mode with validation errors. Loop until validation passes (max 2 attempts). If still failing after 2 attempts, present errors to user for manual resolution.

**If `passed` is true and no warnings:** Proceed to step 9.9.
```

### Step 9.9 — Finalization Gate (NEW)

```markdown
## 9.9. Finalize Plans

Display plan summary:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 GSD ► PLAN SUMMARY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Phase {X}: {name}
Plans: {count}
Tasks: {total across all plans}
Waves: {wave count}

Plan 01: {objective} ({task_count} tasks, wave {wave})
Plan 02: {objective} ({task_count} tasks, wave {wave})
...

Validation: PASSED
Findings resolved: {count}
```

Ask user: "Finalize this plan?"

**If user confirms:** Proceed to step 10 (plan-checker) if enabled, otherwise step 13.
**If user does not confirm:** Ask what needs changing. Re-spawn planner with feedback. Return to step 9.
```

### Step 10-12 — Plan Checker (MODIFY scope note)

Add a note at the top of step 10:

```markdown
**Scope note:** With v2 self-critique handling coverage and CLI validation handling structural rules, the plan-checker focuses on execution feasibility: Can an executor actually implement these tasks? Are the artifact paths valid? Are the input data shapes realistic?
```

No other changes to steps 10-12 — the revision loop mechanics stay the same.

### REQ_SOURCE detection

In step 7 (Use Context Paths from INIT), add logic to detect the requirement source for validation:

```bash
# Determine requirement source for plan-validate
# Feature-level: FEATURE.md has EU/FN/TC trace table
# Phase-level: REQUIREMENTS.md has project-level REQ IDs
REQ_SOURCE="${FEATURE_PATH:-$REQUIREMENTS_PATH}"
```

The INIT JSON should already provide these paths. If `FEATURE_PATH` is not available in current init, fall back to `REQUIREMENTS_PATH`.
  </action>
  <verify>
    <automated>grep -c "9\.5\|9\.7\|9\.9\|plan-validate\|Finalize this plan" get-shit-done/workflows/plan-phase.md</automated>
  </verify>
  <done>plan-phase.md contains: step 9.5 (Q&A one-at-a-time with 3 options), step 9.7 (CLI validation gate), step 9.9 (explicit finalization confirmation). plan-validate CLI is wired in step 9.7. No auto-finalize path exists.</done>
</task>

</tasks>

<verification>
1. gsd-planner.md uses v2 task schema (title/reqs/artifact/inputs/done) in all examples
2. gsd-planner.md contains self-critique section with Round 1 (fix silently) and Round 2 (surface findings)
3. gsd-planner.md specifies hard stop after Round 2
4. plan-phase.md presents findings one-at-a-time (not batched)
5. plan-phase.md offers 3 response options per finding: Accept, Feedback, Research Guidance
6. plan-phase.md runs gsd plan-validate after findings resolved
7. plan-phase.md requires explicit "Finalize this plan?" confirmation
8. plan-phase.md does not auto-finalize under any code path
9. No v1 task format (`<action>`, `<verify>`, `<files>`, `<name>`) remains in gsd-planner.md examples
</verification>

<success_criteria>
- Planner produces plans with per-task REQ traceability (PLAN-01)
- Self-critique catches coverage gaps and surfaces assumptions (PLAN-02)
- User sees findings and provides guidance before finalization (PLAN-03)
- Explicit user confirmation required to finalize (PLAN-04)
- CLI validation enforces zero-orphan-task rule (REQS-03)
- Traceability table integration via plan-validate (REQS-04)
</success_criteria>

<output>
After completion, create `.planning/phases/03-planning-pipeline/03-02-SUMMARY.md`
</output>

---
phase: 09-structure-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gsd/discuss-phase.md
  - commands/gsd/help.md
  - commands/gsd/map-codebase.md
  - commands/gsd/new-milestone.md
  - commands/gsd/pause-work.md
  - commands/gsd/quick.md
  - commands/gsd/verify-work.md
  - get-shit-done/workflows/framing-pipeline.md
  - get-shit-done/workflows/resume-work.md
  - get-shit-done/workflows/transition.md
  - get-shit-done/workflows/progress.md
  - get-shit-done/workflows/execute-plan.md
  - get-shit-done/workflows/plan-phase.md
  - agents/gsd-plan-checker.md
  - agents/gsd-planner.md
  - hooks/gsd-context-monitor.js
autonomous: true
requirements: [INTG-02, DIR-03]

must_haves:
  truths:
    - "No command file references a deleted workflow"
    - "No surviving workflow references /gsd:discuss-phase, /gsd:verify-work, /gsd:pause-work, /gsd:new-milestone, or /gsd:complete-milestone"
    - "Context-monitor hook references valid v2 commands (not deleted /gsd:pause-work)"
    - "Statusline hook confirmed working with no stale references"
  artifacts:
    - path: "hooks/gsd-context-monitor.js"
      provides: "Context warning hook with valid v2 references"
    - path: "hooks/gsd-statusline.js"
      provides: "Statusline hook (confirmed no changes needed)"
  key_links:
    - from: "hooks/gsd-context-monitor.js"
      to: "STATE.md"
      via: "Generic save-state instruction (no command dependency)"
      pattern: "STATE.md"
---

<objective>
Delete 7 orphaned command files that reference deleted workflows, fix all broken slash-command references in surviving workflows/agents, and fix stale references in hooks.

Purpose: Eliminate dead chains and broken references that would cascade into errors during pipeline execution. This is the foundation cleanup that Plan 03 (directory model) depends on.
Output: Clean command surface, working workflow references, verified hooks.
</objective>

<execution_context>
@/Users/philliphall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philliphall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-structure-integration/09-CONTEXT.md
@.planning/phases/09-structure-integration/09-RESEARCH-path-refs.md
@.planning/phases/09-structure-integration/09-RESEARCH-hooks.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete orphaned commands and fix broken slash-command references in surviving workflows and agents</name>
  <files>
    commands/gsd/discuss-phase.md
    commands/gsd/help.md
    commands/gsd/map-codebase.md
    commands/gsd/new-milestone.md
    commands/gsd/pause-work.md
    commands/gsd/quick.md
    commands/gsd/verify-work.md
    get-shit-done/workflows/transition.md
    get-shit-done/workflows/progress.md
    get-shit-done/workflows/execute-plan.md
    get-shit-done/workflows/plan-phase.md
    get-shit-done/workflows/resume-work.md
    agents/gsd-plan-checker.md
    agents/gsd-planner.md
    agents/gsd-phase-researcher.md
  </files>
  <action>
    **Step 1: Delete 7 orphaned command files.** These are NOT in the CMD-01 11-command surface and reference deleted workflows:
    - `commands/gsd/discuss-phase.md` (refs deleted workflow discuss-phase.md)
    - `commands/gsd/help.md` (refs deleted workflow help.md)
    - `commands/gsd/map-codebase.md` (refs deleted workflow map-codebase.md)
    - `commands/gsd/new-milestone.md` (refs deleted workflow new-milestone.md)
    - `commands/gsd/pause-work.md` (refs deleted workflow pause-work.md)
    - `commands/gsd/quick.md` (refs deleted workflow quick.md)
    - `commands/gsd/verify-work.md` (refs deleted workflow verify-work.md)

    Use `rm` for each file. Verify they're gone.

    **Step 2: Fix broken `/gsd:discuss-phase` references in surviving files.** This command is being deleted. Replace with the v2 equivalent based on context:
    - Where it means "discuss the current work unit" -> replace with `/gsd:discuss-capability` or `/gsd:discuss-feature` as appropriate
    - In `workflows/plan-phase.md`: The redirect logic at lines 153-156 that says "if no CONTEXT.md, redirect to discuss-phase" should redirect to `/gsd:discuss-capability` or `/gsd:discuss-feature` instead
    - In `workflows/plan-phase.md` line 604: Replace `/gsd:discuss-phase ${NEXT_PHASE}` with appropriate v2 next-step suggestion
    - In `workflows/transition.md` lines 393, 410, 442: Replace `/gsd:discuss-phase [X+1]` with `/gsd:discuss-capability` or `/gsd:discuss-feature`
    - In `workflows/progress.md` lines 217, 290: Replace `/gsd:discuss-phase` with `/gsd:discuss-capability`
    - In `workflows/resume-work.md` lines 181, 197, 234: Replace `/gsd:discuss-phase` with `/gsd:discuss-capability` or `/gsd:discuss-feature`
    - In `workflows/execute-plan.md` line 396: Replace `/gsd:discuss-phase` with `/gsd:discuss-capability`
    - In `agents/gsd-plan-checker.md` lines 45, 273: Replace with `/gsd:discuss-capability` or `/gsd:discuss-feature`
    - In `agents/gsd-planner.md` line 51: Replace with `/gsd:discuss-capability`
    - In `agents/gsd-phase-researcher.md` line 40: Replace with `/gsd:discuss-capability`

    **Step 3: Fix broken `/gsd:verify-work` references.**
    - In `workflows/execute-plan.md` line 395: Replace `/gsd:verify-work` with `/gsd:review` (the v2 review command)
    - In `workflows/progress.md` lines 251, 298, 325: Replace `/gsd:verify-work` with `/gsd:review`

    **Step 4: Fix broken `/gsd:new-milestone` and `/gsd:complete-milestone` references.**
    - In `workflows/execute-plan.md` line 397: Remove references to `/gsd:complete-milestone` and `/gsd:add-phase` (both deleted). Replace next-step suggestions with v2 equivalents (e.g., "create next capability" or similar).
    - In `workflows/progress.md` line 349: Replace `/gsd:new-milestone` with a note about capability/feature workflow.

    **Step 5: Fix broken `/gsd:add-phase` reference.**
    - In `workflows/execute-plan.md` line 397: Remove `/gsd:add-phase` reference (deleted command).

    **IMPORTANT:** When replacing references, read each file's surrounding context to ensure the replacement makes semantic sense. Don't blindly search-and-replace -- understand what the workflow is trying to suggest to the user and pick the correct v2 equivalent.
  </action>
  <verify>
    <automated>
      # Verify no references to deleted commands remain in surviving workflows/agents
      echo "=== Checking for stale discuss-phase refs ===" && grep -rn "discuss-phase" get-shit-done/workflows/ agents/ commands/gsd/ 2>/dev/null | grep -v "Binary" | grep -v ".md~" || echo "CLEAN"
      echo "=== Checking for stale verify-work refs ===" && grep -rn "verify-work" get-shit-done/workflows/ agents/ commands/gsd/ 2>/dev/null || echo "CLEAN"
      echo "=== Checking for stale pause-work refs ===" && grep -rn "pause-work" get-shit-done/workflows/ agents/ commands/gsd/ 2>/dev/null || echo "CLEAN"
      echo "=== Checking for stale new-milestone refs ===" && grep -rn "new-milestone" get-shit-done/workflows/ agents/ commands/gsd/ 2>/dev/null || echo "CLEAN"
      echo "=== Checking for stale complete-milestone refs ===" && grep -rn "complete-milestone" get-shit-done/workflows/ agents/ commands/gsd/ 2>/dev/null || echo "CLEAN"
      echo "=== Checking deleted commands are gone ===" && ls commands/gsd/discuss-phase.md commands/gsd/help.md commands/gsd/map-codebase.md commands/gsd/new-milestone.md commands/gsd/pause-work.md commands/gsd/quick.md commands/gsd/verify-work.md 2>&1 | grep "No such file" | wc -l
    </automated>
  </verify>
  <done>7 orphaned command files deleted. All references to /gsd:discuss-phase, /gsd:verify-work, /gsd:pause-work, /gsd:new-milestone, /gsd:complete-milestone, /gsd:add-phase in surviving workflows and agents replaced with v2 equivalents.</done>
</task>

<task type="auto">
  <name>Task 2: Fix hooks stale references and verify hook effectiveness</name>
  <files>
    hooks/gsd-context-monitor.js
    hooks/gsd-statusline.js
  </files>
  <action>
    **Step 1: Fix stale `/gsd:pause-work` references in context-monitor.js.**

    At line 101-103 (CRITICAL message), replace:
    `"If using GSD, run /gsd:pause-work to save execution state."`
    With:
    `"If using GSD, update STATE.md with current progress and inform the user."`

    At line 105-107 (WARNING message), replace:
    `"If using GSD, consider /gsd:pause-work to save state."`
    With:
    `"If using GSD, consider updating STATE.md with current progress."`

    Rationale per CONTEXT.md hooks decision: "verify + fix stale refs only, don't enhance for v2 awareness." Use generic language that doesn't depend on any specific command existing, making the hook resilient to future command changes.

    **Step 2: Verify statusline hook has no stale references.**

    Read `hooks/gsd-statusline.js` and confirm:
    - No references to phases, milestones, or deleted commands
    - No references to STATE.md fields that will be renamed
    - Pure context-window visualizer with no GSD-model coupling

    Per the hooks research: "This hook is model-agnostic -- it has zero references to phases, milestones, STATE.md, capabilities, features, or any GSD-specific concepts." Confirm this is still true, log the verification.

    **Step 3: Verify both hooks are syntactically valid.**
    Run `node -c hooks/gsd-context-monitor.js` and `node -c hooks/gsd-statusline.js` to confirm no syntax errors after edits.

    **DO NOT** touch install.js hook registration (gsd-check-update.js) -- that is deferred to Phase 12 per decision 08-04.
    **DO NOT** update docs/context-monitor.md -- prose references deferred to Phase 10 per CONTEXT.md.
  </action>
  <verify>
    <automated>
      echo "=== Syntax check ===" && node -c hooks/gsd-context-monitor.js && node -c hooks/gsd-statusline.js && echo "=== Stale refs check ===" && grep -n "pause-work" hooks/gsd-context-monitor.js hooks/gsd-statusline.js 2>/dev/null && echo "STALE REFS FOUND" || echo "CLEAN -- no stale pause-work refs"
    </automated>
  </verify>
  <done>Context-monitor hook references generic STATE.md save instruction instead of deleted /gsd:pause-work. Statusline hook verified clean. Both hooks pass syntax check. INTG-02 hooks audit complete.</done>
</task>

</tasks>

<verification>
1. `ls commands/gsd/discuss-phase.md commands/gsd/help.md commands/gsd/map-codebase.md commands/gsd/new-milestone.md commands/gsd/pause-work.md commands/gsd/quick.md commands/gsd/verify-work.md 2>&1` -- all should report "No such file"
2. `grep -rn "discuss-phase\|verify-work\|pause-work\|new-milestone\|complete-milestone\|add-phase" get-shit-done/workflows/ agents/ hooks/ commands/gsd/ 2>/dev/null` -- should return zero hits in functional code (prose/comment refs in agents may remain for Phase 10)
3. `node -c hooks/gsd-context-monitor.js && node -c hooks/gsd-statusline.js` -- both pass syntax check
</verification>

<success_criteria>
- 7 orphaned command files deleted from commands/gsd/
- Zero functional references to deleted commands (/gsd:discuss-phase, /gsd:verify-work, /gsd:pause-work, /gsd:new-milestone, /gsd:complete-milestone, /gsd:add-phase) in workflows, agents, and hooks
- All replacements use semantically correct v2 equivalents
- Both hooks pass syntax validation
- Context-monitor uses generic save-state language
</success_criteria>

<output>
After completion, create `.planning/phases/09-structure-integration/09-01-SUMMARY.md`
</output>

---
phase: 09-structure-integration
plan: 03
type: execute
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  - get-shit-done/bin/lib/init.cjs
  - get-shit-done/bin/lib/state.cjs
  - get-shit-done/bin/lib/core.cjs
  - get-shit-done/references/pipeline-invariants.md
  - agents/gsd-executor.md
  - agents/gsd-planner.md
  - agents/gsd-plan-checker.md
  - agents/gsd-verifier.md
autonomous: true
requirements: [DIR-01, DIR-02, DIR-03]

must_haves:
  truths:
    - "New v2 init functions exist for capability/feature pipeline operations (alongside v1 phase functions)"
    - "state.cjs can track active capability, active feature, pipeline position, and last agent summary"
    - "v1 phase functions remain intact for phases 10-12 execution (bootstrap trap respected)"
    - "Pipeline invariants are formally documented in a reference doc"
    - "Original pipeline agents declare expects/produces formally"
  artifacts:
    - path: "get-shit-done/bin/lib/init.cjs"
      provides: "v2 init functions: cmdInitExecuteFeature, cmdInitPlanFeature, cmdInitFeatureOp"
    - path: "get-shit-done/bin/lib/state.cjs"
      provides: "v2 state fields: active_capability, active_feature, pipeline_position, last_agent_summary"
    - path: "get-shit-done/references/pipeline-invariants.md"
      provides: "Formal documentation of 10 pipeline invariants"
  key_links:
    - from: "get-shit-done/bin/lib/init.cjs"
      to: "get-shit-done/bin/lib/core.cjs"
      via: "findCapabilityInternal + findFeatureInternal (already exist)"
      pattern: "findCapabilityInternal\\|findFeatureInternal"
    - from: "get-shit-done/bin/lib/state.cjs"
      to: ".planning/STATE.md"
      via: "buildStateFrontmatter reads/writes v2 fields"
      pattern: "active_capability\\|active_feature\\|pipeline_position"
---

<objective>
Add v2 capability/feature init and state functions alongside v1, create the pipeline invariants reference document, and add formal expects/produces declarations to original pipeline agents.

Purpose: Establish the v2 directory model support in the toolchain so that new v2 projects use capabilities/features while v1 phases/ continues working for the current build. This is the structural foundation for DIR-01/DIR-02/DIR-03.
Output: v2 init functions, v2 state tracking, pipeline invariants doc, agent contract declarations.
</objective>

<execution_context>
@/Users/philliphall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philliphall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-structure-integration/09-CONTEXT.md
@.planning/phases/09-structure-integration/09-RESEARCH-directory-model.md
@.planning/phases/09-structure-integration/09-RESEARCH-pipeline.md
@.planning/phases/09-structure-integration/09-01-SUMMARY.md
@.planning/phases/09-structure-integration/09-02-SUMMARY.md

@/Users/philliphall/.claude/get-shit-done/bin/lib/init.cjs
@/Users/philliphall/.claude/get-shit-done/bin/lib/state.cjs
@/Users/philliphall/.claude/get-shit-done/bin/lib/core.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add v2 capability/feature init functions to init.cjs and update state.cjs for v2 fields</name>
  <files>
    get-shit-done/bin/lib/init.cjs
    get-shit-done/bin/lib/state.cjs
    get-shit-done/bin/lib/core.cjs
  </files>
  <action>
    **BOOTSTRAP TRAP RULE: All v1 functions MUST remain intact. Add v2 functions ALONGSIDE, not as replacements. The project is currently running v1 phases for phases 10-12.**

    **Step 1: Add v2 init functions to init.cjs.**

    Create these NEW functions (do not modify existing v1 functions):

    1. `cmdInitPlanFeature(cwd, capSlug, featSlug)` -- v2 equivalent of cmdInitPlanPhase
       - Use `findCapabilityInternal(cwd, capSlug)` from core.cjs (already exists)
       - Use `findFeatureInternal(cwd, capSlug, featSlug)` from core.cjs (already exists)
       - Find CONTEXT.md, RESEARCH.md, PLAN.md in the feature directory
       - Count plans and summaries in the feature directory
       - Return JSON matching the shape workflows expect:
         ```json
         {
           "feature_dir": ".planning/capabilities/{cap}/features/{feat}",
           "capability_slug": "...",
           "feature_slug": "...",
           "has_research": true/false,
           "has_context": true/false,
           "plans": ["01-PLAN.md", ...],
           "summaries": ["01-SUMMARY.md", ...],
           "plan_count": N,
           "planner_model": "...",
           "researcher_model": "...",
           "checker_model": "...",
           "commit_docs": true/false,
           "research_enabled": true/false
         }
         ```

    2. `cmdInitExecuteFeature(cwd, capSlug, featSlug)` -- v2 equivalent of cmdInitExecutePhase
       - Same pattern: resolve feature dir, find plans, return execution context JSON
       - Include: plan files, wave assignments (from plan frontmatter), config settings

    3. `cmdInitFeatureOp(cwd, capSlug, featSlug, op)` -- v2 equivalent of cmdInitPhaseOp
       - Generic feature operation context (for review, doc, etc.)

    4. `cmdInitFeatureProgress(cwd)` -- v2 equivalent of cmdInitProgress
       - Iterate `.planning/capabilities/` directory instead of `.planning/phases/`
       - Count capabilities, features, plans completed per feature
       - Return progress JSON

    Register these in the gsd-tools.cjs CLI router (the main switch/case) with commands like:
    - `init plan-feature {capSlug} {featSlug}`
    - `init execute-feature {capSlug} {featSlug}`
    - `init feature-op {capSlug} {featSlug} {op}`
    - `init feature-progress`

    **Step 2: Update state.cjs for v2 STATE.md fields.**

    Add v2 field support to `buildStateFrontmatter()` and related functions:

    1. In `buildStateFrontmatter()`:
       - Add extraction for `Active capability` (rename from `Current capability`)
       - Add extraction for `Active feature` (rename from `Current feature`)
       - Add extraction for `Pipeline position` (new field: research/plan/execute/review/doc)
       - Add extraction for `Last agent summary` (new field)
       - Keep existing phase field extraction for backward compatibility

    2. In `cmdStateUpdateProgress()`:
       - Add v2 branch: if `.planning/capabilities/` exists, count capability/feature progress
       - Keep v1 branch: if `.planning/phases/` exists, use existing logic
       - Select branch based on which directory exists (or both if transitional)

    3. In `cmdStateAdvancePlan()`:
       - Add support for `Active capability` and `Active feature` fields in STATE.md body
       - Add support for `Pipeline position` field updates
       - Keep existing `Current Plan` / `Total Plans in Phase` handling for v1

    4. In `cmdStateSnapshot()`:
       - Add v2 field extraction alongside v1 fields

    **Step 3: Verify `findCapabilityInternal` and `findFeatureInternal` in core.cjs are complete.**

    Read core.cjs and verify these functions:
    - `findCapabilityInternal(cwd, slug)` -- exists, returns capability dir path
    - `findFeatureInternal(cwd, capSlug, featSlug)` -- exists, returns feature dir path

    If they need enhancement (e.g., detecting RESEARCH.md, PLAN.md in feature dirs like `cmdInitPlanPhase` does for phase dirs), add that. If complete, log verification.

    **DIR-01 verification:** Confirm that the init-project.md workflow creates `.planning/capabilities/` with no `phases/` directory for new projects. Per the directory-model research, this is already the case. If the workflow has any code that creates a `phases/` dir for new projects, flag it for removal.

    **DIR-02 verification:** Confirm that `.documentation/` structure (architecture.md, domain.md, mapping.md, capabilities/, decisions/) is created by init-project.md workflow. Per research, this is already implemented. Log verification.
  </action>
  <verify>
    <automated>
      echo "=== Verify new init functions exist ===" && grep -c "cmdInitPlanFeature\|cmdInitExecuteFeature\|cmdInitFeatureOp\|cmdInitFeatureProgress" get-shit-done/bin/lib/init.cjs
      echo "=== Verify state.cjs v2 fields ===" && grep -c "active_capability\|active_feature\|pipeline_position\|last_agent_summary\|Active capability\|Active feature\|Pipeline position" get-shit-done/bin/lib/state.cjs
      echo "=== Verify v1 functions still intact ===" && grep -c "cmdInitPlanPhase\|cmdInitExecutePhase\|cmdInitPhaseOp\|cmdInitProgress" get-shit-done/bin/lib/init.cjs
      echo "=== Verify CLI routes registered ===" && grep -c "plan-feature\|execute-feature\|feature-op\|feature-progress" get-shit-done/bin/gsd-tools.cjs
      echo "=== Syntax check ===" && node -c get-shit-done/bin/lib/init.cjs && node -c get-shit-done/bin/lib/state.cjs && echo "PASS"
    </automated>
  </verify>
  <done>v2 init functions (cmdInitPlanFeature, cmdInitExecuteFeature, cmdInitFeatureOp, cmdInitFeatureProgress) added alongside v1 functions. state.cjs supports v2 STATE.md fields (active_capability, active_feature, pipeline_position, last_agent_summary) alongside v1 fields. v1 functions untouched. DIR-01 and DIR-02 verified in init-project workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Create pipeline invariants reference doc and add expects/produces to original agents</name>
  <files>
    get-shit-done/references/pipeline-invariants.md
    agents/gsd-executor.md
    agents/gsd-planner.md
    agents/gsd-plan-checker.md
    agents/gsd-verifier.md
  </files>
  <action>
    **Step 1: Create `get-shit-done/references/pipeline-invariants.md`.**

    Document the 10 pipeline invariants from CONTEXT.md as a formal reference. For each invariant, include:
    - **What:** One-sentence description
    - **Why:** What breaks if violated
    - **Where:** File(s) that implement it
    - **Verify:** How to confirm it's working

    The 10 invariants (from CONTEXT.md):
    1. Fresh context per executor -- subagents get full 200k, orchestrator stays lean
    2. Wave dependency analysis -- plans grouped by dependency, executed in order
    3. Plan-checker verification loop -- draft -> verify -> Q&A -> confirm
    4. Atomic commits per task -- each task = one git commit
    5. Context loading via paths not content -- pass file paths, agents read themselves
    6. State progression via CLI -- gsd-tools.cjs manages STATE.md updates
    7. Session handoff -- STATE.md + SUMMARY.md provide full context for /clear recovery
    8. Requirement ID chain -- every task traces to a REQ ID
    9. Summary frontmatter -- structured SUMMARY.md per plan
    10. Spot-check on executor output -- verify claims before reporting success

    Use the implementation map from the pipeline research to populate the "Where" field for each invariant. Keep the doc concise -- this is a quick-reference, not a deep dive. Target ~150-200 lines.

    **Step 2: Add `expects` and `produces` sections to original pipeline agents.**

    Per CONTEXT.md: "Each agent .md must explicitly declare 'expects' (inputs) and 'produces' (outputs) sections." The 6 research gatherers and 5 review agents already have formal `reads`/`writes` frontmatter. The 5 original agents need updating:

    For `agents/gsd-executor.md`, add to frontmatter:
    ```yaml
    reads: [plan-file, state, config, project-instructions]
    writes: [task-commits, summary, state-updates]
    ```

    For `agents/gsd-planner.md`, add to frontmatter:
    ```yaml
    reads: [state, roadmap, requirements, context, research]
    writes: [plan-files]
    ```

    For `agents/gsd-plan-checker.md`, add to frontmatter:
    ```yaml
    reads: [plan-files, roadmap, requirements, context, research]
    writes: [checker-verdict]
    ```

    For `agents/gsd-verifier.md`, add to frontmatter:
    ```yaml
    reads: [plan-file, summary, roadmap, requirements]
    writes: [verification-report]
    ```

    Also add an `<artifact_contract>` section to each agent's body (after the role section) that documents:
    - **Expects:** List of input artifacts with paths (e.g., "PLAN.md at {feature_dir}/{nn}-PLAN.md")
    - **Produces:** List of output artifacts with paths (e.g., "SUMMARY.md at {feature_dir}/{nn}-SUMMARY.md")

    This makes the pipeline chain visible and verifiable: research produces RESEARCH.md -> planner expects RESEARCH.md + CONTEXT.md -> executor expects PLAN.md -> verifier expects PLAN.md + SUMMARY.md.

    **DO NOT** modify the research gatherer or review agents -- they already have formal reads/writes.
  </action>
  <verify>
    <automated>
      echo "=== Verify invariants doc exists ===" && test -f get-shit-done/references/pipeline-invariants.md && echo "EXISTS" || echo "MISSING"
      echo "=== Verify all 10 invariants documented ===" && grep -c "^##" get-shit-done/references/pipeline-invariants.md
      echo "=== Verify executor has reads/writes ===" && grep -c "reads:\|writes:" agents/gsd-executor.md
      echo "=== Verify planner has reads/writes ===" && grep -c "reads:\|writes:" agents/gsd-planner.md
      echo "=== Verify plan-checker has reads/writes ===" && grep -c "reads:\|writes:" agents/gsd-plan-checker.md
      echo "=== Verify verifier has reads/writes ===" && grep -c "reads:\|writes:" agents/gsd-verifier.md
      echo "=== Verify artifact_contract sections ===" && grep -c "artifact_contract" agents/gsd-executor.md agents/gsd-planner.md agents/gsd-plan-checker.md agents/gsd-verifier.md
    </automated>
  </verify>
  <done>Pipeline invariants reference document created with all 10 invariants. gsd-executor, gsd-planner, gsd-plan-checker, and gsd-verifier agents updated with formal reads/writes frontmatter and artifact_contract sections. Pipeline chain is now visible and verifiable through agent declarations.</done>
</task>

</tasks>

<verification>
1. `grep -c "cmdInitPlanFeature\|cmdInitExecuteFeature" get-shit-done/bin/lib/init.cjs` -- >= 2 (new functions exist)
2. `grep -c "cmdInitPlanPhase\|cmdInitExecutePhase" get-shit-done/bin/lib/init.cjs` -- >= 2 (v1 functions intact)
3. `node -c get-shit-done/bin/lib/init.cjs && node -c get-shit-done/bin/lib/state.cjs` -- both pass syntax check
4. `test -f get-shit-done/references/pipeline-invariants.md` -- invariants doc exists
5. `grep -l "reads:" agents/gsd-executor.md agents/gsd-planner.md agents/gsd-plan-checker.md agents/gsd-verifier.md | wc -l` -- should be 4
6. Manually confirm: init-project.md creates `.planning/capabilities/` (not phases/) for new projects (DIR-01)
7. Manually confirm: init-project.md creates `.documentation/` with correct structure (DIR-02)
</verification>

<success_criteria>
- v2 init functions exist in init.cjs alongside v1 functions (bootstrap trap respected)
- state.cjs handles v2 STATE.md fields (active_capability, active_feature, pipeline_position, last_agent_summary)
- All v1 phase functions still work (grep confirms they're unchanged)
- Pipeline invariants reference doc covers all 10 invariants with What/Why/Where/Verify
- All 4 original pipeline agents have formal reads/writes frontmatter and artifact_contract sections
- DIR-01 verified: init-project creates capabilities/ not phases/
- DIR-02 verified: init-project creates .documentation/ structure
- All modified .cjs files pass node -c syntax check
</success_criteria>

<output>
After completion, create `.planning/phases/09-structure-integration/09-03-SUMMARY.md`
</output>

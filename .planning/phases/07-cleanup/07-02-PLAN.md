---
phase: 07-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  # Conflict resolution
  - agents/gsd-planner.md
  - commands/gsd/resume-work.md
  - get-shit-done/workflows/resume-project.md
  # Deviation rules simplification
  - agents/gsd-executor.md
  - get-shit-done/workflows/execute-plan.md
  - get-shit-done/templates/summary.md
  - get-shit-done/templates/summary-standard.md
  - get-shit-done/templates/summary-complex.md
  - get-shit-done/bin/lib/template.cjs
autonomous: true
requirements: [FOUND-07]

must_haves:
  truths:
    - "Executor has clear unplanned-work guidance — simplified from 4 numbered rules to prose guidance"
    - "resume-work command and resume-project workflow use consistent naming"
    - "No dead init paths remain — init-project.md is the sole project initialization workflow"
    - "SUMMARY template deviation section reflects simplified approach"
  artifacts:
    - path: "agents/gsd-executor.md"
      provides: "Simplified unplanned-work guidance replacing 4 numbered rules"
      contains: "unplanned"
    - path: "get-shit-done/workflows/execute-plan.md"
      provides: "Simplified deviation handling matching executor"
      contains: "unplanned"
  key_links:
    - from: "agents/gsd-executor.md"
      to: "get-shit-done/workflows/execute-plan.md"
      via: "Consistent unplanned-work guidance"
      pattern: "unplanned|unexpected"
    - from: "agents/gsd-executor.md"
      to: "get-shit-done/templates/summary.md"
      via: "SUMMARY deviation format matches executor guidance"
      pattern: "Deviations|Unplanned"
---

<objective>
Resolve the 4 identified conflicts and simplify deviation rules. This is the "reconcile and tighten" plan — after Plan 01 removed dead code, this plan resolves divergences, fixes naming mismatches, and simplifies the executor's unplanned-work handling.

Purpose: Eliminate inconsistencies between v1 and v2 artifacts. Simplify the deviation rules from a rigid 4-rule taxonomy to clear prose guidance that integrates with the v1 pipeline flow, per the inventory verdict (KEEP GSD, modified).

Output: Consistent naming, resolved conflicts, and a simplified executor that handles unplanned work with clear guidance instead of numbered rules.
</objective>

<execution_context>
@/Users/philliphall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philliphall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cleanup/07-INVENTORY.md
@.planning/phases/07-cleanup/research-R1-dependency-risk.md
@.planning/phases/07-cleanup/research-R2-removal-impact.md
@.planning/phases/07-cleanup/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplify deviation rules in executor and execute-plan</name>
  <files>
    agents/gsd-executor.md
    get-shit-done/workflows/execute-plan.md
    get-shit-done/templates/summary.md
    get-shit-done/templates/summary-standard.md
    get-shit-done/templates/summary-complex.md
    get-shit-done/bin/lib/template.cjs
  </files>
  <action>
    The inventory verdict is: KEEP GSD (modified) — simplify + integrate with v1 pipeline. The 4 numbered rules (Bug auto-fix, Missing Critical, Blocking, Architectural) are being collapsed into clear prose guidance. The concept of tracking unplanned work remains; the rigid taxonomy is removed.

    **In `agents/gsd-executor.md`:**
    1. Replace the entire `<deviation_rules>` block (Rules 1-4, scope boundary, fix attempt limit — ~70 lines) with a compact `<unplanned_work>` block (~15 lines) containing:
       - **Auto-fix:** If you encounter a bug, missing dependency, or blocker during task execution — fix it and continue. Document what you fixed and why in the summary.
       - **Stop and ask:** If you discover an architectural issue that changes the plan's fundamental approach — STOP execution and ask the user. Do not attempt architectural fixes autonomously.
       - **Scope boundary:** Auto-fixes must be directly related to the current task. Do not fix unrelated issues discovered during execution.
       - **Limit:** If a single task requires more than 2 auto-fixes, pause and assess whether the plan needs revision.
    2. Update the checkpoint_protocol reference that says "ADD ONE (deviation Rule 3)" to instead say "ADD ONE (auto-fix: missing server start)". The behavior is identical — just removes the numbered rule reference.
    3. Update the `<summary_creation>` section: replace `[Rule 1 - Bug]` format with plain descriptions like "Auto-fixed: case-sensitive email uniqueness check". Keep the `## Deviations from Plan` section heading but rename it to `## Unplanned Changes`. Update the example format.
    4. Update the success_criteria checklist: change "All deviations documented" to "All unplanned changes documented".

    **In `get-shit-done/workflows/execute-plan.md`:**
    1. Replace the `<deviation_rules>` block (~30 lines) with the same compact `<unplanned_work>` guidance (matching the executor, not duplicating — keep it brief, ~10 lines, with a note that the full guidance is in gsd-executor.md).
    2. Update Pattern A description: change "follow deviation/auth rules" to "follow unplanned-work guidance + auth gates".
    3. Update task execution step: change "Implement with deviation rules + auth gates" to "Implement with auth gates. Handle unexpected issues per unplanned-work guidance."
    4. Remove "Deviations are normal — handle via rules below" (the guidance is now inline and self-evident).
    5. Update the `<deviation_documentation>` block: rename to `<unplanned_work_documentation>`. Change `[Rule N - Category]` format to plain descriptions. Keep the requirement to document unplanned changes in SUMMARY.
    6. Update Pattern B: change "track deviations" to "track unplanned changes" and "aggregate files/deviations/decisions" to "aggregate files/changes/decisions".

    **In `get-shit-done/templates/summary.md`:**
    1. Rename `## Deviations from Plan` to `## Unplanned Changes`
    2. Replace `[Rule X - Category] Brief description` format with `**Brief description** — why it was needed`
    3. Update the total count format from `**Total deviations:** [N] auto-fixed ([breakdown by rule])` to `**Unplanned changes:** [N] ([brief breakdown])`
    4. Keep "None - plan executed exactly as written." for the no-changes case
    5. Update explanatory notes to use "unplanned changes" instead of "deviations"

    **In `get-shit-done/templates/summary-standard.md`:**
    1. Rename `## Decisions & Deviations` to `## Decisions & Unplanned Changes`
    2. Update content guidance accordingly

    **In `get-shit-done/templates/summary-complex.md`:**
    1. Rename `## Deviations from Plan (Auto-fixed)` to `## Unplanned Changes`
    2. Update content guidance

    **In `get-shit-done/bin/lib/template.cjs`:**
    1. Update the section list string from `'## Decisions & Deviations'` to `'## Decisions & Unplanned Changes'`
  </action>
  <verify>
    Run: `grep -rn 'Rule 1\|Rule 2\|Rule 3\|Rule 4\|deviation_rules\|deviation Rule' agents/gsd-executor.md get-shit-done/workflows/execute-plan.md 2>/dev/null` — should return nothing (no numbered rule references).
    Run: `grep -c 'unplanned_work\|Unplanned Changes\|unplanned-work' agents/gsd-executor.md get-shit-done/workflows/execute-plan.md get-shit-done/templates/summary.md 2>/dev/null` — should show matches in all 3 files (new terminology present).
    Run: `grep -rn 'deviation_documentation' get-shit-done/workflows/execute-plan.md 2>/dev/null` — should return nothing (renamed to unplanned_work_documentation).
  </verify>
  <done>
    Deviation rules collapsed from 4 numbered rules (~100 lines across 2 files) to compact prose guidance (~25 lines total). Core behavior preserved: auto-fix small issues, stop for architectural changes. Rigid [Rule N - Category] taxonomy replaced with plain descriptions. All SUMMARY templates updated to use "Unplanned Changes" terminology. template.cjs section list updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resolve conflicts — resume naming fix and dead init path cleanup</name>
  <files>
    commands/gsd/resume-work.md
    get-shit-done/workflows/resume-project.md
    agents/gsd-planner.md
  </files>
  <action>
    **Conflict 1: resume-work vs resume-project naming mismatch**

    The command is `resume-work.md` but it calls the workflow `resume-project.md`. Per inventory: "Fix name mismatch."

    Read both files. The command file (`commands/gsd/resume-work.md`) is the user-facing entry point. The workflow file (`get-shit-done/workflows/resume-project.md`) is the implementation. The command references the workflow by path.

    Resolution: Rename the workflow to match the command. Rename `get-shit-done/workflows/resume-project.md` to `get-shit-done/workflows/resume-work.md`. Update the `@` reference in `commands/gsd/resume-work.md` if it points to `resume-project.md`. Search for any other references to `resume-project.md` across the codebase and update them to `resume-work.md`.

    Use `git mv` for the rename to preserve history.

    **Conflict 2: init-project.md wins over new-project.md**

    Per inventory: "init-project.md wins. Unified entry point replaces separate new-project." The `init.md` command + `init-project.md` workflow is the v2 unified entry point. The `new-project.md` command + workflow is the v1 separate path.

    However, `new-project.md` is still a KEPT GSD workflow — it's the installed version that users currently run. Do NOT delete it. Instead:
    - In `commands/gsd/new-project.md`, add a note at the top: "Note: For new projects, prefer `/gsd:init` which auto-detects new vs existing projects."
    - Verify `init.md` command and `init-project.md` workflow exist and are functional.

    **Conflict 3: gsd-planner.md — project version wins**

    Per inventory: "Project wins. Cherry-pick any useful accumulated patches from installed version."

    The project version (28K, agents/gsd-planner.md) is the v2 version with self-critique, REQ traceability, and v2 task schema. The installed version (44K, ~/.claude/agents/gsd-planner.md) has accumulated patches from active use.

    Read the installed version at `~/.claude/agents/gsd-planner.md`. Compare key sections with the project version. Look for:
    - Bug fixes or behavioral improvements not in the project version
    - Additional edge case handling
    - Improved prompt patterns

    Note: Plan 01 already removed TDD references from the project gsd-planner.md. Any cherry-picked content should NOT include TDD references.

    If meaningful patches are found, incorporate them into the project `agents/gsd-planner.md`. If the installed version is just larger due to v1 cruft (milestone/phase terminology, old plan format), no cherry-picking needed.

    Document what was cherry-picked (or that nothing was) in the summary.
  </action>
  <verify>
    Run: `ls get-shit-done/workflows/resume-work.md 2>/dev/null && echo "EXISTS" || echo "MISSING"` — should print EXISTS.
    Run: `ls get-shit-done/workflows/resume-project.md 2>/dev/null && echo "EXISTS" || echo "MISSING"` — should print MISSING (renamed).
    Run: `grep -rn 'resume-project' commands/ get-shit-done/workflows/ 2>/dev/null` — should return nothing (all references updated to resume-work).
    Run: `grep -rn 'gsd:init' commands/gsd/new-project.md 2>/dev/null` — should show the preference note.
    Run: `head -5 agents/gsd-planner.md` — should show the v2 planner (project version).
  </verify>
  <done>
    Resume naming mismatch fixed: workflow renamed from resume-project.md to resume-work.md, all references updated. Init path clarified: new-project.md has note pointing to /gsd:init. Planner conflict resolved: project version wins, any useful patches cherry-picked from installed version.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn 'Rule 1.*Bug\|Rule 2.*Missing\|Rule 3.*Blocking\|Rule 4.*Architectural' agents/ get-shit-done/ --include='*.md' 2>/dev/null` — zero matches (numbered rules gone)
2. `grep -c 'unplanned' agents/gsd-executor.md` — at least 3 matches (guidance block present)
3. `ls get-shit-done/workflows/resume-work.md` — exists
4. `ls get-shit-done/workflows/resume-project.md 2>&1` — "No such file"
5. `grep -rn 'resume-project\.md' commands/ get-shit-done/ agents/ --include='*.md' 2>/dev/null` — zero matches
6. `node get-shit-done/bin/gsd-tools.cjs current-timestamp` — runs without error
</verification>

<success_criteria>
- Deviation rules simplified: 4 numbered rules replaced with compact prose guidance in executor and execute-plan
- SUMMARY templates use "Unplanned Changes" instead of "Deviations from Plan" with [Rule N] format
- resume-work/resume-project naming mismatch resolved
- gsd-planner.md conflict resolved (project version wins, patches cherry-picked if valuable)
- init-project.md confirmed as primary init path with note in new-project.md
- All verification commands pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-cleanup/07-02-SUMMARY.md`
</output>

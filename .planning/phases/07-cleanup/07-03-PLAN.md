---
phase: 07-cleanup
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified: []
autonomous: false
requirements: [FOUND-07]

must_haves:
  truths:
    - "Every pipeline stage fires without error after cleanup"
    - "No broken @file references in any workflow or agent"
    - "No missing CLI command dispatches — every gsd-tools.cjs command still works"
    - "Pipeline produces artifacts at each stage"
  artifacts: []
  key_links:
    - from: "commands/gsd/init.md"
      to: "get-shit-done/workflows/init-project.md"
      via: "slash command spawns workflow"
      pattern: "init-project"
    - from: "get-shit-done/workflows/plan-phase.md"
      to: "agents/gsd-planner.md"
      via: "workflow spawns planner agent"
      pattern: "gsd-planner"
    - from: "get-shit-done/workflows/execute-phase.md"
      to: "agents/gsd-executor.md"
      via: "workflow spawns executor agent"
      pattern: "gsd-executor"
---

<objective>
Run a full pipeline smoke test to confirm cleanup didn't break any stage. Per user decision: "Final step: full pipeline smoke test on a throwaway branch — every stage fires, every agent runs, every artifact produced. Smoke test is baked into phase execution — cleanup isn't 'done' until pipeline runs clean."

Purpose: Validate that Plans 01 and 02 removed dead code without breaking live pipeline paths. This is the gate — cleanup is not complete until the pipeline runs end-to-end.

Output: Verified pipeline integrity. Every @file reference resolves, every CLI command dispatches, every workflow parses.
</objective>

<execution_context>
@/Users/philliphall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philliphall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cleanup/07-01-SUMMARY.md
@.planning/phases/07-cleanup/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate all @file references and CLI command dispatch</name>
  <files></files>
  <action>
    This is a read-only validation task. No files are modified.

    **Step 1 — Extract and validate all @file references:**
    Scan every `.md` file under `get-shit-done/`, `agents/`, and `commands/` for `@` references pointing to other files (patterns like `@/Users/...`, `@.planning/...`, `@get-shit-done/...`). For each reference, verify the target file exists on disk. Report any broken references.

    Specifically check:
    - `get-shit-done/workflows/*.md` — all `<execution_context>` blocks
    - `get-shit-done/workflows/*.md` — all `<context>` blocks with @file refs
    - `agents/*.md` — any embedded @file references
    - `commands/gsd/*.md` — workflow references in slash commands

    **Step 2 — Validate CLI command dispatch:**
    Run each top-level gsd-tools.cjs command to confirm it dispatches without import errors:
    ```
    node get-shit-done/bin/gsd-tools.cjs --help
    node get-shit-done/bin/gsd-tools.cjs current-timestamp
    node get-shit-done/bin/gsd-tools.cjs init --help 2>&1 || true
    node get-shit-done/bin/gsd-tools.cjs commit --help 2>&1 || true
    node get-shit-done/bin/gsd-tools.cjs frontmatter --help 2>&1 || true
    node get-shit-done/bin/gsd-tools.cjs verify --help 2>&1 || true
    ```
    Each should either show help text or run successfully — none should throw an unhandled error or reference a removed function.

    **Step 3 — Verify no removed concepts leak:**
    Run final grep sweep for any remaining references to removed concepts:
    ```
    grep -rn 'tdd_execution\|tdd_integration\|tdd_plan_execution' get-shit-done/ agents/ commands/ --include='*.md' --include='*.cjs'
    grep -rn 'cmdListTodos\|cmdTodoComplete\|cmdInitTodos\|cmdValidateHealth' get-shit-done/ agents/ commands/ --include='*.cjs'
    grep -rn 'discovery-phase\.md\|new-project\.md\.bak' get-shit-done/ agents/ commands/ --include='*.md'
    ```
    All three should return zero matches.

    **If any failures found:** Document exactly which reference is broken, which file contains it, and what it points to. Do NOT auto-fix — report in summary so the issue can be diagnosed.
  </action>
  <verify>
    All @file references resolve to existing files. All CLI commands dispatch without unhandled errors. All removed-concept greps return zero matches.
  </verify>
  <done>
    Pipeline integrity validated: all @file references resolve, all CLI commands dispatch, no removed concepts leak through. Cleanup is confirmed safe.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User confirms pipeline integrity after cleanup</name>
  <action>
    Present smoke test results from Task 1 to the user for review. This is the gate — cleanup is not "done" until the user confirms the pipeline runs clean.

    What was built: Plans 01 and 02 performed all cleanup (archive dead files, remove references, simplify deviation rules, resolve conflicts). Task 1 ran automated validation.

    How to verify:
    1. Review the Task 1 output — confirm all @file references resolve and all CLI commands work
    2. Optionally run a quick manual test: invoke a GSD command (e.g., `/gsd:help` or `/gsd:progress`) to confirm the pipeline responds correctly
    3. Confirm no regressions in the workflow you use most frequently
    4. If satisfied, the cleanup phase is complete
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>User has confirmed pipeline integrity. Phase 7 cleanup is complete.</done>
</task>

</tasks>

<verification>
1. Zero broken @file references across all workflows, agents, and commands
2. All gsd-tools.cjs commands dispatch without import/reference errors
3. Zero grep matches for removed concepts (TDD execution blocks, todo CLI functions, health CLI functions, dead artifact filenames)
4. User confirms pipeline works end-to-end
</verification>

<success_criteria>
- Every @file reference in every workflow and agent resolves to an existing file
- Every gsd-tools.cjs command dispatches without unhandled errors
- Zero references to removed concepts remain
- User has verified pipeline integrity
</success_criteria>

<output>
After completion, create `.planning/phases/07-cleanup/07-03-SUMMARY.md`
</output>

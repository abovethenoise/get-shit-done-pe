---
phase: 07-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # Archived (moved to .archive/07-cleanup/)
  - get-shit-done/references/tdd.md
  - get-shit-done/workflows/add-todo.md
  - get-shit-done/workflows/check-todos.md
  - get-shit-done/workflows/health.md
  - get-shit-done/workflows/discovery-phase.md
  - commands/gsd/add-todo.md
  - commands/gsd/check-todos.md
  - commands/gsd/health.md
  - commands/gsd/new-project.md.bak
  - .archive/07-cleanup/
  - .gitignore
  # CLI cleanup
  - get-shit-done/bin/gsd-tools.cjs
  - get-shit-done/bin/lib/commands.cjs
  - get-shit-done/bin/lib/init.cjs
  - get-shit-done/bin/lib/verify.cjs
  # Reference cleanup in kept files
  - get-shit-done/workflows/execute-plan.md
  - get-shit-done/workflows/execute-phase.md
  - get-shit-done/workflows/plan-phase.md
  - get-shit-done/workflows/discuss-phase.md
  - get-shit-done/workflows/help.md
  - get-shit-done/workflows/progress.md
  - get-shit-done/workflows/resume-project.md
  - get-shit-done/workflows/new-milestone.md
  - get-shit-done/workflows/pause-work.md
  - agents/gsd-executor.md
  - agents/gsd-planner.md
  - agents/gsd-plan-checker.md
  - get-shit-done/templates/phase-prompt.md
  - get-shit-done/templates/planner-subagent-prompt.md
  - get-shit-done/templates/summary.md
  - get-shit-done/templates/state.md
  - get-shit-done/references/git-integration.md
autonomous: true
requirements: [FOUND-07]

must_haves:
  truths:
    - "No TDD references remain in any workflow, agent, template, or CLI file"
    - "No todo system references remain — no /gsd:add-todo, /gsd:check-todos, init todos, list-todos"
    - "No health check references remain — no /gsd:health, validate health"
    - "Dead artifacts (new-project.md.bak, discovery-phase.md) are archived, not at original paths"
    - "All kept workflows still parse correctly (no dangling @file references)"
    - "gsd-tools.cjs dispatches no removed commands"
    - "Archived files exist in .archive/07-cleanup/ preserving relative paths"
    - "Archive directory is listed in .gitignore"
  artifacts:
    - path: "get-shit-done/references/tdd.md"
      provides: "MUST NOT EXIST at original path after cleanup"
    - path: "get-shit-done/workflows/add-todo.md"
      provides: "MUST NOT EXIST at original path after cleanup"
    - path: "get-shit-done/workflows/check-todos.md"
      provides: "MUST NOT EXIST at original path after cleanup"
    - path: "get-shit-done/workflows/health.md"
      provides: "MUST NOT EXIST at original path after cleanup"
    - path: "get-shit-done/workflows/discovery-phase.md"
      provides: "MUST NOT EXIST at original path after cleanup"
    - path: "commands/gsd/new-project.md.bak"
      provides: "MUST NOT EXIST at original path after cleanup"
    - path: "commands/gsd/health.md"
      provides: "MUST NOT EXIST at original path after cleanup"
    - path: "commands/gsd/add-todo.md"
      provides: "MUST NOT EXIST at original path after cleanup"
    - path: "commands/gsd/check-todos.md"
      provides: "MUST NOT EXIST at original path after cleanup"
    - path: ".archive/07-cleanup/"
      provides: "Archive directory containing all 9 removed files with preserved relative paths"
    - path: ".gitignore"
      provides: "Contains .archive/ entry"
  key_links:
    - from: "get-shit-done/workflows/execute-phase.md"
      to: "get-shit-done/references/tdd.md"
      via: "@file reference in execution_context"
      pattern: "@.*tdd\\.md"
      must_state: "REMOVED — no @tdd.md reference"
    - from: "get-shit-done/workflows/progress.md"
      to: "init todos"
      via: "CLI call for todo count"
      pattern: "init todos|list-todos"
      must_state: "REMOVED — no todo CLI calls"
---

<objective>
Remove all REMOVE and DROP items identified in the cleanup inventory: TDD execution pattern (13 files), Todo system (workflows + CLI), Health check (workflow + CLI), and dead artifacts (.bak file, discovery-phase.md). Per user decision, removed files are archived (not deleted) for local recoverability.

Purpose: Eliminate dead code, obsolete features, and unused artifacts that don't serve the v2 pipeline. This is the bulk of the cleanup — mechanical removal with exact line references from R2 research.

Output: A codebase with zero references to TDD execution, todo management, health checks, or dead artifacts. Removed files preserved in `.archive/07-cleanup/`. All kept workflows remain functional with no dangling references.
</objective>

<execution_context>
@/Users/philliphall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philliphall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cleanup/07-INVENTORY.md
@.planning/phases/07-cleanup/research-R2-removal-impact.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive removed files, update .gitignore, and clean CLI commands</name>
  <files>
    get-shit-done/references/tdd.md
    get-shit-done/workflows/add-todo.md
    get-shit-done/workflows/check-todos.md
    get-shit-done/workflows/health.md
    get-shit-done/workflows/discovery-phase.md
    commands/gsd/add-todo.md
    commands/gsd/check-todos.md
    commands/gsd/health.md
    commands/gsd/new-project.md.bak
    .archive/07-cleanup/
    .gitignore
    get-shit-done/bin/gsd-tools.cjs
    get-shit-done/bin/lib/commands.cjs
    get-shit-done/bin/lib/init.cjs
    get-shit-done/bin/lib/verify.cjs
  </files>
  <action>
    **Step 1 — Archive 9 files (not delete):**
    Per user decision: "Archive + gitignore — removed files move to archive directory, added to .gitignore. Not truly deleted, recoverable locally."

    Create archive directory preserving relative paths, then move each file:
    ```
    mkdir -p .archive/07-cleanup/get-shit-done/references
    mkdir -p .archive/07-cleanup/get-shit-done/workflows
    mkdir -p .archive/07-cleanup/commands/gsd
    mv get-shit-done/references/tdd.md .archive/07-cleanup/get-shit-done/references/tdd.md
    mv get-shit-done/workflows/add-todo.md .archive/07-cleanup/get-shit-done/workflows/add-todo.md
    mv get-shit-done/workflows/check-todos.md .archive/07-cleanup/get-shit-done/workflows/check-todos.md
    mv get-shit-done/workflows/health.md .archive/07-cleanup/get-shit-done/workflows/health.md
    mv get-shit-done/workflows/discovery-phase.md .archive/07-cleanup/get-shit-done/workflows/discovery-phase.md
    mv commands/gsd/add-todo.md .archive/07-cleanup/commands/gsd/add-todo.md
    mv commands/gsd/check-todos.md .archive/07-cleanup/commands/gsd/check-todos.md
    mv commands/gsd/health.md .archive/07-cleanup/commands/gsd/health.md
    mv commands/gsd/new-project.md.bak .archive/07-cleanup/commands/gsd/new-project.md.bak
    ```

    **Step 2 — Add archive to .gitignore:**
    Append `.archive/` to `.gitignore` (create the file if it doesn't exist). This ensures archived files stay local and don't pollute the repo.

    **Step 3 — Clean gsd-tools.cjs (use R2 line references as guide, find actual lines by searching for the text):**
    - Remove `list-todos` from help text header
    - Remove `todo complete` from help text
    - Remove `init todos` from init section help text
    - Remove `--type execute|tdd` — change to just `--type execute` in help text
    - Remove `case 'list-todos'` dispatch handler
    - Remove `case 'todo'` dispatch handler block (the `todo complete` handler)
    - Remove `case 'todos'` in init switch
    - Remove `todos` from init workflow available list in error message
    - Remove `validate health` from help text
    - Remove `else if (subcommand === 'health')` branch in validate dispatch
    - Update validate error message to remove `health` from available subcommands

    **Step 4 — Clean commands.cjs:**
    - Remove `cmdListTodos()` function entirely
    - Remove `cmdTodoComplete()` function entirely
    - Remove both from exports

    **Step 5 — Clean init.cjs:**
    - Remove `cmdInitTodos()` function entirely
    - Remove from exports

    **Step 6 — Clean verify.cjs:**
    - Remove `cmdValidateHealth()` function entirely
    - Remove from exports
    - Update file header comment to remove "health validation"
    - In remaining validation code, replace any `/gsd:health --repair` fix-instruction strings with plain text like "Manually verify .planning/ structure"
  </action>
  <verify>
    Run: `node get-shit-done/bin/gsd-tools.cjs --help 2>&1 | grep -E 'todo|tdd|health' ; echo "EXIT:$?"` — should print nothing except EXIT:0 (grep finds no matches).
    Run: `ls get-shit-done/references/tdd.md get-shit-done/workflows/add-todo.md get-shit-done/workflows/check-todos.md get-shit-done/workflows/health.md get-shit-done/workflows/discovery-phase.md commands/gsd/add-todo.md commands/gsd/check-todos.md commands/gsd/health.md commands/gsd/new-project.md.bak 2>&1 | grep -c "No such file"` — should return 9 (none at original paths).
    Run: `ls .archive/07-cleanup/get-shit-done/references/tdd.md .archive/07-cleanup/get-shit-done/workflows/add-todo.md .archive/07-cleanup/commands/gsd/health.md 2>/dev/null | wc -l` — should return 3 (spot-check archived files exist).
    Run: `grep -c '.archive/' .gitignore` — should return at least 1.
    Run: `node get-shit-done/bin/gsd-tools.cjs current-timestamp` — confirms CLI still loads without errors.
  </verify>
  <done>
    All 9 files archived to .archive/07-cleanup/ preserving relative paths. .gitignore updated with .archive/ entry. CLI commands for todos (list-todos, todo complete, init todos), TDD (--type tdd), and health (validate health) removed from gsd-tools.cjs and supporting lib files. CLI still runs for remaining commands. Files recoverable locally from archive.
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean TDD, Todo, and Health references from kept workflows, agents, and templates</name>
  <files>
    get-shit-done/workflows/execute-plan.md
    get-shit-done/workflows/execute-phase.md
    get-shit-done/workflows/plan-phase.md
    get-shit-done/workflows/discuss-phase.md
    get-shit-done/workflows/help.md
    get-shit-done/workflows/progress.md
    get-shit-done/workflows/resume-project.md
    get-shit-done/workflows/new-milestone.md
    get-shit-done/workflows/pause-work.md
    agents/gsd-executor.md
    agents/gsd-planner.md
    agents/gsd-plan-checker.md
    get-shit-done/templates/phase-prompt.md
    get-shit-done/templates/planner-subagent-prompt.md
    get-shit-done/templates/summary.md
    get-shit-done/templates/state.md
    get-shit-done/references/git-integration.md
  </files>
  <action>
    Use research-R2-removal-impact.md as the definitive reference for what to remove. Search for the exact text strings listed in R2 to find current line positions (lines may have shifted from R2's original analysis). For each file below, perform the specified edits:

    **TDD removal from workflows and agents (use R2 Item 1, Section E):**

    1. `workflows/execute-phase.md` — Remove the `@...tdd.md` line from the `<execution_context>` block
    2. `workflows/plan-phase.md` — Remove the `@...tdd.md` line from the execute-phase spawn prompt
    3. `workflows/discuss-phase.md` — Remove `tdd.md` from the "Include @file refs to..." instruction list
    4. `workflows/execute-plan.md` — Remove entire `<tdd_plan_execution>` block. Remove `if tdd="true" -> TDD execution` clause from task execution step. Remove `test`/`refactor` TDD-labeled commit type rows from commit type table (keep the commit types themselves if they have non-TDD uses; remove only the TDD labels/descriptions).
    5. `agents/gsd-executor.md` — Remove `tdd="true"` check from execute_tasks step. Remove entire `<tdd_execution>` block. Remove `test` commit type row labeled as TDD RED.
    6. `agents/gsd-planner.md` — Remove entire `## TDD Detection` section. Remove `tdd` from plan type schema. Remove entire `<tdd_integration>` block. Remove TDD detection from step instructions.
    7. `agents/gsd-plan-checker.md` — Remove `tdd` row from plan type validation table. Remove `tdd` from valid task type list.
    8. `templates/phase-prompt.md` — Remove `tdd` from `type` field description. Remove "TDD candidates" bullet. Remove entire "## TDD Plans" section with its reference to tdd.md.
    9. `templates/planner-subagent-prompt.md` — Remove "TDD detection" from the note about baked-in methodology.
    10. `templates/summary.md` — Remove the TDD note about multiple commits.
    11. `references/git-integration.md` — Remove TDD labels from `test` and `refactor` commit type entries. Keep the commit types themselves (they have valid non-TDD uses like `test` for adding tests generally), just remove the "(TDD RED)" and "(TDD REFACTOR)" labels.

    **Todo removal from workflows and templates (use R2 Item 2, Section E):**

    12. `workflows/help.md` — Remove entire "### Todo Management" section. Remove `todos/` from Planning Directory Structure tree. Remove add-todo and check-todos from Examples section.
    13. `workflows/progress.md` — Remove `init todos or list-todos` from pending todo count step (line ~82). Remove entire "## Pending Todos" section from report template (lines ~119-120).
    14. `workflows/resume-project.md` — Remove all todo references: "Pending Todos" from resume context list, `[N] pending todos` from resume output, "Check pending todos" from user options, "Check todos" from action routing, "Count pending todos" step.
    15. `workflows/new-milestone.md` — Remove `pending todos` from the STATE.md read description. Reword to just "blockers".
    16. `workflows/pause-work.md` — Remove the `init todos` mention from the timestamp guidance. Keep the `current-timestamp` command.
    17. `templates/state.md` — Remove `### Pending Todos` section from the template. Remove the "Pending Todos" guidance from the `<sections>` block.

    **No help.md changes needed for health** (confirmed: /gsd:health is not listed in help.md).

    **IMPORTANT:** Do NOT touch the `<deviation_rules>` blocks or deviation-related content in execute-plan.md or gsd-executor.md — those are handled in Plan 02.
    **IMPORTANT:** Do NOT touch the `add-tests.md` workflow — it uses "TDD" terminology for unit test classification independently of the TDD execution pattern being removed. Leave it as-is.
  </action>
  <verify>
    Run: `grep -rn '@.*tdd\.md' get-shit-done/workflows/ agents/ get-shit-done/templates/ 2>/dev/null` — should return nothing (no dangling @tdd.md references).
    Run: `grep -rn 'tdd_plan_execution\|tdd_execution\|tdd_integration\|type.*tdd' get-shit-done/workflows/ agents/ get-shit-done/templates/ 2>/dev/null | grep -v add-tests` — should return nothing.
    Run: `grep -rn 'init todos\|list-todos\|check-todos\|add-todo\|gsd:health\|validate health' get-shit-done/workflows/ get-shit-done/templates/ agents/ 2>/dev/null` — should return nothing.
    Run: `grep -rn 'Pending Todos' get-shit-done/templates/state.md 2>/dev/null` — should return nothing.
  </verify>
  <done>
    All TDD execution pattern references removed from 11 kept files. All todo system references removed from 6 kept files. No dangling @file references to deleted files remain. The add-tests.md workflow is untouched (uses TDD terminology independently). Deviation rules are untouched (Plan 02 scope).
  </done>
</task>

</tasks>

<verification>
1. `grep -rn 'tdd\.md\|tdd_plan\|tdd_execution\|tdd_integration' get-shit-done/ agents/ commands/ --include='*.md' --include='*.cjs' 2>/dev/null | grep -v add-tests | grep -v node_modules` — zero matches
2. `grep -rn 'add-todo\|check-todos\|list-todos\|init todos\|cmdListTodos\|cmdTodoComplete\|cmdInitTodos' get-shit-done/ agents/ commands/ --include='*.md' --include='*.cjs' 2>/dev/null | grep -v node_modules` — zero matches
3. `grep -rn 'gsd:health\|validate health\|cmdValidateHealth' get-shit-done/ agents/ commands/ --include='*.md' --include='*.cjs' 2>/dev/null | grep -v node_modules` — zero matches
4. `ls commands/gsd/new-project.md.bak get-shit-done/workflows/discovery-phase.md 2>&1 | grep -c "No such file"` — returns 2
5. `ls .archive/07-cleanup/ 2>/dev/null && echo "ARCHIVE EXISTS" || echo "MISSING"` — prints ARCHIVE EXISTS
6. `grep '.archive/' .gitignore 2>/dev/null && echo "GITIGNORE OK" || echo "MISSING"` — prints GITIGNORE OK
7. `node get-shit-done/bin/gsd-tools.cjs current-timestamp` — runs without error
8. `npm test 2>&1 | tail -5` — existing tests still pass (or failures are unrelated to removals)
</verification>

<success_criteria>
- Zero references to TDD execution pattern in any project file (excluding add-tests.md which uses TDD terminology independently)
- Zero references to todo system (workflows, commands, CLI functions)
- Zero references to health check (workflow, command, CLI function)
- Dead artifacts (new-project.md.bak, discovery-phase.md) archived to .archive/07-cleanup/ (not at original paths)
- .gitignore contains .archive/ entry
- gsd-tools.cjs loads and runs remaining commands without error
- No dangling @file references to deleted files in any kept workflow
</success_criteria>

<output>
After completion, create `.planning/phases/07-cleanup/07-01-SUMMARY.md`
</output>

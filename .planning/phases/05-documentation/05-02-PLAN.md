---
phase: 05-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/init.cjs
  - get-shit-done/bin/gsd-tools.cjs
  - get-shit-done/templates/docs.md
autonomous: true
requirements: [DOCS-01, DOCS-02]

must_haves:
  truths:
    - "init doc-phase CLI command returns all context needed by doc-phase workflow"
    - "docs.md template matches CONTEXT.md directory structure (modules + flows + gate), not v1 structure (design + features + lessons)"
  artifacts:
    - path: "get-shit-done/bin/lib/init.cjs"
      provides: "cmdInitDocPhase function returning doc_agent_path, phase info, summary_files, documentation_dir, gate_docs_exist"
    - path: "get-shit-done/bin/gsd-tools.cjs"
      provides: "doc-phase case in init switch dispatching to cmdInitDocPhase"
    - path: "get-shit-done/templates/docs.md"
      provides: "v2 docs template with modules/ + flows/ + gate/ structure, heading templates, ownership tags"
  key_links:
    - from: "get-shit-done/bin/gsd-tools.cjs"
      to: "get-shit-done/bin/lib/init.cjs"
      via: "require + switch case dispatch"
      pattern: "case.*doc-phase.*cmdInitDocPhase"
---

<objective>
Add the `init doc-phase` CLI command and rewrite the docs template to match CONTEXT.md decisions.

Purpose: The workflow (Plan 03) needs an init command to bootstrap context. The existing docs template is v1 (design/features/lessons) and must be replaced with the v2 structure (modules/flows/gate) locked in CONTEXT.md.
Output: Updated `init.cjs` with `cmdInitDocPhase`, updated `gsd-tools.cjs` dispatch, rewritten `docs.md` template
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation/05-CONTEXT.md
@.planning/phases/05-documentation/05-RESEARCH.md

<interfaces>
<!-- cmdInitReviewPhase in init.cjs is the template for cmdInitDocPhase -->
<!-- Pattern: loadConfig, findPhaseInternal, getRoadmapPhaseInternal, resolve paths, output JSON -->
<!-- All init commands follow: (cwd, phase, raw) signature -->
<!-- JSON output uses snake_case keys -->

<!-- gsd-tools.cjs dispatch pattern: case 'doc-phase': cmdInitDocPhase(cwd, args[1], raw); break; -->
<!-- Added to the init switch statement after 'review-phase' case -->
<!-- Error message updated to include doc-phase in available workflows list -->

<!-- init doc-phase output shape (from RESEARCH.md): -->
<!-- doc_agent_model, phase_found, phase_dir, phase_number, phase_name, phase_req_ids, -->
<!-- doc_agent_path, commit_docs, summary_files, documentation_dir, gate_docs_exist, -->
<!-- feature_paths, capability_paths, state_path, roadmap_path -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <title>Add cmdInitDocPhase function to init.cjs and wire dispatch in gsd-tools.cjs</title>
  <reqs>DOCS-01</reqs>
  <artifact>get-shit-done/bin/lib/init.cjs, get-shit-done/bin/gsd-tools.cjs</artifact>
  <inputs>
    - get-shit-done/bin/lib/init.cjs -> cmdInitReviewPhase function as pattern (lines 599-698): loadConfig, findPhaseInternal, getRoadmapPhaseInternal, resolve paths, feature/capability discovery, output JSON
    - get-shit-done/bin/gsd-tools.cjs -> init switch statement (line 550 area): case pattern for dispatch
    - 05-RESEARCH.md -> init doc-phase output shape: doc_agent_model, phase_found, phase_dir, phase_number, phase_name, phase_req_ids, doc_agent_path, commit_docs, summary_files, documentation_dir, gate_docs_exist, feature_paths, capability_paths, state_path, roadmap_path
    - 05-RESEARCH.md -> doc_agent_path = "agents/gsd-doc-writer.md"
    - 05-RESEARCH.md -> documentation_dir = ".documentation"
  </inputs>
  <done>cmdInitDocPhase exported from init.cjs; gsd-tools.cjs dispatches `init doc-phase <phase>` to cmdInitDocPhase; `node gsd-tools.cjs init doc-phase 5` returns JSON with all fields from research output shape; summary_files populated by reading *-SUMMARY.md from phase dir; gate_docs_exist checks .documentation/gate/ directory</done>
</task>

<task type="auto">
  <title>Rewrite docs.md template from v1 structure to v2 CONTEXT.md structure</title>
  <reqs>DOCS-02</reqs>
  <artifact>get-shit-done/templates/docs.md</artifact>
  <inputs>
    - get-shit-done/templates/docs.md -> current v1 template: design.md + features.md + lessons.md per capability
    - 05-CONTEXT.md -> v2 directory structure: .documentation/modules/ (flat, 1:1), .documentation/flows/ (capability-grouped), .documentation/gate/ (scaffolded, human-maintained)
    - 05-CONTEXT.md -> heading templates: Module docs (## Module:, ## Purpose:, ## Exports:, ## Depends-on:, ## Constraints:, ## WHY:), Flow docs (## Flow:, ## Trigger:, ## Input:, ## Steps:, ## Output:, ## Side-effects:, ## WHY:)
    - 05-CONTEXT.md -> ownership tags: [derived] for regenerated sections, [authored] for preserved sections
    - 05-CONTEXT.md -> YAML frontmatter: type, module/flow name, built-from-code-at: {git-sha}, last-verified: {date}
  </inputs>
  <done>docs.md template describes v2 structure (modules/ + flows/ + gate/), includes exact heading templates from CONTEXT.md, documents [derived]/[authored] ownership tags, includes YAML frontmatter fields (type, built-from-code-at, last-verified), no references to v1 design/features/lessons structure</done>
</task>

</tasks>

<verification>
- [ ] `node gsd-tools.cjs init doc-phase 5` runs without error and returns valid JSON
- [ ] JSON output includes all fields from research output shape
- [ ] summary_files field populated from phase directory
- [ ] gate_docs_exist checks .documentation/gate/ presence
- [ ] gsd-tools.cjs error message includes doc-phase in available workflows
- [ ] docs.md template uses modules/ + flows/ + gate/ structure (not design/features/lessons)
- [ ] Template heading formats match CONTEXT.md exactly
</verification>

<success_criteria>
The init command bootstraps all context the doc-phase workflow needs, and the template reflects the locked v2 documentation structure.
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation/05-02-SUMMARY.md`
</output>

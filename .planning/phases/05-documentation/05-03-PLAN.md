---
phase: 05-documentation
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - get-shit-done/workflows/doc-phase.md
  - commands/gsd/doc-phase.md
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03]

must_haves:
  truths:
    - "Doc-phase workflow orchestrates: init -> context assembly -> locate artifacts -> spawn doc agent -> verify output -> Q&A review -> commit"
    - "Slash command /gsd:doc-phase exists and references the workflow"
    - "Q&A review follows same pattern as plan-phase and review-phase (AskUserQuestion in orchestrator)"
  artifacts:
    - path: "get-shit-done/workflows/doc-phase.md"
      provides: "Single-agent documentation pipeline: init -> context -> artifacts -> spawn -> verify -> Q&A -> commit"
    - path: "commands/gsd/doc-phase.md"
      provides: "Slash command entry point referencing doc-phase workflow"
  key_links:
    - from: "commands/gsd/doc-phase.md"
      to: "get-shit-done/workflows/doc-phase.md"
      via: "@execution_context reference"
      pattern: "workflows/doc-phase"
    - from: "get-shit-done/workflows/doc-phase.md"
      to: "agents/gsd-doc-writer.md"
      via: "Task spawn using doc_agent_path from init"
      pattern: "Task.*gsd-doc-writer"
    - from: "get-shit-done/workflows/doc-phase.md"
      to: "get-shit-done/bin/gsd-tools.cjs"
      via: "init doc-phase CLI call"
      pattern: "gsd-tools.*init.*doc-phase"
---

<objective>
Create the doc-phase workflow and slash command that orchestrate the full documentation pipeline.

Purpose: The workflow ties together the doc agent (Plan 01) and init command (Plan 02) into an end-to-end pipeline: init -> context assembly -> locate artifacts -> spawn doc agent -> verify output files -> Q&A review with user -> commit on approval.
Output: `get-shit-done/workflows/doc-phase.md`, `commands/gsd/doc-phase.md`
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation/05-CONTEXT.md
@.planning/phases/05-documentation/05-RESEARCH.md
@.planning/phases/05-documentation/05-01-SUMMARY.md
@.planning/phases/05-documentation/05-02-SUMMARY.md

<interfaces>
<!-- Workflow pattern from review-phase.md (04-03): -->
<!-- <purpose>, <required_reading>, <process> with numbered steps, <key_constraints> -->
<!-- Init -> Context Assembly (Layers 1-3) -> Locate Artifacts -> Spawn Agent -> Verify -> Present -> Commit -->

<!-- Command pattern from commands/gsd/review-phase.md: -->
<!-- Frontmatter: name, description, argument-hint, allowed-tools -->
<!-- Body: <objective>, <execution_context> with @workflow reference, <context> with $ARGUMENTS, <process> -->

<!-- Doc-phase is single-agent pipeline (NOT gather-synthesize): -->
<!-- One Task spawn for doc agent, not parallel gatherers -->
<!-- Q&A review uses AskUserQuestion with 12-char header limit (e.g., "Doc 1/3") -->
<!-- User approves before commit -->

<!-- Impact flags: presented as separate section after doc review Q&A -->
<!-- "The following existing flow docs may be affected: [list]. Review them manually." -->

<!-- From init doc-phase (Plan 02): doc_agent_path, summary_files, documentation_dir, gate_docs_exist -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <title>Create doc-phase workflow with single-agent pipeline and Q&A review loop</title>
  <reqs>DOCS-01, DOCS-02, DOCS-03</reqs>
  <artifact>get-shit-done/workflows/doc-phase.md</artifact>
  <inputs>
    - get-shit-done/workflows/review-phase.md -> workflow structure pattern: <purpose>, <required_reading>, <process> with numbered steps, <key_constraints>
    - get-shit-done/workflows/review-phase.md -> context assembly pattern (Layers 1-3: core, capability, feature)
    - get-shit-done/workflows/review-phase.md -> artifact location via SUMMARY.md files
    - get-shit-done/workflows/review-phase.md -> Q&A pattern: AskUserQuestion with header limit, response handling
    - 05-CONTEXT.md -> trigger model: after review acceptance, final pipeline stage
    - 05-CONTEXT.md -> review gate: user Q&A review before commit (same pattern as plan-phase and review-phase)
    - 05-CONTEXT.md -> impact handling: impacted flow docs flagged only, presented as separate section
    - 05-RESEARCH.md -> single-agent pipeline flow: init -> context assembly -> locate artifacts -> spawn doc agent -> verify output files -> Q&A review -> commit
    - 05-RESEARCH.md -> Q&A constraint: AskUserQuestion header max 12 chars ("Doc 1/3" format)
    - 05-RESEARCH.md -> file-based result collection: agent writes .documentation/ files, workflow verifies existence
  </inputs>
  <done>get-shit-done/workflows/doc-phase.md exists with: numbered process steps (init, context assembly, locate artifacts, spawn doc agent, verify output, Q&A review, commit), single Task spawn (not gather-synthesize), AskUserQuestion for doc review with 12-char headers, impact flags presented as separate section, key_constraints section documenting single-agent pipeline and Q&A pattern</done>
</task>

<task type="auto">
  <title>Create doc-phase slash command entry point</title>
  <reqs>DOCS-01</reqs>
  <artifact>commands/gsd/doc-phase.md</artifact>
  <inputs>
    - commands/gsd/review-phase.md -> command pattern: frontmatter (name, description, argument-hint, allowed-tools), body (<objective>, <execution_context> with @workflow, <context> with $ARGUMENTS, <process>)
    - get-shit-done/workflows/doc-phase.md -> workflow file path for @execution_context reference
  </inputs>
  <done>commands/gsd/doc-phase.md exists with: YAML frontmatter (name: gsd:doc-phase, description, argument-hint: "[phase]", allowed-tools including Read/Write/Bash/Glob/Grep/Task/AskUserQuestion), execution_context referencing doc-phase workflow, process section delegating to workflow</done>
</task>

</tasks>

<verification>
- [ ] Workflow follows established pattern (purpose, required_reading, numbered process steps, key_constraints)
- [ ] Workflow uses single Task spawn (NOT gather-synthesize pattern)
- [ ] Workflow calls `init doc-phase` to bootstrap context
- [ ] Workflow locates artifacts via SUMMARY.md files
- [ ] Q&A review uses AskUserQuestion with headers within 12-char limit
- [ ] Impact flags presented as separate section after doc review
- [ ] User approval required before commit
- [ ] Command frontmatter follows review-phase.md pattern
- [ ] Command references workflow via @execution_context
</verification>

<success_criteria>
The doc-phase workflow orchestrates the full documentation pipeline end-to-end, and the slash command provides a clean entry point. The pipeline follows established patterns from plan-phase and review-phase.
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation/05-03-SUMMARY.md`
</output>

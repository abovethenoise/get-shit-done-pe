---
phase: 12-workflow-optimization-wiring
plan: 06
type: execute
wave: 3
depends_on: [12-01, 12-02]
files_modified:
  - get-shit-done/bin/install.js
  - get-shit-done/references/ui-brand.md
  - get-shit-done/references/git-integration.md
  - get-shit-done/references/planning-config.md
  - get-shit-done/references/git-planning-commit.md
  - get-shit-done/templates/context.md
  - get-shit-done/templates/research.md
  - get-shit-done/templates/summary.md
  - get-shit-done/templates/continue-here.md
  - get-shit-done/templates/UAT.md
  - get-shit-done/templates/user-setup.md
  - get-shit-done/templates/discovery.md
  - get-shit-done/templates/requirements.md
  - get-shit-done/templates/project.md
  - get-shit-done/templates/VALIDATION.md
  - get-shit-done/templates/config.json
  - get-shit-done/templates/requirements.md (DELETE)
  - get-shit-done/templates/UAT.md (DELETE or UPDATE)
  - docs/USER-GUIDE.md
  - get-shit-done/references/git-integration.md
  - get-shit-done/references/continuation-format.md
  - get-shit-done/references/questioning.md
  - agents/gsd-research-prior-art.md
  - agents/gsd-review-quality.md
  - package.json
autonomous: true
requirements: [CMD-01]

must_haves:
  truths:
    - "install.js contains no Codex/Gemini/OpenCode adapter code"
    - "install.js has no patch backup system (gsd-local-patches/)"
    - "install.js has no manifest (gsd-file-manifest.json)"
    - "install.js has no CHANGELOG copy or VERSION file write"
    - "package.json description says Claude Code only, no multi-runtime keywords"
    - "ui-brand.md uses feature/capability banners instead of phase/milestone"
    - "All template path headers reference feature directories, not phase directories"
    - "config.json template has no confirm_phases, confirm_transition, auto_advance keys"
    - "templates/requirements.md deleted (v1, replaced by FEATURE.md direct population)"
    - "templates/UAT.md deleted or updated for v2 (no v1 lens-unaware content remains)"
    - "docs/USER-GUIDE.md rewritten for v2: 4 framing modes, discovery brief workflow, capability-based workflows, lens-weighted pipeline, 3-layer requirements"
    - "git-integration.md uses feature/capability commit format, not phase format"
    - "continuation-format.md has zero 'phase' references (replaced with feature/capability terminology)"
    - "questioning.md has no outer <questioning_guide> wrapper tag"
    - "No instances of 'GSD\\'s', 'GSD framework', 'GSD system', 'fit with GSD' remain in agents/commands/workflows"
    - "gsd-review-quality.md duplicate content (lines 65-116) removed"
  artifacts:
    - path: "get-shit-done/bin/install.js"
      provides: "Claude Code only installer, no v1 bloat"
    - path: "get-shit-done/templates/config.json"
      provides: "v2 config template without phase gates"
  key_links:
    - from: "get-shit-done/bin/install.js"
      to: "commands/gsd/"
      via: "installs all v2 command files"
      pattern: "commands"
---

<objective>
Clean install.js per INST-05/06 (user decision to include in Phase 12) and sweep v1 terminology from references, templates, and config.

Purpose: install.js carries ~300 lines of multi-runtime support and patch backup code that will never be used. Templates and references still say "phase" in path headers and banner text. This cleanup ensures everything is v2-clean before the dead code deletion sweep.

Output: Cleaned install.js. Updated references, templates, config.
</objective>

<execution_context>
@/Users/philliphall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philliphall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-workflow-optimization-wiring/12-CONTEXT.md
@.planning/phases/12-workflow-optimization-wiring/RESEARCH-DEAD-CODE.md
@.planning/phases/12-workflow-optimization-wiring/RESEARCH-BREAKING-POINTS.md

Read files being modified:
@get-shit-done/bin/install.js
@get-shit-done/references/ui-brand.md
@get-shit-done/references/git-integration.md
@get-shit-done/templates/config.json
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean install.js (INST-05/06) and update package.json</name>
  <files>get-shit-done/bin/install.js, package.json</files>
  <action>
Per user Q&A decision: "install.js cleanup in Phase 12."

**install.js INST-05 (remove multi-runtime):**
1. Remove all Codex adapter code (lines referencing Codex workspace, Codex-specific paths, Codex agent format)
2. Remove all Gemini adapter code (Gemini workspace, Gemini-specific paths)
3. Remove all OpenCode adapter code
4. Remove runtime detection logic that checks which AI tool is running
5. Remove runtime conversion functions that transform Claude format to other formats
6. Keep ONLY the Claude Code installation path
7. If there's a runtime selection prompt/config, remove it -- Claude Code is the only target

**install.js INST-06 (remove bloat):**
1. Remove patch backup system: delete all `gsd-local-patches/` related code (backup, restore, reapply logic)
2. Remove manifest system: delete `gsd-file-manifest.json` creation and management
3. Remove CHANGELOG.md copy (lines ~1954-1962 per research)
4. Remove VERSION file write (lines ~1966-1969)
5. Remove `gsd-check-update.js` from hooks install list (already removed in Phase 8 but may still be referenced in install code)
6. Remove `reapply-patches` command reference

**package.json updates:**
1. Remove multi-runtime keywords: "codex", "gemini", "codex-cli", "gemini-cli"
2. Update description: remove "OpenCode, Gemini and Codex" -- Claude Code only
3. Keep `"test"` script reference to run-tests.cjs (still has live tests)

Estimated removal: ~300-400 lines from install.js.
  </action>
  <verify>
    <automated>grep -ci "codex\|gemini\|opencode\|gsd-local-patches\|gsd-file-manifest\|CHANGELOG\|VERSION\|reapply-patches" get-shit-done/bin/install.js | grep -q "^0$" && echo "PASS: install.js clean" || echo "FAIL: v1 bloat remains"</automated>
  </verify>
  <done>install.js is Claude Code only -- no multi-runtime adapters, no patch backup, no manifest, no changelog/version metadata. package.json updated.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep v1 terminology from references, templates, and config</name>
  <files>get-shit-done/references/ui-brand.md, get-shit-done/references/git-integration.md, get-shit-done/references/planning-config.md, get-shit-done/references/git-planning-commit.md, get-shit-done/templates/context.md, get-shit-done/templates/research.md, get-shit-done/templates/summary.md, get-shit-done/templates/continue-here.md, get-shit-done/templates/UAT.md, get-shit-done/templates/user-setup.md, get-shit-done/templates/discovery.md, get-shit-done/templates/requirements.md, get-shit-done/templates/project.md, get-shit-done/templates/VALIDATION.md, get-shit-done/templates/config.json</files>
  <action>
**References updates:**

1. **ui-brand.md:** Replace phase/milestone banners:
   - `PLANNING PHASE {N}` -> `PLANNING FEATURE {cap}/{feat}`
   - `PHASE {N} COMPLETE` -> `FEATURE {feat} COMPLETE`
   - `MILESTONE COMPLETE` -> `FOCUS GROUP COMPLETE`
   - `Phase/milestone level:` -> `Capability/feature level:`

2. **git-integration.md:** Update commit path examples:
   - `.planning/phases/XX-name/` patterns -> `.planning/capabilities/{cap}/features/{feat}/`

3. **planning-config.md:** Rewrite for v2 config shape:
   - Remove: `phase_branch_template`, `milestone_branch_template`, `branching_strategy: phase/milestone`, `complete-milestone` workflow refs, `auto_advance`
   - Document v2 config keys that remain relevant (research, workflow settings, model profiles)

4. **git-planning-commit.md:** Update commit conventions:
   - Remove: `new-milestone`, `plan-phase`, `execute-phase` conventions
   - Add: `docs({cap}/{feat}):` convention for feature work

**Templates path header updates (7 files):**

Per RESEARCH-BREAKING-POINTS.md: "Templates that carry phase terminology -- path headers only"

5. **context.md:** Update path from `.planning/phases/XX-name/{phase_num}-CONTEXT.md` to `.planning/capabilities/{cap}/features/{feat}/CONTEXT.md`

6. **research.md:** Update path from `.planning/phases/XX-name/{phase_num}-RESEARCH.md` to `.planning/capabilities/{cap}/features/{feat}/RESEARCH.md`

7. **summary.md:** Update path from `.planning/phases/XX-name/{phase}-{plan}-SUMMARY.md` to `.planning/capabilities/{cap}/features/{feat}/{plan}-SUMMARY.md`

8. **continue-here.md:** Update path from `.planning/phases/XX-name/` to feature dir

9. **UAT.md:** Update path + remove `plan-phase --gaps` reference

10. **user-setup.md:** Update path from `.planning/phases/XX-name/` to feature dir

11. **discovery.md:** Update path from `.planning/phases/XX-name/DISCOVERY.md` to feature dir

**Other template updates:**

12. **requirements.md:** Replace "current milestone" / "future milestone" scoping with capability scoping

13. **project.md:** Update "Phase" refs to capability/feature language where appropriate. Keep historical references that don't affect functionality.

14. **VALIDATION.md:** Update "phase requirements" to "feature requirements"

15. **config.json template:** Remove v1 gate keys:
    - `confirm_phases: true` -> remove
    - `confirm_transition: true` -> remove
    - `auto_advance: false` -> remove
    Keep all other config keys (research, workflow settings, models, etc.)

16. **templates/codebase/*.md (7 files):** Update "Useful for phase planning when:" label to "Useful for planning when:" (cosmetic, but per research these are flagged)
  </action>
  <verify>
    <automated>grep -rci "planning/phases/XX\|plan-phase\|execute-phase\|PLANNING PHASE\|PHASE.*COMPLETE\|confirm_phases\|confirm_transition\|auto_advance" get-shit-done/references/ui-brand.md get-shit-done/references/git-integration.md get-shit-done/templates/context.md get-shit-done/templates/research.md get-shit-done/templates/summary.md get-shit-done/templates/config.json | grep -v ":0$" | wc -l | grep -q "^0$" && echo "PASS: v1 terminology swept" || echo "FAIL: v1 refs remain"</automated>
  </verify>
  <done>All references updated for v2 (banners, commit conventions, config docs). All template path headers point to feature directories. Config template stripped of v1 gate keys. Codebase templates have generic labels.</done>
</task>

<task type="auto">
  <name>Task 3: V1 artifact cleanup, USER-GUIDE rewrite, and de-branding sweep</name>
  <files>get-shit-done/templates/requirements.md, get-shit-done/templates/UAT.md, docs/USER-GUIDE.md, get-shit-done/references/git-integration.md, get-shit-done/references/continuation-format.md, get-shit-done/references/questioning.md, agents/gsd-research-prior-art.md, agents/gsd-review-quality.md</files>
  <action>
**V1 artifact deletions:**

1. **Delete templates/requirements.md:** This is a v1 template with no 3-layer structure. In v2, requirements populate FEATURE.md directly (EU/FN/TC sections). No v2 workflow references this template. Delete the file.

2. **Delete or update templates/UAT.md:** This is a v1 template with no lens awareness. Check if any v2 workflow still references UAT.md. If no references: delete. If referenced: update to align with v2 review pipeline (lens-aware, EU/FN/TC-based acceptance).

**USER-GUIDE.md rewrite (significant sub-task):**

3. **Rewrite docs/USER-GUIDE.md:** Currently 471 lines, entirely v1. Full rewrite to explain the v2 system:
   - **4 framing modes:** debug, new, enhance, refactor -- what each means, when to use each
   - **Discovery brief workflow:** How `/gsd:debug|new|enhance|refactor <slug>` creates a DISCOVERY-BRIEF.md
   - **Capability-based workflows:** init -> discuss-capability -> discuss-feature -> focus -> plan -> execute -> review -> doc
   - **Lens-weighted pipeline:** How the lens affects every stage (research questions, planning approach, review priorities, doc focus)
   - **3-layer requirements:** EU (end-user stories), FN (functional behavior), TC (technical specs) -- how they're structured in FEATURE.md
   - **Focus groups:** What they are, how `/gsd:focus` creates them, how they sequence work
   - **Commands reference:** The 11-command surface (init, debug, new, enhance, refactor, discuss-capability, discuss-feature, status, resume-work, plan, review) + focus
   - Target: ~300-400 lines. Clear, structured, user-facing. No internal implementation details.

**Reference file fixes:**

4. **Fix git-integration.md commit format:** Currently uses plan-focused format `{type}({phase}-{plan}): {task-name}`. Update to feature/capability model:
   - `docs({cap}/{feat}): {description}` for feature work
   - `chore({cap}): {description}` for capability-level changes
   - `feat({cap}/{feat}): {description}` for new code
   - Remove all phase/milestone commit format examples

5. **Fix continuation-format.md:** Contains 16 "phase" references. Update all to feature/capability terminology:
   - "phase" -> "feature" or "capability" as appropriate
   - "phase directory" -> "feature directory"
   - "phase plan" -> "feature plan"
   - Preserve the structural format -- only update terminology

6. **Flatten questioning.md:** Remove the outer `<questioning_guide>` wrapper XML tag. Keep all inner tags (`<deep_questions>`, `<surface_questions>`, etc.) as flat siblings at the top level. The wrapper adds no value and wastes tokens.

**GSD de-branding sweep:**

7. **Grep and replace branded language:** Search across agents/, commands/gsd/, get-shit-done/workflows/ for:
   - "GSD's" -> "the system's" or "the project's"
   - "GSD framework" -> "the framework" or "the system"
   - "GSD system" -> "the system"
   - "fit with GSD" -> "fit with the system" or "fit the project's constraints"
   - Key file: agents/gsd-research-prior-art.md line 53 "fits GSD's constraints"
   - Do NOT rename file names (gsd-*.md) or command names (/gsd:*) -- only prose references

**Duplicate content cleanup:**

8. **Fix gsd-review-quality.md:** Lines 65-116 are a copy-paste duplicate of earlier content in the file. Remove the duplicated block, keeping only the first occurrence. Verify the remaining content is coherent after removal.
  </action>
  <verify>
    <automated>test ! -f get-shit-done/templates/requirements.md && echo "PASS: requirements.md deleted" || echo "FAIL: requirements.md still exists"</automated>
    <automated>grep -rci "GSD's\|GSD framework\|GSD system\|fit with GSD\|fits GSD" agents/ commands/gsd/ get-shit-done/workflows/ | grep -v ":0$" | wc -l | grep -q "^0$" && echo "PASS: GSD branding removed" || echo "FAIL: GSD branding remains"</automated>
    <automated>grep -c "phase" get-shit-done/references/continuation-format.md | awk '{if ($1 == 0) print "PASS: No phase refs in continuation-format"; else print "FAIL: " $1 " phase refs remain"}'</automated>
    <automated>grep -c "<questioning_guide>" get-shit-done/references/questioning.md | awk '{if ($1 == 0) print "PASS: questioning_guide wrapper removed"; else print "FAIL: wrapper remains"}'</automated>
  </verify>
  <done>requirements.md deleted. UAT.md deleted/updated. USER-GUIDE.md rewritten for v2 (4 lenses, capability workflows, 3-layer requirements, focus groups, 11+ command surface). git-integration.md uses feature/capability commit format. continuation-format.md phase-free. questioning.md flattened. GSD de-branding complete. review-quality.md duplicate removed.</done>
</task>

</tasks>

<verification>
- grep for "Codex", "Gemini", "OpenCode", "gsd-local-patches" in install.js returns zero
- grep for "confirm_phases", "auto_advance" in templates/config.json returns zero
- grep for "PLANNING PHASE" in ui-brand.md returns zero
- grep for ".planning/phases/XX" across all modified template files returns zero
- templates/requirements.md does not exist
- grep for "GSD's", "GSD framework", "GSD system" across agents/commands/workflows returns zero
- grep for "phase" in continuation-format.md returns zero
- grep for "<questioning_guide>" in questioning.md returns zero
- docs/USER-GUIDE.md exists and contains "framing", "capability", "FEATURE.md" references
</verification>

<success_criteria>
install.js is Claude-Code-only with no v1 bloat. All references and templates use v2 terminology and feature directory paths. Config template has v2 shape. V1 templates deleted. USER-GUIDE.md rewritten for v2. GSD branding removed from prose. All reference files use feature/capability terminology.
</success_criteria>

<output>
After completion, create `.planning/phases/12-workflow-optimization-wiring/12-06-SUMMARY.md`
</output>

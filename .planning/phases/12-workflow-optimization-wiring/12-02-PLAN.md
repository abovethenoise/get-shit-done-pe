---
phase: 12-workflow-optimization-wiring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/review.md
  - get-shit-done/workflows/doc.md
  - get-shit-done/workflows/framing-pipeline.md
autonomous: true
requirements: [INTG-01, CMD-01]

must_haves:
  truths:
    - "review.md calls init feature-op <cap> <feat> review instead of init review-phase"
    - "doc.md calls init feature-op <cap> <feat> doc instead of init doc-phase"
    - "framing-pipeline.md accepts FEATURE_SLUG and passes feature-level paths to all 6 stages"
    - "Requirements stage (Stage 2) populates FEATURE.md directly, not a separate REQUIREMENTS.md"
    - "Research stage (Stage 1) writes output to feature directory, not capability directory"
    - "Review trace files written to feature_dir/review/, not phase_dir/review/"
    - "LENS and ANCHOR_QUESTIONS_PATH propagated to all 6 pipeline stages (WKFL-07)"
    - "Each stage workflow receives lens framing context that affects its Q&A, discovery, and agent behavior"
    - "Static <!-- FRAMING INJECTION SLOT --> HTML comments deleted from all 4 review agents + doc-writer, replaced with dynamic framing_context section"
    - "Doc-writer understands 3-layer requirement tracing (EU/FN/TC + quality) for WHY block extraction"
    - "gsd-review-quality.md duplicate content (lines 65-116) cleaned up"
  artifacts:
    - path: "get-shit-done/workflows/review.md"
      provides: "v2 feature-scoped review workflow"
    - path: "get-shit-done/workflows/doc.md"
      provides: "v2 feature-scoped documentation workflow"
    - path: "get-shit-done/workflows/framing-pipeline.md"
      provides: "v2 feature-level pipeline orchestrator"
  key_links:
    - from: "get-shit-done/workflows/framing-pipeline.md"
      to: "get-shit-done/workflows/plan.md"
      via: "stage 3 invocation with FEATURE_SLUG context"
      pattern: "FEATURE_SLUG"
    - from: "get-shit-done/workflows/review.md"
      to: "bin/lib/init.cjs cmdInitFeatureOp"
      via: "gsd-tools init feature-op"
      pattern: "init feature-op"
---

<objective>
Rewrite review.md, doc.md, and framing-pipeline.md to complete the B1 blocker fix. review.md and doc.md currently call dead v1 routes (return errors). framing-pipeline.md operates at capability level but needs to pass feature-level context to all stages.

Purpose: After this plan + Plan 01, the entire pipeline (research -> requirements -> plan -> execute -> review -> doc) speaks v2 feature paths end-to-end.

Output: Three rewritten workflow files completing the v2 pipeline wiring.
</objective>

<execution_context>
@/Users/philliphall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philliphall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-workflow-optimization-wiring/12-CONTEXT.md
@.planning/phases/12-workflow-optimization-wiring/RESEARCH-BREAKING-POINTS.md
@.planning/phases/12-workflow-optimization-wiring/RESEARCH-ARCHITECTURE.md
@.planning/phases/12-workflow-optimization-wiring/RESEARCH-LIFECYCLE.md

Read these files to understand current state:
@get-shit-done/workflows/review.md
@get-shit-done/workflows/doc.md
@get-shit-done/workflows/framing-pipeline.md
@get-shit-done/bin/lib/init.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite review.md and doc.md for v2 feature model</name>
  <files>get-shit-done/workflows/review.md, get-shit-done/workflows/doc.md</files>
  <action>
**review.md rewrite:**

1. **Init call (line ~17):** Replace `init review-phase "${PHASE}" --raw` (DEAD ROUTE -- returns error) with `init feature-op "$CAPABILITY_SLUG" "$FEATURE_SLUG" review`. The `init feature-op` route exists and returns: `capability_dir, feature_dir, has_research, has_context, has_plans, brave_search`.

2. **JSON field mapping:**
   - `phase_found` -> `feature_found` (derive from feature_dir existence)
   - `phase_dir` -> `feature_dir`
   - `phase_number`, `phase_name` -> `feature_slug`, `capability_slug`
   - `phase_req_ids` -> extract from FEATURE.md (EU/FN/TC requirement IDs)

3. **Context assembly:** review.md assembles context for the 4 specialist reviewers. Update to:
   - Requirements source: FEATURE.md (3-layer EU/FN/TC) instead of REQUIREMENTS.md
   - Code files: unchanged (the reviewer reads actual source)
   - Review trace output: `${feature_dir}/review/` instead of `${phase_dir}/review/`

4. **Reviewer spawn -- detailed wiring of 4 specialists (REVW-01 through REVW-08):**

   The review orchestrator (review.md) spawns 4 parallel specialist reviewers. Each reviewer:
   - Receives: FEATURE.md (as requirements source), list of code files changed (from SUMMARY.md task outputs), LENS context, feature_dir
   - Produces: trace file at `${feature_dir}/review/{specialist}-trace.md`

   **4a. End-user reviewer (gsd-review-enduser.md):**
   - Reads: EU-xx requirements from FEATURE.md (Story + Acceptance Criteria)
   - Reviews: Does the code satisfy each acceptance criterion?
   - Output format per EU requirement: `EU-01: MET | NOT MET | REGRESSION` with evidence (file:line references)
   - Writes: `${feature_dir}/review/enduser-trace.md`

   **4b. Functional reviewer (gsd-review-functional.md):**
   - Reads: FN-xx requirements from FEATURE.md (Receives/Returns/Behavior)
   - Reviews: Does each function/module match its behavioral spec?
   - Output format per FN requirement: `FN-01: MET | NOT MET | REGRESSION` with evidence
   - Writes: `${feature_dir}/review/functional-trace.md`

   **4c. Technical reviewer (gsd-review-technical.md):**
   - Reads: TC-xx requirements from FEATURE.md (Intent/Upstream/Downstream/Constraints)
   - Reviews: Are constraints satisfied? Dependencies correct? Performance within bounds?
   - Output format per TC requirement: `TC-01: MET | NOT MET | REGRESSION` with evidence
   - Writes: `${feature_dir}/review/technical-trace.md`

   **4d. Quality reviewer (gsd-review-quality.md):**
   - Reads: All code files, applies DRY/KISS/no-bloat heuristics (REVW-06)
   - Reviews: Dead code? Over-abstraction? Unnecessary complexity? Obsolete patterns?
   - Output format: Issue list with severity (critical/warning/info) and file:line references
   - Writes: `${feature_dir}/review/quality-trace.md`

5. **Synthesis -- review synthesizer wiring (REVW-07):**
   - Synthesizer (gsd-review-synthesizer.md) reads all 4 trace files from `${feature_dir}/review/`
   - Consolidates findings, resolves conflicts per priority: user > functional > technical > quality
   - Produces: `${feature_dir}/review/synthesis.md` with:
     - Per-requirement verdict summary table
     - Critical issues list (blocks release)
     - Warnings list (should fix)
     - Clean bill of health (if no issues)

6. **Q&A findings loop (REVW-08) -- how review findings flow to user:**
   - If synthesis shows ANY "NOT MET" or critical issues:
     a. Present findings to user as structured Q&A: "These requirements were not met: [list]. For each, should we: (a) fix now, (b) defer, (c) mark as acceptable?"
     b. For each "fix now" decision: create a fix task, update FEATURE.md trace table
     c. Loop: re-execute fix tasks, re-run affected reviewers, re-synthesize
     d. Continue until all critical issues resolved or deferred
   - If synthesis is clean: auto-continue to doc stage (per CONTEXT.md auto-chain)
   - Update display text from "Phase {X}" to "Feature: {cap}/{feat}"

7. **Inputs block:** Accept CAPABILITY_SLUG, FEATURE_SLUG, and LENS.

8. **Review agent framing injection update (4 review agents + doc-writer):**

   **IMPORTANT: The user has personally modified these agent files. Only change the framing slot wiring described below. Do NOT alter the agent's voice, tone, content, or review question style.**

   **8a. Replace static HTML framing slots with dynamic injection:**
   All 4 review agents (gsd-review-enduser.md, gsd-review-functional.md, gsd-review-technical.md, gsd-review-quality.md) and gsd-doc-writer.md contain static `<!-- FRAMING INJECTION SLOT ... -->` HTML comment placeholders. DELETE these static slots entirely. Replace with a dynamic injection section in each agent:

   ```
   ## Framing Context
   When framing_context is provided by the orchestrator, adjust review questions accordingly:
   - debug: "Is the bug fixed? Is the root cause addressed?"
   - new: "Does the story pass? Are acceptance criteria met?"
   - enhance: "Is the enhancement working? Is backward compatibility preserved?"
   - refactor: "Is the behavior preserved? Are tests green before and after?"
   ```

   Each agent should have per-lens guidance appropriate to its specialty (enduser focuses on user-facing impact, functional on behavioral contracts, technical on constraint satisfaction, quality on code health).

   **8b. Doc-writer EU/FN/TC awareness:**
   Add 4-5 sentences to gsd-doc-writer.md explaining that review findings trace to three requirement layers:
   - EU = End-user stories and acceptance criteria
   - FN = Functional behavior contracts (receives/returns/behavior)
   - TC = Technical implementation specs (intent/upstream/downstream/constraints)
   - Plus quality concerns (DRY/KISS/no-bloat)

   The doc-writer should understand this 3-layer context when extracting WHY blocks from review citations. When a review finding says "FN-03: NOT MET", the doc-writer knows to look at the functional behavior contract for that requirement to understand what was expected vs what was found.

   **8c. Clean gsd-review-quality.md duplicate content:**
   gsd-review-quality.md has duplicate content -- lines 65-116 are a copy-paste of earlier content in the file. Remove the duplicated block, keeping only the first occurrence.

**doc.md rewrite -- detailed gsd-doc-writer wiring (DOCS-01/02/03):**

1. **Init call (line ~16):** Replace `init doc-phase "${PHASE}" --raw` (DEAD ROUTE) with `init feature-op "$CAPABILITY_SLUG" "$FEATURE_SLUG" doc`.

2. **JSON field mapping:** Same as review.md.

3. **Doc writer invocation -- gsd-doc-writer.md with 3-pass validation:**

   doc.md is an orchestrator that invokes gsd-doc-writer.md. The writer operates with the following strict protocol:

   **Input context assembly:**
   - Feature SUMMARY files: `${feature_dir}/*-SUMMARY.md` (what was built)
   - FEATURE.md: requirements context (what was supposed to be built)
   - Review synthesis: `${feature_dir}/review/synthesis.md` (what reviewers found)
   - Existing `.documentation/` files: current state of documentation
   - LENS context: affects doc focus (see framing-pipeline lens wiring above)

   **Section ownership model (from Phase 5 DOCS-01/02/03):**
   - `[derived]` sections: auto-regenerated from code inspection every doc pass. Examples: API signatures, data models, dependency graphs.
   - `[authored]` sections: written by humans or Claude during first pass, preserved on subsequent passes. Examples: design rationale, usage guidelines, gotchas.
   - doc.md MUST parse section tags before writing. [derived] sections get overwritten. [authored] sections get preserved unless user explicitly requests changes.

   **3-pass self-validation protocol:**
   - **Pass 1 (Structural compliance):** Verify all required heading templates exist per `.documentation/` schema. Check strict heading format for grep consistency (from Phase 5 DOCS-03). Report missing/extra sections.
   - **Pass 2 (Referential integrity):** Every code reference in docs points to a file that exists. Every @file reference resolves. Module names match actual module names. Import paths are correct.
   - **Pass 3 (Gate consistency):** Doc content is consistent with FEATURE.md requirements and review synthesis. No contradictions between what docs say and what code does. Trace table in FEATURE.md updated to reflect doc completion.

   **Write order (from Phase 5 research: 8% truthfulness improvement):**
   - Module docs BEFORE flow docs. Write `.documentation/modules/{cap}-{feat}.md` first, then `.documentation/flows/{cap}-{feat}.md`.

4. **Output paths and targets:**
   - `.documentation/architecture.md`: update if architectural changes made
   - `.documentation/domain.md`: update if domain model changed
   - `.documentation/mapping.md`: update file-to-concept mappings
   - `.documentation/modules/{cap}-{feat}.md`: module-level docs (what it owns, domain model, API)
   - `.documentation/flows/{cap}-{feat}.md`: data flow docs (how data moves through the feature)
   - `.documentation/capabilities/{cap}.md`: update capability-level overview if needed

5. **Review -> Doc auto-chain:** Per CONTEXT.md: "Review -> Document: Auto-chains." If review is clean, doc stage starts automatically. Ensure the handoff from review completion triggers doc without user intervention. If review surfaces issues, Q&A checkpoint intervenes first.

   **Human checkpoint within doc stage:** Per CONTEXT.md: "surfaces documentation changes for user confirmation before writing." After gsd-doc-writer produces draft docs:
   - Present a summary of what will be written/updated (list of files + key changes)
   - User confirms or requests adjustments
   - Only then write to `.documentation/`

6. **FEATURE.md trace table update:** After doc completion, update the FEATURE.md Trace Table to mark the "Docs" column as complete for all requirements that have been documented.

7. **Inputs block:** Accept CAPABILITY_SLUG, FEATURE_SLUG, and LENS.

Reference RESEARCH-BREAKING-POINTS.md "workflows/review.md" and "workflows/doc.md" sections.
  </action>
  <verify>
    <automated>grep -c "review-phase\|doc-phase\|phase_dir\|phase_number\|phase_found\|PHASE" get-shit-done/workflows/review.md get-shit-done/workflows/doc.md | grep -v ":0$" | wc -l | grep -q "^0$" && echo "PASS: No v1 phase refs" || echo "FAIL: v1 phase refs remain"</automated>
  </verify>
  <done>review.md and doc.md call init feature-op, use feature_dir paths, requirements sourced from FEATURE.md, review traces written to feature_dir/review/. Review->Doc auto-chain preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Update framing-pipeline.md for feature-level targeting</name>
  <files>get-shit-done/workflows/framing-pipeline.md</files>
  <action>
framing-pipeline.md is the backbone orchestrator that calls all 6 stages. It currently operates at capability level. Update to feature level per CONTEXT.md: "Pipeline passes feature only (not cap + feature). Stages derive capability from feature's directory path."

1. **Inputs block:** Add FEATURE_SLUG and FEATURE_DIR as required inputs alongside existing CAPABILITY_SLUG, LENS, BRIEF_PATH. The feature slug is now the primary identifier.

**1a. Lens framing context injection (WKFL-07 wiring):**
Per WKFL-07: "same agents receive different question sets based on active framing." The LENS variable (debug/new/enhance/refactor) must affect behavior at EVERY stage:

- **How it works:** framing-pipeline.md reads the anchor questions file for the active lens: `get-shit-done/framings/{LENS}/anchor-questions.md`. This file contains lens-specific question sets. The pipeline MUST inject the lens + anchor questions into the context block passed to EACH stage workflow.

- **Stage-specific framing effects:**
  - **Research (Stage 1):** Lens affects what gatherers prioritize. Debug lens emphasizes system analysis + edge cases. New lens emphasizes domain truth + prior art. Enhance lens emphasizes existing system + intent. Refactor lens emphasizes tech constraints + system architecture. Pass `LENS` and `ANCHOR_QUESTIONS_PATH` to research-workflow.md so gatherers receive framing context.
  - **Requirements (Stage 2):** Lens affects Q&A questions during requirements review. Debug: "What behavior should change?" New: "What user stories matter?" Enhance: "What's working that must be preserved?" Refactor: "What constraints exist on the refactor?" Include lens-specific question prompts in the requirements Q&A.
  - **Plan (Stage 3):** Lens affects planner risk assessment. Debug plans focus on isolation + reproduction. New plans focus on incremental delivery. Enhance plans focus on backward compatibility. Refactor plans focus on test coverage before change. Pass `LENS` in the context block to plan.md.
  - **Execute (Stage 4):** Lens affects executor approach. Debug: hypothesis-driven. New: feature-flag incremental. Enhance: preserve existing tests. Refactor: change structure without changing behavior. Pass `LENS` to execute.md.
  - **Review (Stage 5):** Lens affects reviewer priorities. Debug reviewers focus on root cause confirmation. New reviewers focus on acceptance criteria. Enhance reviewers focus on regression. Refactor reviewers focus on behavioral equivalence. Pass `LENS` to review.md.
  - **Doc (Stage 6):** Lens affects doc focus. Debug: document the fix and root cause. New: document the new capability. Enhance: document what changed and why. Refactor: document structural changes with before/after. Pass `LENS` to doc.md.

- **Implementation:** Each stage workflow receives `LENS` and `ANCHOR_QUESTIONS_PATH` in its context. The workflow reads the anchor questions and injects them into the prompt/context passed to its agents. This is NOT a new agent -- it's context injection into existing agents per WKFL-07.

- **Handoff framing:** When stages auto-chain (execute -> review -> doc), the LENS context MUST propagate through the chain. Each handoff passes LENS forward. The framing does NOT change mid-pipeline.

2. **Stage 1 (Research):** Change `output_dir` from capability level to feature level:
   - Current: `output_dir: .planning/capabilities/{CAPABILITY_SLUG}`
   - New: `output_dir: .planning/capabilities/{CAPABILITY_SLUG}/features/{FEATURE_SLUG}`
   - Pass `feature_path: ${FEATURE_DIR}/FEATURE.md` to research-workflow.md (currently passed as null)

3. **Stage 2 (Requirements):** Per CONTEXT.md locked decision: "Requirements populate FEATURE.md directly."
   - Current: generates requirements to `.planning/capabilities/{CAPABILITY_SLUG}/REQUIREMENTS.md`
   - New: populate the EU/FN/TC sections of FEATURE.md directly
   - Read the existing FEATURE.md template structure, fill in the End-User Requirements, Functional Requirements, and Technical Specs sections
   - User review Q&A on requirements: show populated FEATURE.md sections for confirmation

4. **Stage 3 (Plan):** Already invokes plan.md -- after Plan 01 rewrites it, pass CAPABILITY_SLUG and FEATURE_SLUG in the framing context block so plan.md can call `init plan-feature`.

5. **Stage 4 (Execute):** Same as Stage 3 -- pass cap/feat slugs to execute.md.

6. **Stage 5 (Review):** Pass cap/feat slugs to review.md.

7. **Stage 6 (Doc/Reflect):** Pass cap/feat slugs to doc.md.

8. **Execute -> Review auto-chain:** Per CONTEXT.md: "Execute -> Review: Auto-chains." After execute.md completes, automatically invoke review.md without user intervention. The review stage itself has a human checkpoint if issues are found.

9. **Review -> Doc auto-chain:** After review completes cleanly, automatically invoke doc.md. If review finds issues, Q&A with user for fix decisions first.

10. **Full auto-chain summary:** Per CONTEXT.md: "User kicks off execute once -> builds code -> auto-reviews against requirements -> auto-documents -> done. Only pauses for human decisions (fix Q&A, doc confirmations)." Ensure stages 4-5-6 chain automatically with checkpoint pauses only for issues.

11. **Banner text:** Update from "Capability: {cap}" to "Feature: {cap}/{feat}" throughout.

12. **Completion:** Update completion status tracking from capability-level to feature-level. Update FEATURE.md frontmatter status to "complete" when all stages finish.

13. **INTG-01 verification:** Confirm that Stage 1 still invokes research-workflow.md which spawns the 6 gatherers via gather-synthesize.md. This wiring was done in Phase 9 -- just verify it still works with the new feature-level output_dir.
  </action>
  <verify>
    <automated>grep -c "FEATURE_SLUG\|feature_dir\|FEATURE_DIR" get-shit-done/workflows/framing-pipeline.md | grep -q "[1-9]" && echo "PASS: Feature-level references present" || echo "FAIL: No feature-level references"</automated>
  </verify>
  <done>framing-pipeline.md accepts FEATURE_SLUG, passes feature-level paths to all 6 stages. Research writes to feature dir. Requirements populate FEATURE.md directly. Execute->Review->Doc auto-chains with human checkpoints only for issues. 6 research gatherers confirmed wired (INTG-01).</done>
</task>

</tasks>

<verification>
- grep for "review-phase", "doc-phase" across review.md and doc.md returns zero matches
- grep for "FEATURE_SLUG" in framing-pipeline.md returns multiple matches
- grep for "feature-op" in review.md and doc.md returns matches
- The pipeline stages sequence: research -> requirements -> plan -> execute -> review -> doc, all operating on feature_dir
</verification>

<success_criteria>
The entire framing pipeline (all 6 stages) operates at feature level. review.md and doc.md call working v2 init routes. framing-pipeline.md passes feature context to all stages. Requirements populate FEATURE.md directly. Execute->Review->Doc auto-chains with human pauses only for decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/12-workflow-optimization-wiring/12-02-SUMMARY.md`
</output>

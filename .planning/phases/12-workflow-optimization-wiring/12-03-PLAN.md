---
phase: 12-workflow-optimization-wiring
plan: 03
type: execute
wave: 2
depends_on: [12-01, 12-02]
files_modified:
  - get-shit-done/workflows/init-project.md
  - get-shit-done/bin/lib/init.cjs
  - get-shit-done/templates/state.md
  - get-shit-done/templates/roadmap.md
  - get-shit-done/templates/capability.md
autonomous: true
requirements: [CMD-01]

must_haves:
  truths:
    - "/gsd:init on a new project creates STATE.md and ROADMAP.md during init"
    - "/gsd:init on a brownfield project creates STATE.md and ROADMAP.md during init"
    - "STATE.md template uses focus group model with active capability/feature tracking"
    - "ROADMAP.md template uses focus group model with priority-ordered items and dependency lines"
    - "CAPABILITY.md template has Why section and Features table with priority + depends-on columns"
    - "Init completion message directs users to /gsd:discuss-capability <cap> as primary next step -- NOT /gsd:focus (requires features) and NOT /gsd:new (stubs already created)"
    - "Project Q&A includes design/styling questions (Step 3c.5 for new, Step 4c.5 for brownfield)"
  artifacts:
    - path: "get-shit-done/workflows/init-project.md"
      provides: "v2 init with STATE.md + ROADMAP.md bootstrap"
    - path: "get-shit-done/templates/state.md"
      provides: "v2 STATE.md template with focus groups"
    - path: "get-shit-done/templates/roadmap.md"
      provides: "v2 ROADMAP.md template with focus groups"
    - path: "get-shit-done/templates/capability.md"
      provides: "Updated capability template with priority + why"
  key_links:
    - from: "get-shit-done/workflows/init-project.md"
      to: "get-shit-done/templates/state.md"
      via: "template fill state"
      pattern: "STATE.md"
    - from: "get-shit-done/workflows/init-project.md"
      to: "get-shit-done/templates/roadmap.md"
      via: "template fill roadmap"
      pattern: "ROADMAP.md"
---

<objective>
Fix the B2 blocker: STATE.md and ROADMAP.md are not created during /gsd:init. Also rewrite the state.md, roadmap.md, and capability.md templates for the v2 focus group model.

Purpose: Every pipeline stage reads STATE.md. resume-work.md needs ROADMAP.md. Without these files, the pipeline degrades silently or fails. The template rewrites establish the v2 data model that all downstream workflows consume.

Output: Updated init-project.md with STATE/ROADMAP bootstrap steps. Three rewritten templates.
</objective>

<execution_context>
@/Users/philliphall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philliphall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-workflow-optimization-wiring/12-CONTEXT.md
@.planning/phases/12-workflow-optimization-wiring/RESEARCH-INIT-BOOTSTRAP.md

Read current state of files being modified:
@get-shit-done/workflows/init-project.md
@get-shit-done/templates/state.md
@get-shit-done/templates/roadmap.md
@get-shit-done/templates/capability.md
@get-shit-done/bin/lib/init.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add STATE.md + ROADMAP.md bootstrap to init-project.md and update init.cjs</name>
  <files>get-shit-done/workflows/init-project.md, get-shit-done/bin/lib/init.cjs</files>
  <action>
**init-project.md changes:**

1. **New-project flow -- add Steps 3g and 3h after Step 3f (documentation seeded):**

   Step 3g: Write ROADMAP.md
   - Write `.planning/ROADMAP.md` using v2 roadmap template
   - For new projects: empty focus groups section (no active focus yet)
   - Include Overview section with project name from PROJECT.md
   - Commit: `docs: initialize roadmap`
   - Update init-state.json with completed_sections += ["roadmap_md"]

   Step 3h: Write STATE.md
   - Write `.planning/STATE.md` using v2 state template
   - Empty active focus groups (no active work yet)
   - Project reference points to PROJECT.md
   - Commit: `docs: initialize state`
   - Update init-state.json with completed_sections += ["state_md"]

2. **Brownfield flow -- add Steps 4g and 4h after Step 4f (documentation seeded):**

   Step 4g: Write ROADMAP.md
   - Write `.planning/ROADMAP.md` using v2 roadmap template
   - Pre-populate one focus group from inferred capabilities (user can refine via /gsd:focus later)
   - Commit: `docs: initialize roadmap from codebase scan`
   - Update init-state.json with completed_sections += ["roadmap_md"]

   Step 4h: Write STATE.md
   - Same as new-project step 3h
   - Commit: `docs: initialize state`
   - Update init-state.json with completed_sections += ["state_md"]

3. **Full Project Q&A scope (Steps 3a-3c) -- verify and expand:**
   Per CONTEXT.md locked decision: "New project flow: Project Q&A (goals, tech stack, architecture, brand/style) -> scaffolding -> Capabilities Q&A"

   The current init-project.md has Steps 3a (Goals), 3b (Tech Stack), 3c (Architecture). These cover goals + tech stack + architecture but are MISSING design/styling/brand. Add:

   **Step 3c.5: Design & Styling Q&A** (new step, insert between 3c and 3d):
   - Ask about visual style preferences: minimalist vs feature-rich, design system/brand guidelines, UI framework preferences (if applicable)
   - Ask about styling approach: CSS framework, component library, design tokens, theming
   - For non-UI projects: ask about API design philosophy, output formatting, developer experience priorities
   - This is a LIGHT Q&A (2-4 questions) -- not a design system. Captures opinions that affect downstream planning.
   - Update init-state.json with completed_sections += ["design_style"]
   - Add "design_style" to the section arrays in init.cjs (both new and brownfield paths)

   **Brownfield equivalent (Step 4c.5):** During gap-fill Q&A, add design/styling questions:
   - "What design patterns or styling conventions exist in this codebase?"
   - "Any design opinions or standards you want to enforce going forward?"
   - Fold answers into PROJECT.md under a Design & Standards section

4. **Capabilities Q&A step (Step 3e.0) -- insert before capability creation:**
   Per CONTEXT.md: "New project flow: Project Q&A -> scaffolding -> Capabilities Q&A (all potential capabilities at high level)"
   - Present inferred capabilities from Q&A context to user
   - User confirms, adds, removes, reorders
   - Then proceed to capability-create for confirmed list

5. **Completion message (Step 5):** Change from:
   `"Run /gsd:new to set up requirements and roadmap"`
   to:
   ```
   "Project initialized with {N} capabilities and {M} feature stubs.

   Next step:
   - Run /gsd:discuss-capability <name> to flesh out a capability stub
     (this creates feature stubs within the capability)

   Later, once you have features:
   - Run /gsd:discuss-feature <cap/feat> to detail a specific feature
   - Run /gsd:focus to create a focus group and prioritize what to build

   (The capabilities above are stubs -- discuss them to discover features before planning.)"
   ```

   **CRITICAL: Post-init flow ordering.**
   - **Primary next step:** `/gsd:discuss-capability <cap>` -- fleshes out a capability stub, which creates feature stubs within it.
   - **NOT `/gsd:focus`** -- focus groups sequence features. Features don't exist yet after init. Focus comes AFTER discuss-capability has created feature stubs.
   - **NOT `/gsd:new`** -- init already created the capability stubs. `/gsd:new` is for adding capabilities later, not the first action after init.
   - The flow is: init -> discuss-capability (creates features) -> discuss-feature (optional depth) -> focus (sequence features) -> plan -> execute.

**init.cjs changes:**

5. **cmdInitProject() section arrays:** Add "roadmap_md" and "state_md" to the section arrays:
   ```javascript
   // New-project sections:
   ['goals', 'tech_stack', 'architecture', 'design_style', 'project_md', 'capability_map', 'documentation', 'roadmap_md', 'state_md']
   // Brownfield sections:
   ['scan', 'validation', 'gap_fill', 'design_style', 'project_md', 'capability_map', 'documentation', 'roadmap_md', 'state_md']
   ```
   This enables partial-run resume to pick up where it left off including the new steps.
  </action>
  <verify>
    <automated>grep -c "roadmap_md\|state_md" get-shit-done/bin/lib/init.cjs | grep -q "[1-9]" && grep -c "ROADMAP.md\|STATE.md" get-shit-done/workflows/init-project.md | grep -q "[2-9]" && echo "PASS: Bootstrap steps present" || echo "FAIL: Missing bootstrap"</automated>
  </verify>
  <done>init-project.md creates STATE.md and ROADMAP.md for both new and brownfield flows. init.cjs section arrays include roadmap_md and state_md for partial-run resume. Capabilities Q&A step added. Completion message updated.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite state.md, roadmap.md, and capability.md templates</name>
  <files>get-shit-done/templates/state.md, get-shit-done/templates/roadmap.md, get-shit-done/templates/capability.md</files>
  <action>
**templates/state.md -- full rewrite to v2 format:**

Replace the entire template with the v2 focus group model from RESEARCH-INIT-BOOTSTRAP.md:

```markdown
---
gsd_state_version: 2.0
active_focus: null
status: unknown
last_updated: "{date}"
progress:
  capabilities: 0
  features: 0
  total_plans: 0
  completed_plans: 0
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated {date})
**Core value:** {core_value}

## Active Focus Groups

{empty on init -- populated by /gsd:focus}

## Key Decisions

{empty on init}

## Blockers

{none}

## Session Continuity

Last session: {date}
Last action: Project initialized
Resume: Run /gsd:new or /gsd:discuss-capability to begin
```

Key changes from v1: Focus groups replace phase tracking. No performance metrics section. No phase-based current position. Active capability/feature tracked per focus group.

**templates/roadmap.md -- full rewrite to v2 focus group format:**

Replace the entire phase-based template with the focus group model from CONTEXT.md:

```markdown
# Roadmap: {project_name}

## Overview

{project_overview}

## Active Focus Groups

{empty on init for new projects, pre-populated for brownfield}

## Completed Focus Groups

{empty}
```

The focus group format per CONTEXT.md:
```
### Focus: {group-name}
**Goal:** {one sentence}
**Priority Order:**
1. {cap}/{feat} -> depends: none
2. {cap}/{feat} -> depends: {above}
**Status:**
- [ ] {cap}/{feat} (not started)
```

Key changes: No phases. No success criteria (live in FEATURE.md). No requirement mappings (live in FEATURE.md). No plan lists. Per-item dependency lines. Completed groups collapse to simple checklist.

**templates/capability.md -- update for v2:**

Add to the existing template:
1. Add `name: "{name}"` to frontmatter (needed for slug resolution matching)
2. Add `## Why` section after `## Goal` per CONTEXT.md: "CAPABILITY.md holds: what + why + feature list + priority order"
3. Update Features table to include Priority and Depends-On columns:
   `| Feature | Priority | Depends-On | Status |`
4. Add priority order field concept -- features are listed in priority order

Do NOT change other sections (Domain Model, Invariants, Boundaries, Architecture Spine, Dependencies, Decisions) -- these are fine.
  </action>
  <verify>
    <automated>grep -c "Active Focus Groups\|active_focus" get-shit-done/templates/state.md | grep -q "[1-9]" && grep -c "Active Focus Groups\|Priority Order" get-shit-done/templates/roadmap.md | grep -q "[1-9]" && grep -c "Why\|Priority\|Depends-On" get-shit-done/templates/capability.md | grep -q "[1-9]" && echo "PASS: Templates updated" || echo "FAIL: Template content missing"</automated>
  </verify>
  <done>state.md template uses v2 focus group model. roadmap.md template uses focus group format with priority-ordered items. capability.md has name in frontmatter, Why section, and Features table with Priority + Depends-On columns.</done>
</task>

</tasks>

<verification>
- grep for "Active Focus Groups" in templates/state.md and templates/roadmap.md returns matches
- grep for "gsd_state_version: 2.0" in templates/state.md returns match
- grep for "roadmap_md" in init.cjs returns match
- grep for "STATE.md" in init-project.md returns multiple matches (bootstrap steps)
- No "Phase" or "milestone" references in the rewritten templates (except historical docs)
</verification>

<success_criteria>
/gsd:init creates STATE.md and ROADMAP.md as part of both new and brownfield flows. Templates use the v2 focus group model. B2 blocker is resolved.
</success_criteria>

<output>
After completion, create `.planning/phases/12-workflow-optimization-wiring/12-03-SUMMARY.md`
</output>

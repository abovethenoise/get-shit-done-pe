---
phase: 12-workflow-optimization-wiring
plan: 07
type: execute
wave: 4
depends_on: [12-01, 12-02, 12-03, 12-04, 12-05, 12-06, 12-09]
files_modified:
  - get-shit-done/workflows/*.md (19 dead files deleted)
  - commands/gsd/*.md (17 dead files deleted)
  - get-shit-done/templates/*.md (dead templates deleted)
  - get-shit-done/references/*.md (dead references deleted)
  - get-shit-done/bin/gsd-tools.cjs
  - get-shit-done/bin/lib/init.cjs
  - get-shit-done/bin/lib/phase.cjs
  - get-shit-done/bin/lib/milestone.cjs
  - get-shit-done/bin/lib/verify.cjs
  - get-shit-done/bin/lib/state.cjs
  - get-shit-done/bin/lib/core.cjs
  - get-shit-done/bin/lib/config.cjs
  - get-shit-done/bin/lib/roadmap.cjs
autonomous: true
requirements: [CMD-01]

must_haves:
  truths:
    - "19 dead workflow files deleted"
    - "17 dead command files deleted"
    - "Dead templates deleted (phase-prompt.md, milestone-archive.md, milestone.md, planner-subagent-prompt.md)"
    - "Dead references deleted (phase-argument-parsing.md, decimal-phase-calculation.md)"
    - "phase.cjs entirely deleted"
    - "Dead init functions removed from init.cjs (~480 lines)"
    - "Dead router cases removed from gsd-tools.cjs (phase, milestone, phases, phase-plan-index, etc.)"
    - "Dead functions removed from milestone.cjs, verify.cjs, state.cjs, core.cjs"
    - "v1 config defaults (phase_branch_template, milestone_branch_template) removed from core.cjs and config.cjs"
  artifacts:
    - path: "get-shit-done/bin/lib/phase.cjs"
      provides: "FILE DELETED"
    - path: "get-shit-done/bin/gsd-tools.cjs"
      provides: "Router with only v2 routes"
  key_links:
    - from: "get-shit-done/bin/gsd-tools.cjs"
      to: "get-shit-done/bin/lib/init.cjs"
      via: "only v2 init routes remain"
      pattern: "init plan-feature|init execute-feature|init feature-op"
---

<objective>
Delete all dead v1 code. Per CONTEXT.md: "Wire v2 first, then delete v1." Plans 01-06 wired v2. This plan removes ~10,500 lines of dead v1 artifacts.

Purpose: Clean break before Phase 13 E2E testing. Every line of code remaining after this plan is either v2 or still serving a live function.

Output: ~10,500 lines of dead code removed across workflows, commands, templates, references, and lib modules.
</objective>

<execution_context>
@/Users/philliphall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philliphall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-workflow-optimization-wiring/12-CONTEXT.md
@.planning/phases/12-workflow-optimization-wiring/RESEARCH-DEAD-CODE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead workflow files, command files, templates, and references</name>
  <files>get-shit-done/workflows/*.md (deletions), commands/gsd/*.md (deletions), get-shit-done/templates/*.md (deletions), get-shit-done/references/*.md (deletions)</files>
  <action>
Follow the deletion dependency order from RESEARCH-DEAD-CODE.md Section 13 "PHASE 1 (safe to delete now)."

**Delete 19 dead workflow files (~4,600 lines):**
Per RESEARCH-DEAD-CODE.md Section 4:
- plan-phase.md
- execute-phase.md
- discuss-phase.md
- verify-phase.md
- research-phase.md
- new-milestone.md
- complete-milestone.md
- audit-milestone.md
- plan-milestone-gaps.md
- add-phase.md
- insert-phase.md
- remove-phase.md
- discovery-phase.md
- list-phase-assumptions.md
- diagnose-issues.md
- verify-work.md
- pause-work.md
- transition.md
- execute-plan.md (v1 wrapper -- the execute-plan.md in the v2 pipeline was rewritten in Plan 01; this is a separate v1 file if it still exists. Verify before deleting -- check if the file was already rewritten. If the execute-plan.md was rewritten in Plan 01, do NOT delete it.)

IMPORTANT: Before deleting execute-plan.md, verify it is the v1 version (references init execute-phase) and NOT the v2 version already rewritten in Plan 01. The file was listed in RESEARCH-DEAD-CODE.md as dead but was also modified in Plan 01. If Plan 01 already rewrote it to v2, skip this deletion.

**Delete 17 dead command files (~500 lines):**
Per RESEARCH-DEAD-CODE.md Section 5:
- add-phase.md
- audit-milestone.md
- complete-milestone.md
- discuss-phase.md
- execute-phase.md
- insert-phase.md
- list-phase-assumptions.md
- new-milestone.md
- plan-milestone-gaps.md
- plan-phase.md
- remove-phase.md
- research-phase.md
- verify-phase.md
- verify-work.md
- pause-work.md
- reapply-patches.md
- new-project.md.bak

Verify each file exists before deletion. Some may have been deleted in earlier phases.

**Delete dead templates:**
Per RESEARCH-DEAD-CODE.md Section 6:
- milestone-archive.md
- milestone.md
- phase-prompt.md (v1 plan template)
- planner-subagent-prompt.md (v1 planner spawn template)
- discovery.md (if v1-only -- verify; may have been updated in Plan 06)
- retrospective.md (milestone retrospective)

Verify each exists and is genuinely dead. If a template was updated in Plan 06, it is NOT dead.

**Delete dead references:**
Per RESEARCH-DEAD-CODE.md Section 7:
- phase-argument-parsing.md (entire doc describes v1 phase parsing)
- decimal-phase-calculation.md (entire doc describes v1 decimal phases)

These are definitively dead. Delete them.

Commit all deletions: `chore(12): delete dead v1 workflow, command, template, and reference files`
  </action>
  <verify>
    <automated>ls get-shit-done/workflows/plan-phase.md get-shit-done/workflows/execute-phase.md get-shit-done/workflows/new-milestone.md commands/gsd/plan-phase.md commands/gsd/new-milestone.md get-shit-done/references/phase-argument-parsing.md 2>/dev/null | wc -l | grep -q "^0$" && echo "PASS: Dead files deleted" || echo "FAIL: Dead files remain"</automated>
  </verify>
  <done>All dead v1 workflow files (~19), command files (~17), templates, and references deleted. Only surviving v2 files remain.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead CLI routes and lib functions</name>
  <files>get-shit-done/bin/gsd-tools.cjs, get-shit-done/bin/lib/init.cjs, get-shit-done/bin/lib/phase.cjs, get-shit-done/bin/lib/milestone.cjs, get-shit-done/bin/lib/verify.cjs, get-shit-done/bin/lib/state.cjs, get-shit-done/bin/lib/core.cjs, get-shit-done/bin/lib/config.cjs, get-shit-done/bin/lib/roadmap.cjs</files>
  <action>
Follow RESEARCH-DEAD-CODE.md Sections 1-3 deletion dependency order (PHASE 2 + PHASE 3 combined since agents are now rewritten).

**phase.cjs -- DELETE ENTIRE FILE (~870 lines):**
All functions are dead: cmdPhasesList, cmdPhaseNextDecimal, cmdFindPhase, cmdPhasePlanIndex, cmdPhaseAdd, cmdPhaseInsert, cmdPhaseRemove, cmdPhaseComplete.

**gsd-tools.cjs -- remove dead router cases (~150 lines):**
Per RESEARCH-DEAD-CODE.md Section 1.2:
- Delete `case 'phase':` block (next-decimal, add, insert, remove, complete)
- Delete `case 'milestone':` block (complete)
- Delete `case 'phases':` block (list)
- Delete `case 'phase-plan-index':` route
- Delete `validate consistency` subcommand
- Delete `scaffold context/uat/verification/phase-dir` routes
- Delete `template fill` (summary/plan/verification) and `template select` routes
- Delete `verify phase-completeness` route
- Remove dead init routes: `init execute-phase`, `init plan-phase`, `init new-milestone`, `init verify-work`, `init milestone-op`, `init progress`, `init phase-op`
- Remove `require('./lib/phase.cjs')` import
- Update header comment (lines 33-96) to document v2 routes only
- Update error message on line 562 to remove dead route names

**init.cjs -- remove dead functions (~480 lines):**
- Delete cmdInitExecutePhase (~71 lines)
- Delete cmdInitPlanPhase (~77 lines)
- Delete cmdInitNewMilestone (~30 lines)
- Delete cmdInitVerifyWork (~28 lines)
- Delete cmdInitMilestoneOp (~59 lines)
- Delete cmdInitProgress (~96 lines)
- Delete cmdInitPhaseOp (~80 lines, agents now rewritten)
- Update module.exports to remove dead function exports

**milestone.cjs -- remove dead function (~185 lines):**
- Delete cmdMilestoneComplete
- Keep cmdRequirementsMarkComplete (still used by requirements mark-complete route)

**verify.cjs -- remove dead functions (~170 lines):**
- Delete cmdVerifyPhaseCompleteness
- Delete cmdValidateConsistency
- Keep cmdValidateHealth, cmdVerifyPlanStructure, cmdVerifyReferences, cmdVerifyCommits, cmdVerifyArtifacts, cmdVerifyKeyLinks

**state.cjs -- remove dead functions (~65 lines):**
- Delete cmdStateAdvancePlan
- Delete cmdStateRecordMetric
- Update buildStateFrontmatter: remove v1 field extraction (current_phase, current_phase_name, current_plan, total_phases, completed_phases .planning/phases/ scan). Keep v2 fields (active_capability, active_feature, pipeline_position).

**core.cjs -- remove dead helpers (~200 lines):**
- Delete normalizePhaseName, comparePhaseNum, searchPhaseInDir, findPhaseInternal, getArchivedPhaseDirs, getRoadmapPhaseInternal
- Delete getMilestoneInfo (no more callers after init functions deleted)
- Remove v1 config defaults: phase_branch_template, milestone_branch_template from loadConfig

**config.cjs -- remove v1 defaults (~10 lines):**
- Remove phase_branch_template and milestone_branch_template from cmdConfigEnsureSection defaults

**roadmap.cjs -- audit and clean:**
- cmdRoadmapGetPhase: DELETE (no callers after agents rewritten)
- cmdRoadmapAnalyze: DELETE (only called by dead v1 workflows)
- cmdRoadmapUpdatePlanProgress: DELETE or ADAPT for feature-level (check if any v2 workflow calls this)
- Keep any functions that serve v2 workflows

Commit: `chore(12): remove dead v1 CLI routes and lib functions (~2,000 lines)`
  </action>
  <verify>
    <automated>test ! -f get-shit-done/bin/lib/phase.cjs && grep -c "plan-phase\|execute-phase\|new-milestone\|verify-work\|milestone-op\|phase-plan-index\|case 'phase':\|case 'milestone':\|case 'phases':" get-shit-done/bin/gsd-tools.cjs | grep -q "^0$" && echo "PASS: Dead routes removed" || echo "FAIL: Dead code remains"</automated>
  </verify>
  <done>phase.cjs deleted. Dead router cases removed from gsd-tools.cjs. Dead init functions removed from init.cjs. Dead functions removed from milestone.cjs, verify.cjs, state.cjs, core.cjs, config.cjs, roadmap.cjs. Only v2-serving code remains in lib modules.</done>
</task>

</tasks>

<verification>
- phase.cjs file does not exist
- grep for "case 'phase':" in gsd-tools.cjs returns zero
- grep for "cmdInitExecutePhase\|cmdInitPlanPhase\|cmdInitNewMilestone" in init.cjs returns zero
- grep for "cmdMilestoneComplete" in milestone.cjs returns zero
- grep for "normalizePhaseName\|findPhaseInternal\|getMilestoneInfo" in core.cjs returns zero
- node gsd-tools.cjs (with no args) runs without crash (no missing imports)
</verification>

<success_criteria>
~10,500 lines of dead v1 code removed. No dead imports, no broken requires. gsd-tools.cjs router contains only v2 routes. Clean break achieved.
</success_criteria>

<output>
After completion, create `.planning/phases/12-workflow-optimization-wiring/12-07-SUMMARY.md`
</output>

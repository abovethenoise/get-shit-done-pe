---
phase: 12-workflow-optimization-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/plan.md
  - get-shit-done/workflows/execute.md
  - get-shit-done/workflows/execute-plan.md
autonomous: true
requirements: [CMD-01]

must_haves:
  truths:
    - "plan.md calls init plan-feature <cap> <feat> instead of init plan-phase"
    - "execute.md calls init execute-feature <cap> <feat> instead of init execute-phase"
    - "execute-plan.md uses feature directory paths instead of .planning/phases/ paths"
    - "No phase number variables (PHASE, phase_number, padded_phase) remain in any modified file"
    - "PRD express path, Nyquist validation, gap-closure decimal logic, and auto-advance sections are removed"
  artifacts:
    - path: "get-shit-done/workflows/plan.md"
      provides: "v2 feature-scoped planning workflow"
    - path: "get-shit-done/workflows/execute.md"
      provides: "v2 feature-scoped execution orchestrator"
    - path: "get-shit-done/workflows/execute-plan.md"
      provides: "v2 feature-scoped single-plan executor"
  key_links:
    - from: "get-shit-done/workflows/plan.md"
      to: "bin/lib/init.cjs cmdInitPlanFeature"
      via: "gsd-tools init plan-feature"
      pattern: "init plan-feature"
    - from: "get-shit-done/workflows/execute.md"
      to: "bin/lib/init.cjs cmdInitExecuteFeature"
      via: "gsd-tools init execute-feature"
      pattern: "init execute-feature"
---

<objective>
Rewrite plan.md, execute.md, and execute-plan.md to call v2 feature-scoped CLI init routes instead of v1 phase-scoped routes. This is the first half of the B1 blocker fix -- the planning and execution pipeline speaks v2.

Purpose: The v2 CLI routes (init plan-feature, init execute-feature) already exist and return correct data shapes. These 3 workflows still call v1 routes that either return errors or produce wrong paths. Swapping the init calls + field names is the highest-leverage single change.

Output: Three rewritten workflow files that operate on feature directories instead of phase directories.
</objective>

<execution_context>
@/Users/philliphall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philliphall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-workflow-optimization-wiring/12-CONTEXT.md
@.planning/phases/12-workflow-optimization-wiring/RESEARCH-BREAKING-POINTS.md
@.planning/phases/12-workflow-optimization-wiring/RESEARCH-CONSOLIDATION.md
@.planning/phases/12-workflow-optimization-wiring/RESEARCH-ARCHITECTURE.md

Read these files to understand the current v1 state before rewriting:
@get-shit-done/workflows/plan.md
@get-shit-done/workflows/execute.md
@get-shit-done/workflows/execute-plan.md
@get-shit-done/bin/lib/init.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite plan.md for v2 feature model</name>
  <files>get-shit-done/workflows/plan.md</files>
  <action>
Rewrite the planning workflow to use v2 feature-scoped init and paths:

1. **Init call (line ~18):** Replace `init plan-phase "$PHASE"` with `init plan-feature "$CAPABILITY_SLUG" "$FEATURE_SLUG"`. These slugs come from the framing-pipeline context or from direct user invocation via /gsd:plan.

2. **JSON field mapping:** Replace all v1 field references:
   - `phase_found` -> `feature_found`
   - `phase_dir` -> `feature_dir`
   - `phase_number` -> DROP (not applicable)
   - `phase_name` -> `feature_slug`
   - `phase_slug` -> `feature_slug`
   - `padded_phase` -> DROP
   - `phase_req_ids` -> derive from FEATURE.md EU/FN/TC sections
   - `requirements_path` -> `feature_dir + "/FEATURE.md"`

3. **Remove v1-only sections:**
   - PRD express path (~55 lines, `--prd` flag handling)
   - Nyquist validation section (~35 lines, already disabled in config)
   - Gap-closure mode (`--gaps` flag, ~50 lines of decimal phase logic)
   - Auto-advance chain (~45 lines, `--auto` flag + `workflow.auto_advance` config check). Per CONTEXT.md: "No auto-advance. User runs /gsd:plan when ready."

4. **Path surgery throughout:**
   - Replace `.planning/phases/${padded_phase}-${phase_slug}` with `${feature_dir}`
   - Replace `roadmap get-phase "${PHASE}"` with reading FEATURE.md directly for goal/requirements
   - Replace `commit "docs(${padded_phase}):..."` with `commit "docs(${CAPABILITY_SLUG}/${FEATURE_SLUG}):..."`
   - Replace `"Phase {phase_number}: {phase_name}"` with `"Feature: ${CAPABILITY_SLUG}/${FEATURE_SLUG}"`
   - Replace `PHASE ${PHASE} PLANNED` banner with `FEATURE ${FEATURE_SLUG} PLANNED`

5. **Planner context block:** Update the prompt passed to gsd-planner.md:
   - Replace `**Phase:** {phase_number}` with `**Feature:** {CAPABILITY_SLUG}/{FEATURE_SLUG}`
   - Replace `phase requirement IDs` with requirement IDs extracted from FEATURE.md
   - Point `@file` references to feature directory, not phase directory
   - **Lens framing injection (WKFL-07):** Add `**Lens:** {LENS}` and `**Anchor Questions:** @{ANCHOR_QUESTIONS_PATH}` to the planner context block. The planner reads the lens-specific anchor questions to adjust risk assessment, task decomposition style, and planning approach per framing. Debug plans focus on isolation; new plans on incremental delivery; enhance plans on backward compatibility; refactor plans on test-before-change.

6. **Self-critique loop, plan-checker spawn, revision loop:** Keep these intact -- they are logic-agnostic to phase vs feature. Only update field names in the context they pass.

7. **Inputs block at top of workflow:** Accept CAPABILITY_SLUG and FEATURE_SLUG as inputs (from framing-pipeline or from direct /gsd:plan invocation).

Reference RESEARCH-BREAKING-POINTS.md "workflows/plan.md" section for the complete line-by-line inventory of v1 references to replace.
  </action>
  <verify>
    <automated>grep -c "plan-phase\|phase_dir\|phase_number\|padded_phase\|PHASE_ARG\|phase_slug\|phase_found\|init plan-phase" get-shit-done/workflows/plan.md | grep -q "^0$" && echo "PASS: No v1 phase refs" || echo "FAIL: v1 phase refs remain"</automated>
  </verify>
  <done>plan.md calls init plan-feature, uses feature_dir throughout, all phase variables removed, PRD/Nyquist/gap-closure/auto-advance sections deleted. Self-critique and plan-checker loops preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite execute.md and execute-plan.md for v2 feature model</name>
  <files>get-shit-done/workflows/execute.md, get-shit-done/workflows/execute-plan.md</files>
  <action>
**execute.md rewrite:**

1. **Init call (line ~19):** Replace `init execute-phase "${PHASE_ARG}"` with `init execute-feature "$CAPABILITY_SLUG" "$FEATURE_SLUG"`.

2. **JSON field mapping:** Same pattern as plan.md:
   - `phase_found` -> `feature_found`
   - `phase_dir` -> `feature_dir`
   - `phase_number`, `phase_name`, `phase_slug` -> DROP / replace with feature equivalents
   - `phase_req_ids` -> derive from FEATURE.md

3. **Remove v1-only sections:**
   - Decimal phase gap-closure logic (`close_parent_artifacts` step, ~60 lines where `$PHASE_NUMBER == *.*`)
   - `phase complete "${PHASE_NUMBER}"` call -> replace with feature status update: update FEATURE.md frontmatter `status: complete`
   - `roadmap update-plan-progress "${PHASE}"` call -> replace with feature-level tracking (count PLANs vs SUMMARYs in feature_dir)
   - `phase-plan-index "${PHASE_NUMBER}"` -> replace with direct feature directory plan scan: `ls ${feature_dir}/*-PLAN.md`
   - `"phase" or "milestone"` branching strategy section -> remove "phase" option, keep "milestone" for git branching only if user desires

4. **Path surgery:**
   - All `.planning/phases/XX-name/` references -> `${feature_dir}/`
   - Phase completion/advance STATE.md logic -> feature-level state tracking (update STATE.md active feature)
   - `find-phase "${PARENT_PHASE}"` -> remove (no parent phase concept in feature model)

5. **Wave execution, checkpoint handling, spot-check logic:** Keep intact -- these are agnostic.

6. **Inputs block:** Accept CAPABILITY_SLUG and FEATURE_SLUG.

**execute-plan.md rewrite:**

1. **Init call (line ~18):** Replace `init execute-phase "${PHASE}"` with `init execute-feature "$CAPABILITY_SLUG" "$FEATURE_SLUG"`.

2. **Path patterns throughout (~15 locations):**
   - `.planning/phases/XX-name/*-PLAN.md` -> `${feature_dir}/*-PLAN.md`
   - `.planning/phases/XX-name/*-SUMMARY.md` -> `${feature_dir}/*-SUMMARY.md`
   - `{phase}-{plan}-PLAN.md` naming -> `{plan}-PLAN.md` (no phase prefix)
   - `{phase}-{plan}-SUMMARY.md` -> `{plan}-SUMMARY.md`

3. **State update calls:**
   - `state advance-plan` -> update active plan counter in feature scope
   - `roadmap update-plan-progress "${PHASE}"` -> count plans/summaries in feature_dir

4. **Commit patterns:** Replace `docs(phase-${PARENT_PHASE})` with `docs(${CAPABILITY_SLUG}/${FEATURE_SLUG})`

5. **Phase/milestone complete routing (lines 396-397):** Replace with feature completion check: if all plans have summaries, update FEATURE.md status.

Reference RESEARCH-BREAKING-POINTS.md "workflows/execute.md" and "workflows/execute-plan.md" sections for line-by-line inventories.
  </action>
  <verify>
    <automated>grep -c "execute-phase\|phase_dir\|phase_number\|PHASE_ARG\|phase_slug\|phase_found\|plan-phase\|phase complete\|close_parent_artifacts" get-shit-done/workflows/execute.md get-shit-done/workflows/execute-plan.md | grep -v ":0$" | wc -l | grep -q "^0$" && echo "PASS: No v1 phase refs" || echo "FAIL: v1 phase refs remain"</automated>
  </verify>
  <done>execute.md and execute-plan.md call init execute-feature, use feature_dir paths, phase gap-closure removed, feature status tracking replaces phase completion. Wave execution and checkpoint logic preserved.</done>
</task>

</tasks>

<verification>
- grep for "plan-phase", "execute-phase", "phase_dir", "phase_number" across all 3 modified files returns zero matches
- grep for "plan-feature" in plan.md returns at least 1 match
- grep for "execute-feature" in execute.md and execute-plan.md returns at least 1 match each
- grep for "feature_dir" in all 3 files returns matches (confirming v2 paths used)
</verification>

<success_criteria>
The three core pipeline workflows (plan, execute, execute-plan) call v2 init routes and operate on feature directories. No phase-scoped variables, paths, or CLI calls remain. V1-only complexity (PRD, Nyquist, gap-closure, auto-advance, decimal phases) is removed.
</success_criteria>

<output>
After completion, create `.planning/phases/12-workflow-optimization-wiring/12-01-SUMMARY.md`
</output>

---
phase: 12-workflow-optimization-wiring
plan: 09
type: execute
wave: 3
depends_on: [12-01, 12-02, 12-03, 12-04]
files_modified:
  - get-shit-done/workflows/execute.md
  - get-shit-done/workflows/execute-plan.md
  - get-shit-done/workflows/plan.md
  - get-shit-done/workflows/progress.md
  - get-shit-done/workflows/resume-work.md
  - get-shit-done/workflows/review.md
  - get-shit-done/workflows/doc.md
autonomous: true
requirements: [CMD-01]

must_haves:
  truths:
    - "All 7 workflows are condensed: v1 cruft removed, unnecessary verbosity trimmed"
    - "All 7 workflows use v2 terminology and paths (feature directories, FEATURE.md, capability/feature slugs)"
    - "progress.md updated for feature/capability/focus group model"
    - "resume-work.md updated for feature/capability model with focus group support"
    - "Orchestration logic preserved in all workflows -- only prose and v1 artifacts removed"
    - "No workflow references phase directories, phase numbers, milestones, or v1 init routes"
  artifacts:
    - path: "get-shit-done/workflows/execute.md"
      provides: "Condensed v2 execution orchestrator"
    - path: "get-shit-done/workflows/execute-plan.md"
      provides: "Condensed v2 single-plan executor"
    - path: "get-shit-done/workflows/plan.md"
      provides: "Condensed v2 planning workflow"
    - path: "get-shit-done/workflows/progress.md"
      provides: "v2 progress workflow with capability/feature/focus group display"
    - path: "get-shit-done/workflows/resume-work.md"
      provides: "v2 resume workflow with focus group support"
    - path: "get-shit-done/workflows/review.md"
      provides: "Condensed v2 review workflow"
    - path: "get-shit-done/workflows/doc.md"
      provides: "Condensed v2 documentation workflow"
  key_links:
    - from: "get-shit-done/workflows/resume-work.md"
      to: ".planning/capabilities/"
      via: "scans for incomplete features and focus group status"
      pattern: "capabilities"
    - from: "get-shit-done/workflows/progress.md"
      to: "bin/lib/init.cjs cmdInitFeatureProgress"
      via: "gsd-tools init feature-progress"
      pattern: "feature-progress"
---

<objective>
Optimization pass on 7 workflow files: condense, remove v1 cruft, update terminology, and optimize for the v2 workflow model. This is NOT a rewrite -- it's a focused cleanup that preserves orchestration logic while removing unnecessary verbosity and outdated references.

Purpose: After Plans 12-01 and 12-02 rewrite the init calls and field mappings, the workflow files will have v2 wiring but may still carry v1 prose, redundant comments, over-specified instructions, and stale terminology. This plan trims them to match the lean v2 model. Workflows don't need goldilocks-tight 1500 tokens like agents, but should be clear and efficient.

Not in scope: framing-pipeline.md (already v2-ready), framing-discovery.md (already v2-ready).

Output: 7 optimized workflow files.
</objective>

<execution_context>
@/Users/philliphall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philliphall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-workflow-optimization-wiring/12-CONTEXT.md
@.planning/phases/12-workflow-optimization-wiring/RESEARCH-CONSOLIDATION.md
@.planning/phases/12-workflow-optimization-wiring/RESEARCH-BREAKING-POINTS.md

Read the workflow files to audit:
@get-shit-done/workflows/execute.md
@get-shit-done/workflows/execute-plan.md
@get-shit-done/workflows/plan.md
@get-shit-done/workflows/progress.md
@get-shit-done/workflows/resume-work.md
@get-shit-done/workflows/review.md
@get-shit-done/workflows/doc.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and condense execute.md, execute-plan.md, and plan.md</name>
  <files>get-shit-done/workflows/execute.md, get-shit-done/workflows/execute-plan.md, get-shit-done/workflows/plan.md</files>
  <action>
For each of these 3 core pipeline workflows, apply the same optimization pattern:

**Step 1: Audit.** Count current lines. Identify:
- v1 cruft: references to phases, milestones, decimal gap-closure, PRD express, Nyquist, auto-advance that may remain as comments or dead branches after Plan 12-01 rewrites
- Unnecessary verbosity: over-specified instructions that repeat what the spawned agent already knows, redundant context comments, excessive inline documentation
- Stale terminology: any remaining "phase", "milestone", "plan-phase", "execute-phase" in comments, banners, or display text

**Step 2: Condense.** For each workflow:
- Remove all v1 cruft identified in audit
- Trim verbose instructions to essential orchestration logic only
- Collapse redundant sections (e.g., if init call + field mapping + error handling is spread across 50 lines but could be 15)
- Remove comments that explain obvious operations
- Keep all orchestration logic: init calls, agent spawning, wave execution, checkpoints, state updates, error handling

**Step 3: Terminology update.** Verify all display text, banner messages, commit messages, and inline references use v2 language:
- "Feature: {cap}/{feat}" not "Phase {N}"
- "feature directory" not "phase directory"
- "FEATURE.md" not "REQUIREMENTS.md"

**Per-workflow specifics:**

**execute.md:**
- After Plan 12-01 rewrites init calls, may still carry verbose v1 comments about phase branching strategy, milestone completion routing, decimal phase logic
- Wave execution and checkpoint logic: preserve entirely, just trim verbosity
- Feature status tracking: ensure it updates FEATURE.md frontmatter, not phase completion

**execute-plan.md:**
- After Plan 12-01 rewrites paths, may still carry verbose path mapping documentation
- Core task execution loop: preserve, just condense
- Summary creation and verification: preserve

**plan.md:**
- After Plan 12-01 removes PRD/Nyquist/gap-closure/auto-advance sections, the file should be significantly shorter
- Self-critique loop and plan-checker spawn: preserve entirely
- Planner context block: should already be v2 from Plan 12-01, verify and trim if over-specified
  </action>
  <verify>
    <automated>for f in get-shit-done/workflows/execute.md get-shit-done/workflows/execute-plan.md get-shit-done/workflows/plan.md; do lines=$(wc -l < "$f"); echo "$f: $lines lines"; done</automated>
    <automated>grep -ci "phase_dir\|phase_number\|plan-phase\|execute-phase\|milestone\|decimal\|Nyquist\|auto.advance\|PRD express" get-shit-done/workflows/execute.md get-shit-done/workflows/execute-plan.md get-shit-done/workflows/plan.md | grep -v ":0$" | wc -l | grep -q "^0$" && echo "PASS: No v1 cruft" || echo "FAIL: v1 cruft remains"</automated>
  </verify>
  <done>execute.md, execute-plan.md, and plan.md condensed. V1 cruft removed. Orchestration logic preserved. All terminology v2.</done>
</task>

<task type="auto">
  <name>Task 2: Optimize progress.md, resume-work.md, review.md, and doc.md</name>
  <files>get-shit-done/workflows/progress.md, get-shit-done/workflows/resume-work.md, get-shit-done/workflows/review.md, get-shit-done/workflows/doc.md</files>
  <action>
Apply the same audit-condense-terminology pattern to these 4 workflows:

**progress.md:**
- Must use `init feature-progress` (v2 route), not `init progress` (v1)
- Display logic: capability/feature tree with focus group progress from ROADMAP.md
- Remove all phase-based routing (Route A/B/C/D/E/F pattern)
- Replace with feature pipeline stage detection (research/plan/execute/review/doc/complete)
- Show focus group status and suggest next actions based on focus group state

**resume-work.md:**
- Must scan `.planning/capabilities/*/features/*/` not `.planning/phases/*`
- Must support multiple active focus groups: ask which to resume if multiple exist
- Route to `/gsd:plan|execute|review <cap/feat>` based on feature pipeline state
- Determine pipeline stage from artifact existence: PLAN without SUMMARY = suggest execute, SUMMARY without review = suggest review
- Reconstruction fallback if STATE.md missing: derive position from capability/feature directory artifacts
- Display focus group name + active feature instead of phase number

**review.md:**
- After Plan 12-02 rewrites init calls, condense any remaining verbose reviewer spawn instructions
- The 4-reviewer parallel dispatch + synthesis pattern: preserve, condense surrounding prose
- Q&A findings loop: preserve logic, trim verbose step descriptions
- Ensure LENS propagation is clean and not over-documented

**doc.md:**
- After Plan 12-02 rewrites init calls, condense any remaining verbose doc-writer instructions
- 3-pass self-validation: preserve logic, trim verbose descriptions
- Section ownership model ([derived]/[authored]): preserve, referenced concisely
- Human checkpoint for doc confirmation: preserve
- Write order (modules before flows): preserve
  </action>
  <verify>
    <automated>for f in get-shit-done/workflows/progress.md get-shit-done/workflows/resume-work.md get-shit-done/workflows/review.md get-shit-done/workflows/doc.md; do lines=$(wc -l < "$f"); echo "$f: $lines lines"; done</automated>
    <automated>grep -ci "\.planning/phases\|init progress\|phase_number\|execute-phase\|Phase \[" get-shit-done/workflows/progress.md get-shit-done/workflows/resume-work.md | grep -v ":0$" | wc -l | grep -q "^0$" && echo "PASS: No v1 refs in progress/resume" || echo "FAIL: v1 refs remain"</automated>
  </verify>
  <done>progress.md shows capability/feature tree with focus groups. resume-work.md scans feature dirs, supports multiple focus groups, routes per pipeline state. review.md and doc.md condensed with orchestration logic preserved. All 4 workflows use v2 terminology throughout.</done>
</task>

</tasks>

<verification>
- All 7 workflows have reduced line counts compared to pre-optimization state
- grep for "phase_dir", "phase_number", "plan-phase", "execute-phase", "milestone" across all 7 files returns zero
- grep for "feature_dir" or "FEATURE_SLUG" in all 7 files returns matches
- grep for "init feature-progress" in progress.md returns match
- grep for ".planning/capabilities" in resume-work.md returns match
- Orchestration logic preserved: agent spawning, wave execution, checkpoints, auto-chains all functional
</verification>

<success_criteria>
7 workflow files are condensed, v1-clean, and optimized for the v2 model. No phase/milestone terminology remains. Orchestration logic fully preserved. progress.md and resume-work.md use feature/capability/focus group model.
</success_criteria>

<output>
After completion, create `.planning/phases/12-workflow-optimization-wiring/12-09-SUMMARY.md`
</output>

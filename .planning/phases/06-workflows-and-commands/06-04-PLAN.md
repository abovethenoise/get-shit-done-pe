---
phase: 06-workflows-and-commands
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/init-project.md
  - commands/gsd/init.md
  - get-shit-done/bin/lib/init.cjs
  - get-shit-done/bin/gsd-tools.cjs
autonomous: true
requirements: [INIT-01, INIT-02]

must_haves:
  truths:
    - "/init auto-detects new vs existing project mode from filesystem evidence"
    - "New-project mode runs deep Q&A producing PROJECT.md + capability map + .documentation/ seed"
    - "Existing-project mode: parallel scan -> user validation (independent sections) -> gap fill"
    - "Incremental writes persist state; re-running detects partial run and offers resume"
  artifacts:
    - path: "get-shit-done/workflows/init-project.md"
      provides: "Init workflow: auto-detect mode, new-project Q&A flow (goals/opinions/constraints/architecture -> capability map), existing-project scan flow (parallel scan -> independent validation -> gap fill), incremental writes with resume"
    - path: "commands/gsd/init.md"
      provides: "Slash command: /gsd:init — single entry point for project setup"
    - path: "get-shit-done/bin/lib/init.cjs"
      provides: "cmdInitProject: auto-detects mode (new/existing/ambiguous), returns mode + project context + partial-run state"
    - path: "get-shit-done/bin/gsd-tools.cjs"
      provides: "Dispatcher case for init project"
  key_links:
    - from: "commands/gsd/init.md"
      to: "get-shit-done/workflows/init-project.md"
      via: "execution_context @-reference"
      pattern: "init-project"
    - from: "get-shit-done/workflows/init-project.md"
      to: "get-shit-done/workflows/gather-synthesize.md"
      via: "Existing-project scan uses gather-synthesize pattern for parallel codebase analysis"
      pattern: "gather-synthesize"
    - from: "get-shit-done/bin/lib/init.cjs"
      to: ".planning/"
      via: "Auto-detection checks .planning/ existence and code presence"
      pattern: "planning.*exists"
---

<objective>
Create the project initialization command (/init) with auto-detection of new vs existing projects, and both initialization flows.

Purpose: /init is the entry point for all new GSD projects. It must handle both greenfield (no existing code) and brownfield (existing codebase) scenarios, producing the project model that all other commands consume.
Output: /init slash command, init-project workflow, init command extension
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-workflows-and-commands/06-CONTEXT.md
@.planning/phases/06-workflows-and-commands/06-RESEARCH.md

<interfaces>
<!-- Existing new-project patterns: -->
<!-- commands/gsd/new-project.md -> existing v1 command (to be replaced by /init new-project flow) -->
<!-- get-shit-done/workflows/new-project.md -> existing v1 new-project workflow -->
<!-- init.cjs cmdInitNewProject -> existing brownfield detection, project context assembly -->

<!-- Auto-detection from CONTEXT.md: -->
<!-- .planning/ exists + code exists = existing -->
<!-- Nothing = new -->
<!-- Ambiguous = ask one question -->

<!-- New-project flow from CONTEXT.md: -->
<!-- Deep Q&A: goals, tech stack opinions, architecture approach, constraints -->
<!-- Output: PROJECT.md + capability map + .documentation/ files -->

<!-- Existing-project flow from CONTEXT.md: -->
<!-- Phase 1: Automated scan (structure, stack, entry points, deps, data models, patterns) -> System Understanding Draft -->
<!-- Phase 2: User validation (independent sections, flag low-confidence, targeted intent questions) -->
<!-- Phase 3: Gap fill (what scan couldn't determine, domain context, known tech debt) -->
<!-- Output: PROJECT.md + capability map + .documentation/ files -->

<!-- Incremental writes from CONTEXT.md: -->
<!-- Each phase writes output as it goes, not all-at-once -->
<!-- State persisted. Re-running /init detects partial run and offers resume -->

<!-- gather-synthesize.md from Phase 2: -->
<!-- Used for existing-project parallel scan (multiple scan agents, one synthesizer) -->

<!-- init resume naming collision resolved: -->
<!-- Pipeline resume uses init resume-pipeline (RESEARCH.md conflict resolution) -->
<!-- Existing init resume is untouched -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <title>Create init-project workflow with auto-detection, new-project Q&A, and existing-project scan flows</title>
  <reqs>INIT-01, INIT-02</reqs>
  <artifact>get-shit-done/workflows/init-project.md</artifact>
  <inputs>
    - 06-CONTEXT.md -> auto-detection logic (filesystem evidence), new-project deep Q&A flow, existing-project 3-phase flow (scan -> validate -> gap fill), incremental writes with resume
    - 06-CONTEXT.md -> documentation structure (.documentation/ with architecture.md, domain.md, mapping.md, capabilities/, decisions/)
    - 06-RESEARCH.md -> brownfield detection reuse from cmdInitNewProject, incremental writes with partial-run detection
    - get-shit-done/workflows/new-project.md -> existing v1 workflow pattern (reference for structure, not content)
    - get-shit-done/workflows/gather-synthesize.md -> reusable for existing-project parallel scan
    - 06-RESEARCH.md -> constraints: no interactive stdin, use AskUserQuestion, stdout=JSON from gsd-tools
  </inputs>
  <done>get-shit-done/workflows/init-project.md exists with: auto-detection step (checks .planning/ + code presence, disambiguates via single question), new-project mode (Q&A about goals/opinions/constraints/architecture -> writes PROJECT.md + capability map + .documentation/ seed), existing-project mode (parallel scan via gather-synthesize -> independent section validation via AskUserQuestion -> gap fill Q&A -> writes same outputs), incremental write steps (each section persisted immediately), partial-run detection (checks which sections exist, offers resume)</done>
</task>

<task type="auto">
  <title>Create /init slash command and cmdInitProject function</title>
  <reqs>INIT-01, INIT-02</reqs>
  <artifact>commands/gsd/init.md, get-shit-done/bin/lib/init.cjs, get-shit-done/bin/gsd-tools.cjs</artifact>
  <inputs>
    - commands/gsd/new-project.md -> existing command pattern (frontmatter + objective + execution_context + context)
    - get-shit-done/bin/lib/init.cjs -> cmdInitNewProject pattern for new function
    - 06-CONTEXT.md -> /init auto-detects mode, single command
    - 06-RESEARCH.md -> init resume-pipeline for pipeline resume (distinct from project init resume)
  </inputs>
  <done>commands/gsd/init.md exists with frontmatter (name: gsd:init, description, allowed-tools including AskUserQuestion and Task), references init-project.md workflow. cmdInitProject function in init.cjs returns: detected_mode (new/existing/ambiguous), planning_exists, code_exists, partial_run state (which sections already written), project context if existing. gsd-tools.cjs dispatcher handles "init project"</done>
</task>

</tasks>

<verification>
- [ ] init-project.md has auto-detection logic (checks .planning/ and code)
- [ ] New-project flow includes Q&A for goals, tech stack opinions, architecture, constraints
- [ ] New-project flow outputs PROJECT.md, capability map, .documentation/ seed
- [ ] Existing-project flow uses gather-synthesize for parallel scan
- [ ] Existing-project validation uses independent sections (confirming stack doesn't block confirming architecture)
- [ ] Existing-project gap fill captures domain context and tech debt
- [ ] Incremental writes: each section persisted immediately, not all-at-once
- [ ] Partial-run detection works: re-running /init detects what exists and offers resume
- [ ] /init slash command references init-project.md workflow
- [ ] cmdInitProject returns correct JSON shape with mode detection
- [ ] No conflict with existing init resume (uses init project, not init resume)
</verification>

<success_criteria>
/init correctly detects project state (new/existing/ambiguous), runs the appropriate initialization flow with incremental writes, and produces PROJECT.md + capability map + .documentation/ for both new and existing projects.
</success_criteria>

<output>
After completion, create `.planning/phases/06-workflows-and-commands/06-04-SUMMARY.md`
</output>

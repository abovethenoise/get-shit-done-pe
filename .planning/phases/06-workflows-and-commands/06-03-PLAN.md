---
phase: 06-workflows-and-commands
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - get-shit-done/workflows/framing-pipeline.md
  - get-shit-done/references/escalation-protocol.md
  - get-shit-done/workflows/framing-discovery.md
autonomous: true
requirements: [WKFL-06, WKFL-07]

must_haves:
  truths:
    - "After discovery, all framings converge to the same pipeline stages: research -> requirements -> plan -> execute -> review -> docs"
    - "Framing context (lens metadata + brief) is injected into each pipeline stage, shaping behavior without changing agent definitions"
    - "Review receives three inputs: requirements (contract), lens metadata (disposition), brief (intent)"
    - "Every pipeline stage can push back upstream via 3-tier escalation (minor/moderate/major)"
    - "Escalation loop terminates after 1 backward reset per run"
  artifacts:
    - path: "get-shit-done/workflows/framing-pipeline.md"
      provides: "Post-discovery pipeline orchestration: research (lens-aware) -> auto-generate requirements (lens-weighted) -> plan -> execute -> review (3 inputs) -> reflect (doc agent). Framing context passed as paths at each stage"
    - path: "get-shit-done/references/escalation-protocol.md"
      provides: "Universal 3-tier escalation reference: minor (flag+continue), moderate (pause+propose), major (halt+recommend upstream return). Per-stage severity examples. Loop termination rules"
    - path: "get-shit-done/workflows/framing-discovery.md"
      provides: "Updated discovery workflow with pipeline handoff step that invokes framing-pipeline.md"
  key_links:
    - from: "get-shit-done/workflows/framing-discovery.md"
      to: "get-shit-done/workflows/framing-pipeline.md"
      via: "After brief is written, discovery hands off to pipeline with brief path + lens metadata"
      pattern: "framing-pipeline"
    - from: "get-shit-done/workflows/framing-pipeline.md"
      to: "get-shit-done/references/escalation-protocol.md"
      via: "execution_context @-reference for escalation rules at every stage"
      pattern: "escalation-protocol"
    - from: "get-shit-done/workflows/framing-pipeline.md"
      to: "get-shit-done/workflows/review-phase.md"
      via: "Review stage invokes review-phase with additional brief + lens metadata context"
      pattern: "review-phase"
---

<objective>
Create the post-discovery pipeline orchestration that wires all framings through the same stages with lens-aware behavior, plus the universal escalation protocol.

Purpose: Discovery produces a brief; this plan builds everything that happens after -- converging all four framings into research -> requirements -> plan -> execute -> review -> docs with framing context shaping each stage's behavior. The escalation protocol ensures any stage can push back upstream safely.
Output: Pipeline workflow, escalation reference doc, updated discovery workflow with handoff
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-workflows-and-commands/06-CONTEXT.md
@.planning/phases/06-workflows-and-commands/06-RESEARCH.md
@.planning/phases/06-workflows-and-commands/06-01-SUMMARY.md
@.planning/phases/06-workflows-and-commands/06-02-SUMMARY.md

<interfaces>
<!-- Pipeline stages from CONTEXT.md: -->
<!-- 1. Research: Phase 2 agents, lens-aware (receive lens metadata, adjust focus) -->
<!-- 2. Requirements: auto-generate 3-layer (EU/FN/TC) from brief, lens-weighted (debug: thin EU rich TC, new: rich EU thin TC) -->
<!-- 3. Plan: Phase 3 planner, risk posture from lens -->
<!-- 4. Execute: Phase 3 executor, aggressiveness from lens -->
<!-- 5. Review: Phase 4 review-phase with 3 inputs (reqs + lens + brief) -->
<!-- 6. Reflect: Phase 5 doc agent as final step -->

<!-- Framing context injection from CONTEXT.md: -->
<!-- Same agents, different question sets / context based on active framing -->
<!-- Lens metadata + brief path passed at workflow level, not embedded in agents -->

<!-- Existing workflow references: -->
<!-- research-phase.md, plan-phase.md, execute-phase.md, review-phase.md, doc-phase.md -->
<!-- These are wired as stages within framing-pipeline.md -->

<!-- Escalation protocol from CONTEXT.md: -->
<!-- Minor: flag it, continue, reflect captures it -->
<!-- Moderate: pause, surface to user, propose amendment -->
<!-- Major: halt, recommend returning to specific stage (propose-and-confirm) -->
<!-- Loop termination: 1 backward reset per run, then hard stop -->

<!-- Review 3-input model from CONTEXT.md: -->
<!-- 1. Requirements (contract) -- did we build what was specified -->
<!-- 2. Lens metadata (disposition) -- are we reviewing this the right way -->
<!-- 3. Brief (intent) -- does this solve the original problem -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <title>Create framing-pipeline workflow orchestrating all 6 post-discovery stages with lens-aware context injection</title>
  <reqs>WKFL-06, WKFL-07</reqs>
  <artifact>get-shit-done/workflows/framing-pipeline.md, get-shit-done/workflows/framing-discovery.md</artifact>
  <inputs>
    - 06-CONTEXT.md -> pipeline convergence: always full pipeline, no stage skipping. Research agents lens-aware. Requirements auto-generated lens-weighted. Plan/execute/review shaped by lens. Reflect = Phase 5 doc agent.
    - 06-CONTEXT.md -> framing context injection: same agents, different question sets. Orchestrator passes paths not content.
    - 06-CONTEXT.md -> review receives 3 inputs: requirements + lens metadata + brief
    - 06-CONTEXT.md -> plan-to-execute handoff carries forward Phase 3 format
    - 06-RESEARCH.md -> gather-synthesize pattern reusable, 4-layer context with Layer 4 as framing injection slot
    - get-shit-done/workflows/research-phase.md -> existing research workflow (invoked as stage 1)
    - get-shit-done/workflows/plan-phase.md -> existing plan workflow (invoked as stage 3)
    - get-shit-done/workflows/execute-phase.md -> existing execute workflow (invoked as stage 4)
    - get-shit-done/workflows/review-phase.md -> existing review workflow (invoked as stage 5, with 3 inputs)
    - get-shit-done/workflows/doc-phase.md -> existing doc workflow (invoked as stage 6, the reflect step)
    - get-shit-done/workflows/framing-discovery.md (from 06-02) -> discovery workflow needs pipeline handoff step added
  </inputs>
  <done>get-shit-done/workflows/framing-pipeline.md exists with 6 sequential stages (research -> requirements -> plan -> execute -> review -> reflect), each receiving framing context (brief path + lens metadata) as input. Each stage references existing workflow via @-ref pattern with framing context additions. Requirements generation step auto-drafts 3-layer reqs from brief with lens-specific weighting. Review stage passes 3 inputs (reqs + lens + brief). framing-discovery.md updated with final step that hands off to framing-pipeline.md with brief path and lens metadata</done>
</task>

<task type="auto">
  <title>Create escalation protocol reference document with per-stage severity rubric</title>
  <reqs>WKFL-06</reqs>
  <artifact>get-shit-done/references/escalation-protocol.md</artifact>
  <inputs>
    - 06-CONTEXT.md -> universal escalation: 3-tier severity at every stage. Minor = flag + continue. Moderate = pause + propose amendment. Major = halt + recommend upstream return (propose-and-confirm, not auto-return).
    - 06-CONTEXT.md -> major issue handling: propose and confirm, user confirms, auto-restarts from that stage
    - 06-RESEARCH.md -> escalation loop termination: global pipeline-run counter, hard stop after 1 backward reset per run
    - 06-RESEARCH.md -> severity threshold calibration: 2-3 concrete examples per tier per stage
  </inputs>
  <done>get-shit-done/references/escalation-protocol.md exists defining: 3-tier model (minor/moderate/major) with mechanism per tier, 2-3 concrete examples per tier per pipeline stage (research, requirements, plan, execute, review), loop termination rules (1 backward reset per run, then hard stop with user restart), propose-and-confirm pattern for major issues</done>
</task>

</tasks>

<verification>
- [ ] framing-pipeline.md has 6 stages in correct order (research -> requirements -> plan -> execute -> review -> reflect)
- [ ] Each stage receives framing context (brief path + lens metadata)
- [ ] Requirements generation step handles lens-specific weighting (debug: thin EU rich TC, new: rich EU thin TC)
- [ ] Review stage explicitly passes 3 inputs (requirements, lens metadata, brief)
- [ ] Reflect stage is wired to Phase 5 doc agent
- [ ] Orchestrator passes paths not content at every stage
- [ ] framing-discovery.md updated with pipeline handoff step
- [ ] escalation-protocol.md has 3-tier model with mechanisms
- [ ] escalation-protocol.md has per-stage examples (2-3 per tier per stage)
- [ ] Loop termination rule: 1 backward reset per run
- [ ] No auto-return for major issues -- propose-and-confirm pattern
</verification>

<success_criteria>
The full pipeline from discovery through documentation is orchestrated: any framing command produces a brief, then the pipeline runs all 6 stages with lens-aware behavior, and any stage can escalate upstream via the documented protocol.
</success_criteria>

<output>
After completion, create `.planning/phases/06-workflows-and-commands/06-03-SUMMARY.md`
</output>

---
phase: 04-review-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/templates/review.md
  - get-shit-done/bin/lib/frontmatter.cjs
  - get-shit-done/bin/lib/init.cjs
  - get-shit-done/bin/gsd-tools.cjs
autonomous: true
requirements: [REVW-01, REVW-02]
must_haves:
  truths:
    - "Review template uses 3-level verdict scale: met / not met / regression (no PASS/PARTIAL/FAIL/BLOCKED)"
    - "Review template references 4 reviewer types: end-user, functional, technical, code quality (not Domain/Code/Integration)"
    - "Review template includes regression subtype labeling (proven vs suspected)"
    - "FRONTMATTER_SCHEMAS has a 'review' entry with required fields"
    - "init review-phase returns reviewer agent paths, synthesizer path, feature/capability paths, and config flags"
    - "gsd-tools.cjs dispatches 'init review-phase' to init.cjs"
  artifacts:
    - path: "get-shit-done/templates/review.md"
      provides: "v2 review template with 3-level verdicts, 4 reviewer types, per-requirement trace structure"
      contains: "met / not met / regression"
    - path: "get-shit-done/bin/lib/frontmatter.cjs"
      provides: "review schema in FRONTMATTER_SCHEMAS"
      contains: "review"
    - path: "get-shit-done/bin/lib/init.cjs"
      provides: "cmdInitReviewPhase function"
      exports: ["cmdInitReviewPhase"]
    - path: "get-shit-done/bin/gsd-tools.cjs"
      provides: "init review-phase dispatch case"
      contains: "review-phase"
  key_links:
    - from: "get-shit-done/bin/gsd-tools.cjs"
      to: "get-shit-done/bin/lib/init.cjs"
      via: "require + dispatch for init review-phase"
      pattern: "cmdInitReviewPhase"
    - from: "get-shit-done/bin/lib/frontmatter.cjs"
      to: "get-shit-done/templates/review.md"
      via: "Schema validates review frontmatter"
      pattern: "review"
---

<objective>
Update review infrastructure: rewrite the review template for v2 verdict scale and reviewer types, add review frontmatter schema, and create `init review-phase` CLI bootstrapping.

Purpose: The review template defines the artifact structure that reviewers write into. The frontmatter schema enables validation. The init command provides the workflow with all paths and config it needs at startup.

Output: Updated `review.md` template, extended `frontmatter.cjs` with review schema, new `cmdInitReviewPhase` in `init.cjs`, dispatch wiring in `gsd-tools.cjs`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-review-layer/04-CONTEXT.md
@.planning/phases/04-review-layer/04-RESEARCH.md

<interfaces>
<!-- Current review template (v1 — to be replaced) -->
Verdict: PASS | PARTIAL | FAIL | BLOCKED
Reviewers: Domain, Code, Integration
Per-REQ trace: Code/Domain/Integration evidence, Gap Analysis, Fix Scope, Blocking Assessment

<!-- Target review template (v2) -->
Verdict per REQ: met / not met / regression
Regression subtypes: proven / suspected
Reviewers: end-user, functional, technical, code quality
Evidence: file:line + quoted code + reasoning
No severity in template (synthesizer assigns)

<!-- Existing frontmatter schemas (frontmatter.cjs) -->
```javascript
const FRONTMATTER_SCHEMAS = {
  plan: { required: ['phase', 'plan', 'type', 'wave', 'depends_on', 'files_modified', 'autonomous', 'must_haves'] },
  summary: { required: ['phase', 'plan', 'subsystem', 'tags', 'duration', 'completed'] },
  verification: { required: ['phase', 'verified', 'status', 'score'] },
};
```

<!-- Existing init pattern (init.cjs) -->
cmdInitPlanPhase: returns models, config flags, phase info, plan inventory, file paths
cmdInitExecutePhase: returns models, config flags, phase info, plans, branch name, file paths
Pattern: read config, find phase, extract req IDs from roadmap, build result object, output(result, raw)

<!-- gsd-tools.cjs dispatch pattern -->
```javascript
case 'init': {
  const workflow = args[1];
  switch (workflow) {
    case 'execute-phase':
      init.cmdInitExecutePhase(cwd, args[2], raw);
      break;
    case 'plan-phase':
      init.cmdInitPlanPhase(cwd, args[2], raw);
      break;
```
</interfaces>
</context>

<tasks>

<task>
  <title>Rewrite review template for v2 verdict scale and reviewer types</title>
  <reqs>REVW-01, REVW-02</reqs>
  <artifact>get-shit-done/templates/review.md</artifact>
  <inputs>
    - 04-CONTEXT.md -> verdict scale (met / not met / regression), regression subtypes (proven/suspected)
    - 04-CONTEXT.md -> reviewer types (end-user, functional, technical, code quality)
    - 04-CONTEXT.md -> evidence format (file:line + quoted code + reasoning)
    - 04-CONTEXT.md -> no severity in reviewer output (synthesizer-assigned)
    - 04-RESEARCH.md -> template gap: v1 uses PASS/PARTIAL/FAIL/BLOCKED + Domain/Code/Integration
    - get-shit-done/templates/review.md -> current v1 template structure
  </inputs>
  <done>review.md template uses 3-level verdict (met/not met/regression). Lists 4 reviewer sections (end-user, functional, technical, code quality). Per-REQ trace includes verdict + evidence (file:line, quoted code, reasoning). Regression findings have proven/suspected label. No severity field in per-REQ trace. Old PASS/PARTIAL/FAIL/BLOCKED and Domain/Code/Integration references removed.</done>
</task>

<task>
  <title>Add review frontmatter schema and init review-phase command</title>
  <reqs>REVW-01, REVW-02</reqs>
  <artifact>get-shit-done/bin/lib/frontmatter.cjs, get-shit-done/bin/lib/init.cjs, get-shit-done/bin/gsd-tools.cjs</artifact>
  <inputs>
    - get-shit-done/bin/lib/frontmatter.cjs -> FRONTMATTER_SCHEMAS object, add 'review' entry
    - get-shit-done/bin/lib/init.cjs -> cmdInitPlanPhase/cmdInitExecutePhase pattern for new cmdInitReviewPhase
    - get-shit-done/bin/gsd-tools.cjs -> init switch case dispatch pattern
    - get-shit-done/bin/lib/core.cjs -> resolveModelFromRole, findPhaseInternal, loadConfig
    - 04-RESEARCH.md -> init review-phase returns: reviewer models, feature/capability paths, review config flags
  </inputs>
  <done>FRONTMATTER_SCHEMAS in frontmatter.cjs has a 'review' key with required fields matching the v2 review template frontmatter. cmdInitReviewPhase exists in init.cjs, returns reviewer agent paths (4 reviewers + synthesizer), phase info, feature/capability paths, and config flags. gsd-tools.cjs has 'review-phase' case under init switch dispatching to cmdInitReviewPhase. cmdInitReviewPhase is exported from init.cjs module.exports.</done>
</task>

</tasks>

<verification>
1. review.md template verdict scale is met/not met/regression — no PASS/PARTIAL/FAIL/BLOCKED anywhere
2. review.md lists 4 reviewer sections: end-user, functional, technical, code quality — no Domain/Code/Integration
3. review.md per-REQ trace includes evidence format (file:line, quoted code, reasoning)
4. review.md regression findings have proven/suspected label field
5. FRONTMATTER_SCHEMAS.review exists in frontmatter.cjs
6. cmdInitReviewPhase exists in init.cjs and is exported
7. gsd-tools.cjs init switch has 'review-phase' case
8. cmdInitReviewPhase returns agent paths, phase info, and config flags
</verification>

<success_criteria>
- Review template reflects v2 reviewer types and verdict scale (REVW-01, REVW-02)
- Infrastructure supports review workflow bootstrapping via init command
- Frontmatter validation available for REVIEW.md files
</success_criteria>

<output>
After completion, create `.planning/phases/04-review-layer/04-02-SUMMARY.md`
</output>

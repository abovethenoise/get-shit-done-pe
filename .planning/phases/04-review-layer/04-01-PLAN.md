---
phase: 04-review-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-review-enduser.md
  - agents/gsd-review-functional.md
  - agents/gsd-review-technical.md
  - agents/gsd-review-quality.md
  - agents/gsd-review-synthesizer.md
autonomous: true
requirements: [REVW-01, REVW-02, REVW-03, REVW-04, REVW-05, REVW-06, REVW-07]
must_haves:
  truths:
    - "4 reviewer agents exist: gsd-review-enduser, gsd-review-functional, gsd-review-technical, gsd-review-quality"
    - "1 synthesizer agent exists: gsd-review-synthesizer"
    - "All agents use v2 skeleton: YAML frontmatter (name, description, tools, role_type: judge, reads, writes) + Role/Goal/Success Criteria/Scope/Output Format sections"
    - "Reviewer agents report verdicts only (met / not met / regression) — no severity assignment"
    - "Every finding cites file:line + quoted code + reasoning — findings without evidence are not actionable"
    - "Synthesizer assigns severity (blocker / major / minor) after seeing all 4 reports"
    - "Synthesizer spot-checks by reading cited file:line references — cannot spawn sub-agents"
    - "Synthesizer has mandatory conflicts section including disagreements and tensions"
    - "Reviewer agents are ~1500 tokens max; synthesizer ~2000 tokens max"
    - "Framing injection slot exists in each reviewer but is not populated"
  artifacts:
    - path: "agents/gsd-review-enduser.md"
      provides: "End-user reviewer — traces against EU-xx story + acceptance criteria"
      contains: "met / not met / regression"
    - path: "agents/gsd-review-functional.md"
      provides: "Functional reviewer — traces against FN-xx behavior specs"
      contains: "met / not met / regression"
    - path: "agents/gsd-review-technical.md"
      provides: "Technical reviewer — traces against TC-xx implementation specs"
      contains: "spec-vs-reality"
    - path: "agents/gsd-review-quality.md"
      provides: "Code quality reviewer — DRY/KISS/bloat/dependency scrutiny"
      contains: "prove this complexity is necessary"
    - path: "agents/gsd-review-synthesizer.md"
      provides: "Review synthesizer — consolidates 4 reports, assigns severity, resolves conflicts"
      contains: "blocker / major / minor"
  key_links:
    - from: "agents/gsd-review-enduser.md"
      to: "agents/gsd-research-domain.md"
      via: "Shared v2 agent skeleton pattern"
      pattern: "role_type: judge"
    - from: "agents/gsd-review-synthesizer.md"
      to: "agents/gsd-research-synthesizer.md"
      via: "Shared synthesizer pattern (heavier version)"
      pattern: "role_type: judge"
---

<objective>
Create 5 agent definitions for the review layer: 4 specialist reviewers (end-user, functional, technical, code quality) and 1 synthesizer.

Purpose: Each reviewer produces evidence-gated trace reports against its requirement layer. The synthesizer consolidates, assigns severity, spot-checks, and resolves conflicts. Together they implement the parallel review pattern that feeds user-facing recommendations.

Output: 5 agent definition files in `agents/` directory following the v2 skeleton established by research agents.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-review-layer/04-CONTEXT.md
@.planning/phases/04-review-layer/04-RESEARCH.md

<interfaces>
<!-- v2 agent skeleton (from research agents) -->
```yaml
---
name: gsd-review-{role}
description: {one line}
tools: Read, Write, Bash, Grep, Glob
role_type: judge
reads: [{context layers}]
writes: [review-trace-report]
---

## Role
## Goal
## Success Criteria
## Scope
## Tool Guidance
## Citation Requirement
## Output Format
```

<!-- Verdict scale (CONTEXT.md — locked) -->
3-level: met / not met / regression
- Regression subtypes: proven (test fails, removed function) and suspected (code analysis)
- No "partially met" — too subjective

<!-- Evidence requirement (CONTEXT.md — locked) -->
Every finding cites file:line, quotes relevant code/behavior, explains reasoning
Findings without evidence are not actionable

<!-- Severity (CONTEXT.md — locked) -->
Reviewers do NOT assign severity — verdicts and findings only
Synthesizer assigns severity after seeing all 4 reports

<!-- Priority ordering (CONTEXT.md — locked) -->
user > functional > technical > quality

<!-- Two-phase verification prompting (RESEARCH.md) -->
1. Internalize requirement layer
2. Trace code against it
Separation improves accuracy from 52% to 85%

<!-- Framing injection slot -->
Reviewer agents work without framing context (default path)
Include documented slot where Phase 6 injects framing-specific question sets
Do NOT populate the slot in Phase 4
</interfaces>
</context>

<tasks>

<task>
  <title>Create 4 specialist reviewer agent definitions</title>
  <reqs>REVW-01, REVW-02, REVW-03, REVW-04, REVW-05, REVW-06</reqs>
  <artifact>agents/gsd-review-enduser.md, agents/gsd-review-functional.md, agents/gsd-review-technical.md, agents/gsd-review-quality.md</artifact>
  <inputs>
    - 04-CONTEXT.md -> reviewer postures (end-user: literal/unforgiving; functional: behavior contract enforcer; technical: spec compliance + feasibility; quality: guilty until proven innocent)
    - 04-CONTEXT.md -> verdict scale (met / not met / regression), evidence requirements, no severity assignment
    - 04-CONTEXT.md -> framing-aware review (prompts injected by workflow, NOT baked into agents)
    - 04-CONTEXT.md -> agent sizing (~1500 tokens, quality up to ~1700)
    - 04-RESEARCH.md -> two-phase verification prompting (internalize then trace)
    - 04-RESEARCH.md -> reviewer layer scoping (primary own layer, secondary cross-layer observations)
    - agents/gsd-research-domain.md -> v2 agent skeleton reference (frontmatter + 7 sections)
  </inputs>
  <done>4 agent files exist in agents/ directory. Each has YAML frontmatter with role_type: judge. Each has Role/Goal/Success Criteria/Scope/Tool Guidance/Citation Requirement/Output Format sections. Output Format enforces two-phase prompting (internalize, then trace). Verdict scale is 3-level (met/not met/regression). No severity assignment in any reviewer. Each contains a framing injection slot comment. End-user ~1500 tokens, functional ~1500 tokens, technical ~1500 tokens, quality <=1700 tokens.</done>
</task>

<task>
  <title>Create review synthesizer agent definition</title>
  <reqs>REVW-07, REVW-02</reqs>
  <artifact>agents/gsd-review-synthesizer.md</artifact>
  <inputs>
    - 04-CONTEXT.md -> severity assignment (blocker/major/minor by synthesizer only)
    - 04-CONTEXT.md -> conflict resolution (judgment first, priority ordering as tiebreaker: user > functional > technical > quality)
    - 04-CONTEXT.md -> spot-checking (read cited file:line references before presenting)
    - 04-CONTEXT.md -> overlapping findings presented separately, mandatory conflicts section
    - 04-RESEARCH.md -> synthesizer ~2000 token budget, cannot spawn sub-agents
    - 04-RESEARCH.md -> spot-check selectively (prioritize not-met/regression, sample 3-5 citations per reviewer)
    - agents/gsd-research-synthesizer.md -> synthesizer pattern reference (heavier version for review)
  </inputs>
  <done>agents/gsd-review-synthesizer.md exists. YAML frontmatter has role_type: judge. Contains Role/Goal/Success Criteria/Scope/Tool Guidance/Citation Requirement/Output Format sections. Output Format includes: severity assignment, mandatory conflicts section (disagreements + tensions), per-finding evidence verification, priority ordering. Synthesizer reads files directly for spot-checking (Read/Grep tools). Under 2000 tokens.</done>
</task>

</tasks>

<verification>
1. All 5 agent files exist in agents/ directory
2. All use role_type: judge in frontmatter
3. All follow v2 skeleton (7 sections)
4. Reviewers use 3-level verdict scale (met / not met / regression) — no "partially met"
5. No reviewer assigns severity
6. Every reviewer's Output Format enforces evidence citation (file:line + quoted code + reasoning)
7. Every reviewer's Output Format has two phases (internalize requirements, then trace)
8. Synthesizer assigns severity (blocker / major / minor)
9. Synthesizer has mandatory conflicts section
10. Synthesizer uses Read/Grep for spot-checking, not sub-agent spawning
11. Each reviewer has a framing injection slot (documented but empty)
12. Token budgets: reviewers ~1500 (quality <=1700), synthesizer ~2000
</verification>

<success_criteria>
- 4 parallel specialist reviewers exist with distinct postures and layer focus (REVW-01)
- Each produces structured trace reports with per-requirement verdicts (REVW-02)
- End-user traces against story + acceptance (REVW-03)
- Functional traces against behavior specs (REVW-04)
- Technical traces against implementation specs (REVW-05)
- Code quality traces for DRY/KISS/complexity/bloat/dependencies (REVW-06)
- Synthesizer consolidates, assigns severity, resolves conflicts with priority ordering (REVW-07)
</success_criteria>

<output>
After completion, create `.planning/phases/04-review-layer/04-01-SUMMARY.md`
</output>

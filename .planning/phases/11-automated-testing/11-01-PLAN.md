---
phase: 11-automated-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gsd/*.md
  - get-shit-done/workflows/*.md
  - get-shit-done/templates/*.md
  - get-shit-done/references/*.md
  - agents/*.md
  - .planning/phases/11-automated-testing/11-01-SUMMARY.md
autonomous: true
requirements:
  - CMD-03

must_haves:
  truths:
    - "Every @file reference in the active toolchain resolves to an existing file"
    - "Every command file references an existing workflow"
    - "No artifact references a deleted workflow, agent, or template"
    - "Obvious renames are auto-fixed in place"
  artifacts:
    - path: ".planning/phases/11-automated-testing/ref-scan-results.md"
      provides: "Full @file reference scan report"
    - path: ".planning/phases/11-automated-testing/cross-ref-audit.md"
      provides: "Cross-reference audit results"
  key_links:
    - from: "commands/gsd/*.md"
      to: "get-shit-done/workflows/*.md"
      via: "@file reference"
      pattern: "@~/.claude/get-shit-done/workflows/"
    - from: "get-shit-done/workflows/*.md"
      to: "agents/*.md"
      via: "@file reference"
      pattern: "@~/.claude/agents/"
---

<objective>
Scan every @file reference across the entire active toolchain and verify each resolves to an existing source file. Separately audit cross-references (command->workflow->agent->template chains) for dangling pointers. Auto-fix obvious renames; log non-obvious breaks for the friction log.

Purpose: Static analysis catches broken references before runtime testing. If @file refs are broken, CLI smoke tests and E2E are meaningless.
Output: ref-scan-results.md, cross-ref-audit.md, and auto-fixed @file references in source files.
</objective>

<execution_context>
@/Users/philliphall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philliphall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-automated-testing/11-CONTEXT.md
@.planning/phases/11-automated-testing/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scan all @file references and auto-fix obvious renames</name>
  <files>
    commands/gsd/*.md
    get-shit-done/workflows/*.md
    get-shit-done/templates/*.md
    get-shit-done/references/*.md
    agents/*.md
    .planning/phases/11-automated-testing/ref-scan-results.md
  </files>
  <action>
Write a bash script (disposable, in /tmp/) that:

1. Finds every `@~/.claude/<path>` reference across these directories:
   - `commands/gsd/`
   - `get-shit-done/workflows/`
   - `get-shit-done/templates/`
   - `get-shit-done/references/`
   - `agents/`

2. For each reference found:
   - Strip the `@~/.claude/` prefix
   - Check if the resulting path exists relative to the repo root
   - Record: source_file, line_number, reference, resolved (yes/no)

3. For unresolved references, check if an obvious rename exists:
   - `plan-phase.md` -> `plan.md`
   - `research-phase.md` -> `research-workflow.md`
   - `verify-phase.md` -> deleted (no replacement)
   - Any other file where the basename matches but directory differs
   If an obvious rename is found, fix the reference IN THE SOURCE FILE and log the fix.

4. Write results to `.planning/phases/11-automated-testing/ref-scan-results.md` with:
   - Total refs scanned
   - Resolved count
   - Unresolved count
   - Auto-fixed count
   - Table of all unresolved refs (after auto-fixes)
   - Table of all auto-fixes applied

Per CONTEXT: "Scan everything -- no exceptions for code blocks or examples. If an @file ref exists, it must resolve."
Per CONTEXT: Source paths only -- install path (~/.claude/) validation is Phase 12's concern.

Delete the script after it runs successfully.
  </action>
  <verify>
    <automated>grep -c "UNRESOLVED" .planning/phases/11-automated-testing/ref-scan-results.md; echo "Report exists: $(test -f .planning/phases/11-automated-testing/ref-scan-results.md && echo yes || echo no)"</automated>
  </verify>
  <done>ref-scan-results.md exists with full scan report. Every auto-fixable rename has been applied to source files. Remaining unresolved refs (if any) are logged for friction log discussion.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-reference audit -- command-to-workflow-to-agent chain integrity</name>
  <files>
    .planning/phases/11-automated-testing/cross-ref-audit.md
  </files>
  <action>
Audit the full artifact chain for dangling references:

1. **Command -> Workflow mapping:** For each of the 9 command files in `commands/gsd/`, extract the workflow it invokes. Verify that workflow file exists in `get-shit-done/workflows/`.

2. **Workflow -> Agent mapping:** For each of the 14 workflow files, extract any agent references. Verify each agent file exists in `agents/`.

3. **Workflow -> Template mapping:** For each workflow, extract template references. Verify each template exists in `get-shit-done/templates/`.

4. **Workflow -> Reference mapping:** For each workflow, extract reference document refs. Verify each exists in `get-shit-done/references/`.

5. **Dead artifact detection:** Check for references to known-deleted artifacts:
   - `discuss-phase` (v1 concept -- CONTEXT flags this explicitly)
   - `verify-work`, `research-phase`, `transition`
   - `gsd-codebase-mapper` (deleted agent)
   - Any workflow/agent/template that was removed in phases 8-10

6. **Command count reconciliation:** CMD-01 lists 11 commands (init, debug, new, enhance, refactor, discuss-capability, discuss-feature, status, resume, plan, review). Only 9 command files exist. Document which are missing and whether they are workflow-internal stages or genuinely missing.

Write results to `.planning/phases/11-automated-testing/cross-ref-audit.md` with:
- Chain integrity table (command -> workflow -> agents/templates/references)
- Any broken links found
- Dead artifact references found (with severity: blocker/friction/cosmetic)
- Command count reconciliation findings

Known pre-findings from research:
- `research.md` template line 21 references `/gsd:discuss-phase` (dead)
- `init.md` references `/gsd:new-project` in after-text (no such command)
  </action>
  <verify>
    <automated>test -f .planning/phases/11-automated-testing/cross-ref-audit.md && echo "Audit complete" || echo "MISSING"</automated>
  </verify>
  <done>cross-ref-audit.md exists with full chain integrity report, dead artifact findings, and command count reconciliation. All findings tagged with severity levels.</done>
</task>

</tasks>

<verification>
- ref-scan-results.md contains scan of all ~57 @file references
- cross-ref-audit.md contains chain audit for all 9 commands and 14 workflows
- Auto-fixes applied to source files where applicable
- No disposable scripts remain in the repo
</verification>

<success_criteria>
Every @file reference has been scanned and categorized as resolved, auto-fixed, or flagged for discussion. The full command->workflow->agent chain has been audited for integrity. Both reports exist with severity-tagged findings.
</success_criteria>

<output>
After completion, create `.planning/phases/11-automated-testing/11-01-SUMMARY.md`
</output>

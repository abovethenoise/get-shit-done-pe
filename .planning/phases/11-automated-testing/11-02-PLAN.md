---
phase: 11-automated-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11-automated-testing/cli-smoke-results.md
  - .planning/phases/11-automated-testing/11-02-SUMMARY.md
autonomous: true
requirements:
  - CMD-03

must_haves:
  truths:
    - "Every CLI init route returns exit code 0 and valid JSON when given proper fixtures"
    - "Dead routes return informative error messages (not crashes)"
    - "New-project mode works on an empty directory"
    - "Existing-project mode works on a directory with .planning/ scaffolding"
  artifacts:
    - path: ".planning/phases/11-automated-testing/cli-smoke-results.md"
      provides: "CLI route smoke test results"
  key_links:
    - from: "gsd-tools.cjs"
      to: "lib/*.cjs"
      via: "CLI route dispatch"
      pattern: "init (project|framing-discovery|discuss-capability|discuss-feature|resume|progress|plan-feature|execute-feature|feature-op|feature-progress)"
---

<objective>
Smoke test every surviving CLI init route by running `node gsd-tools.cjs init <route>` against synthetic fixture directories. Verify exit code 0, valid JSON output, and expected output shape. Also verify dead routes return error messages gracefully.

Purpose: Proves the CLI tool layer works before the E2E simulation attempts to use it. Catches crashes, missing handlers, and malformed output.
Output: cli-smoke-results.md with per-route pass/fail and output samples.
</objective>

<execution_context>
@/Users/philliphall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philliphall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-automated-testing/11-CONTEXT.md
@.planning/phases/11-automated-testing/11-RESEARCH.md
@gsd-tools.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build synthetic fixture directories and run CLI smoke tests</name>
  <files>
    .planning/phases/11-automated-testing/cli-smoke-results.md
  </files>
  <action>
Create TWO temp directories for testing:

**Directory A: Empty project** (`/tmp/gsd-test-empty-*`)
- Empty directory, no .planning/
- For testing `init project` (new-project mode)

**Directory B: Existing project** (`/tmp/gsd-test-existing-*`)
- Structure per research fixtures:
```
.planning/
  STATE.md          (frontmatter: gsd_state_version: 1.0, milestone: test, milestone_name: test, status: planning)
  REQUIREMENTS.md   (minimal valid markdown)
  ROADMAP.md        (minimal valid markdown with phase headers)
  capabilities/
    test-cap/
      CAPABILITY.md (minimal frontmatter: name, status)
      features/
        test-feat/
          FEATURE.md (minimal frontmatter: name, status)
config.json         ({"commit_docs": false, "research": false})
```

Write a bash script (disposable, in /tmp/) that runs each CLI route and captures:
- Exit code
- stdout (first 500 chars)
- Whether stdout is valid JSON (where expected)
- Duration

**Routes to test against Directory B (existing project):**

| # | Route | Expected |
|---|-------|----------|
| 1 | `init project --cwd=<dirB>` | JSON with project mode detection |
| 2 | `init framing-discovery debug --cwd=<dirB>` | JSON with framing context |
| 3 | `init framing-discovery new --cwd=<dirB>` | JSON with framing context |
| 4 | `init framing-discovery enhance --cwd=<dirB>` | JSON with framing context |
| 5 | `init framing-discovery refactor --cwd=<dirB>` | JSON with framing context |
| 6 | `init discuss-capability --cwd=<dirB>` | JSON with capability context |
| 7 | `init discuss-feature --cwd=<dirB>` | JSON with feature context |
| 8 | `init resume --cwd=<dirB>` | JSON with resume context |
| 9 | `init progress --cwd=<dirB>` | JSON with progress data |
| 10 | `init plan-feature --cwd=<dirB>` | JSON with planning context |
| 11 | `init execute-feature --cwd=<dirB>` | JSON with execution context |
| 12 | `init feature-op --cwd=<dirB>` | JSON output |
| 13 | `init feature-progress --cwd=<dirB>` | JSON output |

**Routes to test against Directory A (empty project):**

| # | Route | Expected |
|---|-------|----------|
| 14 | `init project --cwd=<dirA>` | JSON with new-project mode |

**Dead routes to verify graceful error:**

| # | Route | Expected |
|---|-------|----------|
| 15 | `init phase-op --cwd=<dirB>` | Error message (not crash) |
| 16 | `init review-phase --cwd=<dirB>` | Error message (not crash) |
| 17 | `init doc-phase --cwd=<dirB>` | Error message (not crash) |

**Also test key atomic routes:**

| # | Route | Expected |
|---|-------|----------|
| 18 | `state load --cwd=<dirB>` | Valid JSON of STATE.md |
| 19 | `progress --cwd=<dirB>` | Progress output |
| 20 | `capability-list --cwd=<dirB>` | List output |
| 21 | `feature-list --cwd=<dirB>` | List output |

For each route, record: route, exit_code, json_valid, output_sample, duration_ms, verdict (PASS/FAIL/WARN).

Write results to `.planning/phases/11-automated-testing/cli-smoke-results.md` with:
- Summary: total routes tested, pass count, fail count, warn count
- Per-route results table
- Output samples for any failures
- Findings tagged with severity (blocker/friction/cosmetic)

Clean up: delete temp directories and the test script after completion.
  </action>
  <verify>
    <automated>test -f .planning/phases/11-automated-testing/cli-smoke-results.md && grep -c "PASS\|FAIL\|WARN" .planning/phases/11-automated-testing/cli-smoke-results.md</automated>
  </verify>
  <done>cli-smoke-results.md exists with results for all 21+ routes. Every surviving init route has a PASS/FAIL/WARN verdict. Temp directories and test script are cleaned up.</done>
</task>

</tasks>

<verification>
- cli-smoke-results.md covers all 13 live init routes, 3 dead routes, and 5+ atomic routes
- No temp directories or disposable scripts remain
- Every route has a clear verdict
</verification>

<success_criteria>
Every surviving CLI route has been smoke tested and categorized. Dead routes confirmed to return error messages (not crashes). Results documented with severity-tagged findings ready for friction log consolidation.
</success_criteria>

<output>
After completion, create `.planning/phases/11-automated-testing/11-02-SUMMARY.md`
</output>

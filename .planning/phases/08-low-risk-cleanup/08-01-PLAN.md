---
phase: 08-low-risk-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gsd/add-phase.md
  - commands/gsd/add-tests.md
  - commands/gsd/audit-milestone.md
  - commands/gsd/cleanup.md
  - commands/gsd/complete-milestone.md
  - commands/gsd/insert-phase.md
  - commands/gsd/join-discord.md
  - commands/gsd/list-phase-assumptions.md
  - commands/gsd/new-project.md
  - commands/gsd/plan-milestone-gaps.md
  - commands/gsd/reapply-patches.md
  - commands/gsd/remove-phase.md
  - commands/gsd/set-profile.md
  - commands/gsd/settings.md
  - commands/gsd/update.md
autonomous: true
requirements:
  - CMD-02

must_haves:
  truths:
    - The 15 dead commands no longer exist in commands/gsd/
    - The 11 v2 commands (init, debug, new, enhance, refactor, discuss-capability, discuss-feature, status, resume-work, plan-phase, review-phase) still exist and are untouched
    - No surviving file in the project references any of the 15 deleted command filenames via @file or slash-command path
  artifacts:
    - commands/gsd/ contains exactly the surviving commands (not the 15 deleted ones)
  key_links:
    - Smoke scan grep finds zero references to deleted command names in the surviving project tree
---

<objective>
Delete the 15 verified-dead commands from commands/gsd/. These are phase-management utilities, milestone lifecycle commands, and v1 features that have no place in the 11-command v2 surface.

Purpose: Remove dead weight before Phase 9 wiring. Every file deleted is a file Phase 10 and Phase 11 won't need to audit.
Output: 15 files removed from commands/gsd/, one atomic git commit.
</objective>

<execution_context>
@/Users/philliphall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philliphall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-low-risk-cleanup/08-RESEARCH.md

Kill list source: 08-RESEARCH.md "Commands to DELETE (15 files)" table.
Keep list source: 08-RESEARCH.md "Commands FLAGGED for Phase 10" — these are NOT touched here.

SCOPE BOUNDARY: Delete from get-shit-done-pe/ project source tree only. The installed tree (~/.claude/) is rebuilt by Phase 12 install.js. Do not touch ~/.claude/.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dependency trace then delete 15 dead commands</name>
  <files>
    commands/gsd/add-phase.md
    commands/gsd/add-tests.md
    commands/gsd/audit-milestone.md
    commands/gsd/cleanup.md
    commands/gsd/complete-milestone.md
    commands/gsd/insert-phase.md
    commands/gsd/join-discord.md
    commands/gsd/list-phase-assumptions.md
    commands/gsd/new-project.md
    commands/gsd/plan-milestone-gaps.md
    commands/gsd/reapply-patches.md
    commands/gsd/remove-phase.md
    commands/gsd/set-profile.md
    commands/gsd/settings.md
    commands/gsd/update.md
  </files>
  <action>
For each of the 15 files below, grep the project source tree for references before deleting:

```bash
# Grep pattern — run once per file or combined:
grep -r "add-phase\|add-tests\|audit-milestone\|cleanup\|complete-milestone\|insert-phase\|join-discord\|list-phase-assumptions\|new-project\|plan-milestone-gaps\|reapply-patches\|remove-phase\|set-profile\|settings\|update" \
  /Users/philliphall/get-shit-done-pe/commands/ \
  /Users/philliphall/get-shit-done-pe/get-shit-done/ \
  /Users/philliphall/get-shit-done-pe/agents/ \
  --include="*.md" --include="*.js" --include="*.cjs" -l
```

Interpret results:
- If a hit is IN one of the 15 files being deleted: ignore (self-reference, deleting anyway)
- If a hit is IN a surviving file via @file reference: STOP — present the conflict to the user with the exact file and line before proceeding
- If no hits in surviving files: proceed with deletion

Delete the 15 files:
```bash
cd /Users/philliphall/get-shit-done-pe
rm commands/gsd/add-phase.md \
   commands/gsd/add-tests.md \
   commands/gsd/audit-milestone.md \
   commands/gsd/cleanup.md \
   commands/gsd/complete-milestone.md \
   commands/gsd/insert-phase.md \
   commands/gsd/join-discord.md \
   commands/gsd/list-phase-assumptions.md \
   commands/gsd/new-project.md \
   commands/gsd/plan-milestone-gaps.md \
   commands/gsd/reapply-patches.md \
   commands/gsd/remove-phase.md \
   commands/gsd/set-profile.md \
   commands/gsd/settings.md \
   commands/gsd/update.md
```

Commit atomically (commands category only):
```bash
cd /Users/philliphall/get-shit-done-pe
git add -A commands/gsd/
git commit -m "chore(cleanup): remove 15 dead commands (CMD-02)

Deleted phase-management utilities, milestone lifecycle commands,
and v1-only features not present in the 11-command v2 surface.

Deleted:
- add-phase, add-tests, audit-milestone, cleanup, complete-milestone
- insert-phase, join-discord, list-phase-assumptions, new-project
- plan-milestone-gaps, reapply-patches, remove-phase
- set-profile, settings, update"
```
  </action>
  <verify>
```bash
# 1. Verify all 15 files are gone
ls /Users/philliphall/get-shit-done-pe/commands/gsd/ | grep -E "^(add-phase|add-tests|audit-milestone|cleanup|complete-milestone|insert-phase|join-discord|list-phase-assumptions|new-project|plan-milestone-gaps|reapply-patches|remove-phase|set-profile|settings|update)\.md$"
# Expected: no output (files don't exist)

# 2. Verify the 11 v2 commands still exist
ls /Users/philliphall/get-shit-done-pe/commands/gsd/ | grep -E "^(init|debug|new|enhance|refactor|discuss-capability|discuss-feature|status|resume-work|plan-phase|review-phase)\.md$"
# Expected: all 11 listed (some may have slightly different names — verify against actual filenames)

# 3. Smoke scan: surviving files referencing deleted command names
grep -r "add-phase\|audit-milestone\|complete-milestone\|insert-phase\|join-discord\|list-phase-assumptions\|plan-milestone-gaps\|reapply-patches\|remove-phase\|set-profile" \
  /Users/philliphall/get-shit-done-pe/commands/ \
  /Users/philliphall/get-shit-done-pe/get-shit-done/ \
  /Users/philliphall/get-shit-done-pe/agents/ \
  --include="*.md" --include="*.js" --include="*.cjs"
# Expected: no output (no surviving references)
```
  </verify>
  <done>
15 files deleted from commands/gsd/. Smoke scan returns zero hits in surviving files. Commit exists with message "chore(cleanup): remove 15 dead commands (CMD-02)". The 11 v2 commands are untouched.
  </done>
</task>

</tasks>

<verification>
Run the smoke scan from the verify block. If any surviving file references a deleted command name, report the specific file and line — do not proceed until confirmed with user.
</verification>

<success_criteria>
- commands/gsd/ contains no file from the 15-file kill list
- All 11 v2 commands present in commands/gsd/
- Grep of project source finds zero live references to deleted command filenames
- One atomic git commit with message referencing CMD-02
</success_criteria>

<output>
After completion, create `.planning/phases/08-low-risk-cleanup/08-01-SUMMARY.md`
</output>

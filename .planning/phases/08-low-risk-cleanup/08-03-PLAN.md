---
phase: 08-low-risk-cleanup
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - agents/gsd-codebase-mapper.md
  - agents/gsd-debugger.md
  - agents/gsd-integration-checker.md
  - agents/gsd-project-researcher.md
  - agents/gsd-research-synthesizer.md
  - agents/gsd-roadmapper.md
autonomous: true
requirements:
  - CLN-02

must_haves:
  truths:
    - The 6 orphaned agents no longer exist in agents/
    - The 17 surviving agents remain untouched
    - No surviving command, workflow, or agent references any of the 6 deleted agent filenames via @file or agent spawn reference
  artifacts:
    - agents/ contains only the 17 keep-listed agents
  key_links:
    - Smoke scan finds zero references to deleted agent names in surviving project files
---

<objective>
Delete the 6 verified-orphaned agents from agents/. Each was audited: its only callers are commands and workflows already deleted in Plans 01 and 02. None are in any surviving v2 reference chain.

Purpose: Complete the deletion of the dead command→workflow→agent dependency chain. What Plan 01 started at the top (commands), Plan 03 finishes at the bottom (agents).
Output: 6 files removed from agents/, one atomic git commit.
</objective>

<execution_context>
@/Users/philliphall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philliphall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-low-risk-cleanup/08-RESEARCH.md

Kill list source: 08-RESEARCH.md "Agents to DELETE (6 files)" table.
Keep list source: 08-RESEARCH.md "Agents to KEEP" table — 17 agents, do NOT touch.

Critical note from research: gsd-debugger.md is NOT the agent used by the /debug framing command. /debug uses framing-discovery.md, not gsd-debugger.md. The only caller of gsd-debugger.md was verify-work → diagnose-issues, both now deleted.

SCOPE BOUNDARY: Delete from get-shit-done-pe/agents/ only. Do not touch ~/.claude/agents/.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dependency trace then delete 6 orphaned agents</name>
  <files>
    agents/gsd-codebase-mapper.md
    agents/gsd-debugger.md
    agents/gsd-integration-checker.md
    agents/gsd-project-researcher.md
    agents/gsd-research-synthesizer.md
    agents/gsd-roadmapper.md
  </files>
  <action>
Grep surviving files for references to each agent being deleted:

```bash
grep -r "gsd-codebase-mapper\|gsd-debugger\|gsd-integration-checker\|gsd-project-researcher\|gsd-research-synthesizer\|gsd-roadmapper" \
  /Users/philliphall/get-shit-done-pe/commands/ \
  /Users/philliphall/get-shit-done-pe/get-shit-done/ \
  /Users/philliphall/get-shit-done-pe/agents/ \
  --include="*.md" --include="*.js" --include="*.cjs"
```

Interpret results:
- Hits inside one of the 6 files being deleted: ignore (self-reference)
- Hits in a SURVIVING file: STOP — present exact file + line to user. The research identified all callers were already deleted, but verify this holds post-Plan-01/02.

Confirm gsd-debugger.md is not referenced by any framing or surviving workflow (critical: do not confuse with /debug command):
```bash
grep -r "gsd-debugger" /Users/philliphall/get-shit-done-pe/get-shit-done/ --include="*.md"
# Expected: no output (framing-discovery.md does NOT reference gsd-debugger.md)
```

Delete 6 files:
```bash
cd /Users/philliphall/get-shit-done-pe
rm agents/gsd-codebase-mapper.md \
   agents/gsd-debugger.md \
   agents/gsd-integration-checker.md \
   agents/gsd-project-researcher.md \
   agents/gsd-research-synthesizer.md \
   agents/gsd-roadmapper.md
```

Commit atomically (agents category only):
```bash
cd /Users/philliphall/get-shit-done-pe
git add -A agents/
git commit -m "chore(cleanup): remove 6 orphaned agents (CLN-02)

Deleted agents whose only callers (commands and workflows) were
already removed in the previous two commits.

Deleted:
- gsd-codebase-mapper (caller: map-codebase, deleted)
- gsd-debugger (caller: verify-work→diagnose-issues chain, deleted)
- gsd-integration-checker (caller: audit-milestone, deleted)
- gsd-project-researcher (callers: new-project/new-milestone, deleted)
- gsd-research-synthesizer (callers: new-project/new-milestone, deleted)
- gsd-roadmapper (callers: new-project/new-milestone, deleted)"
```
  </action>
  <verify>
```bash
# 1. Verify 6 files are gone
ls /Users/philliphall/get-shit-done-pe/agents/ | grep -E "^(gsd-codebase-mapper|gsd-debugger|gsd-integration-checker|gsd-project-researcher|gsd-research-synthesizer|gsd-roadmapper)\.md$"
# Expected: no output

# 2. Verify 17 surviving agents present (spot check key ones)
ls /Users/philliphall/get-shit-done-pe/agents/ | grep -E "^(gsd-planner|gsd-executor|gsd-verifier|gsd-phase-researcher|gsd-plan-checker|gsd-doc-writer|gsd-review-synthesizer)\.md$"
# Expected: all 7 listed (confirms surviving agents untouched)

# 3. Smoke scan for deleted agent references
grep -r "gsd-codebase-mapper\|gsd-debugger\|gsd-integration-checker\|gsd-project-researcher\|gsd-research-synthesizer\|gsd-roadmapper" \
  /Users/philliphall/get-shit-done-pe/commands/ \
  /Users/philliphall/get-shit-done-pe/get-shit-done/ \
  /Users/philliphall/get-shit-done-pe/agents/ \
  --include="*.md"
# Expected: no output
```
  </verify>
  <done>
6 files deleted from agents/. 17 surviving agents untouched. Smoke scan returns zero hits. One atomic git commit referencing CLN-02.
  </done>
</task>

</tasks>

<verification>
Smoke scan of entire project source for deleted agent names returns zero results. Confirm /debug framing (framing-discovery.md) is intact and does NOT reference gsd-debugger.md — it should use the framing pipeline, not the old debugger agent.
</verification>

<success_criteria>
- agents/ contains none of the 6 kill-listed agent files
- All 17 keep-listed agents present (17 = 11 pipeline agents + 6 unlinked v2 gatherers awaiting Phase 9 wiring)
- Smoke scan of project finds zero references to deleted agent names
- One atomic git commit referencing CLN-02
</success_criteria>

<output>
After completion, create `.planning/phases/08-low-risk-cleanup/08-03-SUMMARY.md`
</output>

---
phase: 10-remaining-cleanup-polish
plan: 05
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - get-shit-done/bin/gsd-tools.cjs
  - get-shit-done/bin/lib/init.cjs
  - get-shit-done/bin/lib/core.cjs
  - get-shit-done/references/model-profiles.md
  - get-shit-done/references/model-profile-resolution.md
  - get-shit-done/workflows/progress.md
  - get-shit-done/workflows/resume-work.md
autonomous: true
requirements: [CLN-03, CLN-05, INTG-03]

must_haves:
  truths:
    - "gsd-tools.cjs has no init plan-phase, init review-phase, init doc-phase, or init phase-op routes"
    - "init.cjs has no cmdInitPlanPhase, cmdInitReviewPhase, cmdInitDocPhase, cmdInitPhaseOp functions"
    - "core.cjs MODEL_PROFILES has no gsd-codebase-mapper entry"
    - "core.cjs ROLE_MODEL_MAP has executor, judge, and quick roles"
    - "model-profiles.md uses v2 role-based mapping, no deleted agent references"
    - "progress.md routes to v2 commands (no /gsd:plan-phase or /gsd:execute-phase refs)"
    - "resume-work.md routes to v2 commands (no /gsd:plan-phase or /gsd:execute-phase refs)"
  artifacts: []
  key_links:
    - "progress.md → v2 framing commands"
    - "resume-work.md → v2 framing commands"
    - "model-profiles.md → core.cjs ROLE_MODEL_MAP"
---

<objective>
Delete dead phase CLI init routes, update model profiles to v2 standard, and fix routing in progress.md and resume-work.md.

Purpose: Three related cleanup tasks that depend on phase commands being deleted (plan 10-01): (1) the CLI init routes that only served deleted commands become dead, (2) model profiles need updating to v2 4-tier standard, (3) progress.md and resume-work.md still route users to deleted phase commands.
Output: Dead init routes removed, model profiles modernized, routing workflows updated.
</objective>

<execution_context>
@/Users/philliphall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philliphall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-remaining-cleanup-polish/10-CONTEXT.md
@.planning/phases/10-remaining-cleanup-polish/research/cli-dead-code-findings.md
@.planning/phases/10-remaining-cleanup-polish/research/reference-doc-audit-findings.md
@.planning/phases/10-remaining-cleanup-polish/research/phase-command-cascade-findings.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead phase init routes and update model profiles</name>
  <files>
    get-shit-done/bin/gsd-tools.cjs
    get-shit-done/bin/lib/init.cjs
    get-shit-done/bin/lib/core.cjs
    get-shit-done/references/model-profiles.md
    get-shit-done/references/model-profile-resolution.md
  </files>
  <action>
**A. Delete dead init routes from gsd-tools.cjs and init.cjs:**

Remove these 4 init routes from gsd-tools.cjs:
- `init plan-phase` (only caller was plan-phase.md command, now deleted)
- `init review-phase` (only caller was review-phase.md command, now deleted)
- `init doc-phase` (only caller was doc-phase.md command, now deleted)
- `init phase-op` (only callers were verify-phase.md + research-phase.md, both deleted)

Remove corresponding functions from init.cjs:
- cmdInitPlanPhase
- cmdInitReviewPhase
- cmdInitDocPhase
- cmdInitPhaseOp

KEEP `init execute-phase` and `cmdInitExecutePhase` — still called by execute.md (renamed) and execute-plan.md. Consider renaming route to `init execute` for consistency but NOT required now (functional, not cosmetic).

**B. Update core.cjs model profiles:**

In MODEL_PROFILES table, remove entry for `gsd-codebase-mapper` (agent deleted in Phase 8). Also remove entries for: `gsd-roadmapper`, `gsd-debugger`, `gsd-integration-checker` (all deleted agents). Keep only `gsd-planner` and `gsd-executor` as v1 fallbacks. Add deprecation comment above the table.

In ROLE_MODEL_MAP, add `quick: 'haiku'` to match v2 standard:
```javascript
const ROLE_MODEL_MAP = {
  executor: 'sonnet',
  judge: 'inherit',
  quick: 'haiku',
};
```

**C. Update model-profiles.md:**

Replace the v1 per-agent profile table with v2 role-based mapping table (4-tier: Main/Orchestrator=Opus, Research/Execution=Sonnet, Judge/Review=Opus inherit, Quick=Haiku). Remove references to deleted agents (gsd-roadmapper, gsd-codebase-mapper, gsd-debugger, gsd-integration-checker). Update "v2 Agent Assignments" table to drop phase references (e.g., "Planner (Phase 3)" → "Planner"). Remove or update the "Coexistence" section — v1 table removal is now safe. Remove stale "Phase 7 cleanup removes v1 table" claim.

**D. Update model-profile-resolution.md:**

Replace v1 bash-grep resolution pattern description with v2 `resolveModelFromRole()` flow. Update the lookup description: v2 agents use `role_type` frontmatter, mapped to model via ROLE_MODEL_MAP. Keep the `inherit` note (still accurate).
  </action>
  <verify>
    <automated>! grep -q "init plan-phase\|init review-phase\|init doc-phase\|init phase-op" get-shit-done/bin/gsd-tools.cjs && ! grep -q "gsd-codebase-mapper\|gsd-roadmapper\|gsd-debugger\|gsd-integration-checker" get-shit-done/bin/lib/core.cjs && grep -q "quick.*haiku" get-shit-done/bin/lib/core.cjs && echo "PASS"</automated>
  </verify>
  <done>Dead init routes removed. core.cjs model profiles updated with haiku tier and deleted agents removed. model-profiles.md and model-profile-resolution.md updated to v2 standard.</done>
</task>

<task type="auto">
  <name>Task 2: Update progress.md and resume-work.md routing to v2 commands</name>
  <files>
    get-shit-done/workflows/progress.md
    get-shit-done/workflows/resume-work.md
  </files>
  <action>
**progress.md:** Replace all routing references to deleted phase commands:
- `/gsd:execute-phase {phase}` → describe using v2 framing commands (`/gsd:new`, `/gsd:enhance`, `/gsd:refactor`, `/gsd:debug`) which route through framing-pipeline
- `/gsd:plan-phase` → same v2 command routing
- `/gsd:review-phase` → same v2 command routing
- Any text that says "run plan-phase" or "run execute-phase" → update to v2 language

The key routing logic: In v2, users enter via framing commands. The framing-pipeline orchestrates the plan→execute→review→doc sequence. There are no standalone phase commands to route to.

**resume-work.md:** Same treatment:
- `/gsd:execute-phase {phase}` → v2 framing commands
- `/gsd:plan-phase` → v2 framing commands
- All phase command references → v2 capability/feature command routing

For both files, also update any language that references "phases" as execution units. Use "feature" or "capability" language where appropriate. Keep references to `.planning/phases/` directory paths if they exist (that's filesystem structure, not conceptual).
  </action>
  <verify>
    <automated>! grep -q "gsd:plan-phase\|gsd:execute-phase\|gsd:review-phase\|gsd:doc-phase\|gsd:research-phase" get-shit-done/workflows/progress.md get-shit-done/workflows/resume-work.md && echo "PASS"</automated>
  </verify>
  <done>progress.md and resume-work.md route to v2 framing commands, no references to deleted phase commands</done>
</task>

</tasks>

<verification>
- `node -c get-shit-done/bin/gsd-tools.cjs` passes
- `grep "plan-phase\|review-phase\|doc-phase\|phase-op" get-shit-done/bin/gsd-tools.cjs` returns only `execute-phase` (kept)
- `grep "gsd:plan-phase\|gsd:execute-phase" get-shit-done/workflows/progress.md` returns 0
- model-profiles.md contains role-based table with 4 tiers
</verification>

<success_criteria>
Dead init routes removed. Model profiles reflect v2 4-tier standard with no deleted agents. progress.md and resume-work.md route to v2 framing commands.
</success_criteria>

<output>
After completion, create `.planning/phases/10-remaining-cleanup-polish/10-05-SUMMARY.md`
</output>

---
phase: 10-remaining-cleanup-polish
plan: 05
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - get-shit-done/references/model-profiles.md
  - get-shit-done/references/model-profile-resolution.md
  - get-shit-done/references/continuation-format.md
autonomous: true
requirements: [CLN-05]

must_haves:
  truths:
    - "model-profiles.md reflects v2 4-tier standard (Opus/Sonnet/Opus-inherit/Haiku)"
    - "model-profiles.md has no references to deleted agents"
    - "model-profile-resolution.md describes v2 resolveModelFromRole pattern"
    - "continuation-format.md uses v2 capability/feature examples"
    - "All 10 surviving reference docs are accurate for v2"
  artifacts:
    - path: "get-shit-done/references/model-profiles.md"
      provides: "v2 model assignment reference with 4-tier standard"
    - path: "get-shit-done/references/model-profile-resolution.md"
      provides: "v2 model resolution flow documentation"
    - path: "get-shit-done/references/continuation-format.md"
      provides: "Session continuation format with v2 examples"
  key_links:
    - from: "get-shit-done/workflows/research-workflow.md"
      to: "get-shit-done/references/model-profiles.md"
      via: "@file reference"
      pattern: "model-profiles"
    - from: "get-shit-done/workflows/plan-phase.md"
      to: "get-shit-done/references/model-profile-resolution.md"
      via: "@file reference"
      pattern: "model-profile-resolution"
    - from: "get-shit-done/workflows/resume-work.md"
      to: "get-shit-done/references/continuation-format.md"
      via: "@file reference"
      pattern: "continuation-format"
---

<objective>
Update the 3 reference docs that need v2 alignment: model-profiles.md, model-profile-resolution.md, and continuation-format.md.

Purpose: CLN-05 requires every reference doc to be accurate for v2. These 3 need content updates (the other 7 keepers are already accurate).
Output: All 10 surviving reference docs are v2-accurate.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-remaining-cleanup-polish/10-CONTEXT.md
@.planning/phases/10-remaining-cleanup-polish/research/reference-doc-audit-findings.md
@.planning/phases/10-remaining-cleanup-polish/research/cli-dead-code-findings.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite model-profiles.md to v2 standard</name>
  <files>
    get-shit-done/references/model-profiles.md
  </files>
  <action>
    Rewrite model-profiles.md following the before/after spec in reference-doc-audit-findings.md Section 4a.

    **Replace the v1 MODEL_PROFILES table (10 agents x 3 profiles) with v2 role-based mapping:**

    ```
    ## Model Assignment

    v2 uses role-based resolution. Agents declare `role_type` in YAML frontmatter.

    | Role | Model | Rationale |
    |------|-------|-----------|
    | **Main / Orchestrator** | Opus | Architecture decisions, goal decomposition, user interaction |
    | **Research / Execution** | Sonnet | Follows explicit instructions, gathers information, implements plans |
    | **Judge / Review** | Opus (inherit) | Validates, synthesizes, reviews — needs full reasoning power |
    | **Quick tasks** | Haiku | Read-only exploration, structured output extraction |
    ```

    **Replace v2 Agent Assignments table — drop phase references:**

    ```
    | Agent | Role Type | Model |
    | 6x research gatherers | executor | Sonnet |
    | Research synthesizer | judge | Opus (inherit) |
    | Planner | executor | Sonnet |
    | Plan validator | judge | Opus (inherit) |
    | 4x specialist reviewers | judge | Opus (inherit) |
    | Review synthesizer | judge | Opus (inherit) |
    | Documentation writer | executor | Sonnet |
    ```

    **Remove these sections entirely:**
    - "Profile Philosophy" (quality/balanced/budget) — v2 doesn't use tiered profiles
    - "Per-Agent Overrides" — v2 uses role_type, not per-agent config
    - "Switching Profiles" — no longer applies
    - "Design Rationale" per-agent Q&A — replace with brief role-based rationale

    **Update Coexistence section:**
    Replace "v1 MODEL_PROFILES table remains... Phase 7 cleanup removes v1 table" with:
    "All agents use v2 `role_type` frontmatter for model resolution. The v1 per-agent lookup table in core.cjs is deprecated and will be removed."

    **Keep these sections:**
    - "Claude Code Constraint" about `inherit` vs `opus` — still accurate
    - "Resolution Priority" for `resolveModelFromRole()` — still accurate

    **Remove deleted agents from ANY remaining tables:**
    - gsd-roadmapper, gsd-codebase-mapper, gsd-debugger, gsd-integration-checker
    - NOTE: gsd-verifier is STILL ACTIVE (spawned by execute-phase.md) — do NOT remove it
  </action>
  <verify>
    <automated>cd get-shit-done && grep -c "gsd-codebase-mapper\|gsd-roadmapper\|gsd-debugger\|gsd-integration-checker\|Phase 7 cleanup" references/model-profiles.md | grep "^0$" && grep -c "Quick tasks\|Haiku\|role_type\|gsd-verifier" references/model-profiles.md</automated>
  </verify>
  <done>
    model-profiles.md rewritten to v2 standard. 4-tier model (Opus/Sonnet/Opus-inherit/Haiku) documented. No deleted agents referenced. v1 profile philosophy sections removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update model-profile-resolution.md and continuation-format.md</name>
  <files>
    get-shit-done/references/model-profile-resolution.md
    get-shit-done/references/continuation-format.md
  </files>
  <action>
    **model-profile-resolution.md:**
    - Replace v1 bash-grep resolution pattern with v2 `resolveModelFromRole()` flow description
    - The v2 resolution chain: read agent frontmatter -> check `role_type` field -> map via ROLE_MODEL_MAP -> fallback to MODEL_PROFILES if no role_type
    - Update the lookup description: v2 agents use `role_type` frontmatter, not per-agent table lookup
    - Keep the pass-through note about `"inherit"` meaning Opus (still accurate)
    - Update "Usage" section to describe v2 flow
    - Remove references to deleted agents
    - Keep this tightly coupled with model-profiles.md (both are read by plan-phase.md and research-workflow.md)

    **continuation-format.md:**
    - All examples use v1 phase language: `/gsd:execute-phase 2`, `/gsd:plan-phase 3`, "Phase 2: Authentication", milestone-level completions
    - Update examples to use v2 capability/feature language:
      - `/gsd:execute-phase 2` -> keep (execute-phase still exists with phase numbers)
      - `/gsd:plan-phase 3` -> keep (plan-phase still exists)
      - `/gsd:research-phase 2` at lines 102, 131, 152 -> REMOVE (research-phase deleted in Plan 10-02)
      - "Phase 2: Authentication" -> "Feature: Authentication" or similar
      - Milestone-level completions -> capability-level completions
    - Update "Pulling Context" section: reference capability/feature structure instead of ROADMAP.md phases exclusively
    - Keep the core `## Next Up` format structure (still valid and consumed by resume-work.md)
  </action>
  <verify>
    <automated>cd get-shit-done && grep -c "bash.*grep\|gsd-codebase-mapper\|gsd-roadmapper" references/model-profile-resolution.md | grep "^0$" && echo "---" && grep -c "research-phase" references/continuation-format.md | grep "^0$" && echo "Both files clean"</automated>
  </verify>
  <done>
    model-profile-resolution.md describes v2 resolveModelFromRole flow. continuation-format.md uses v2 examples with no references to deleted commands. All 10 surviving reference docs are now v2-accurate.
  </done>
</task>

</tasks>

<verification>
- `ls get-shit-done/references/` should show exactly 10 files (after Plan 10-02 deletes 5)
- `grep -r "gsd-codebase-mapper\|gsd-roadmapper\|gsd-debugger\|gsd-integration-checker\|gsd-verifier\|research-phase" get-shit-done/references/` should return zero results
- model-profiles.md should contain "Haiku" and "role_type"
</verification>

<success_criteria>
All 3 reference docs updated to v2. Combined with Plan 10-02 (5 deletions), every surviving reference doc is v2-accurate. CLN-05 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/10-remaining-cleanup-polish/10-05-SUMMARY.md`
</output>

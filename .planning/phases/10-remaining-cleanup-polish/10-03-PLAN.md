---
phase: 10-remaining-cleanup-polish
plan: 03
type: execute
wave: 2
depends_on: [10-02]
files_modified:
  - get-shit-done/templates/context.md
  - get-shit-done/templates/phase-prompt.md
  - get-shit-done/templates/summary.md
  - get-shit-done/templates/summary-complex.md
  - get-shit-done/templates/summary-minimal.md
  - get-shit-done/templates/summary-standard.md
  - get-shit-done/templates/research.md
  - get-shit-done/templates/planner-subagent-prompt.md
autonomous: true
requirements: [CLN-04]

must_haves:
  truths:
    - "All 8 high-priority templates use v2 capability/feature language instead of v1 phase language"
    - "phase-prompt.md (most consumed template) is carefully updated without breaking plan generation"
    - "All 4 summary variants are synchronized in structure"
    - "No template references deleted files or deleted agents"
  artifacts:
    - path: "get-shit-done/templates/context.md"
      provides: "Feature context template (was phase context)"
    - path: "get-shit-done/templates/phase-prompt.md"
      provides: "Plan template updated for v2 language"
    - path: "get-shit-done/templates/summary.md"
      provides: "Plan summary template updated for v2"
    - path: "get-shit-done/templates/research.md"
      provides: "Research template updated for v2"
  key_links:
    - from: "get-shit-done/workflows/plan-phase.md"
      to: "get-shit-done/templates/phase-prompt.md"
      via: "defines plan output format"
      pattern: "phase-prompt"
    - from: "get-shit-done/workflows/execute-plan.md"
      to: "get-shit-done/templates/summary.md"
      via: "defines summary output format"
      pattern: "summary"
---

<objective>
Update the 8 highest-priority templates from v1 phase language to v2 capability/feature language.

Purpose: CLN-04 requires every template to serve v2 artifact types. These 8 templates are actively consumed by workflows and contain heavy phase language that must be modernized.
Output: Templates using capability/feature terminology consistently.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-remaining-cleanup-polish/10-CONTEXT.md
@.planning/phases/10-remaining-cleanup-polish/research/template-audit-findings.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update context.md, research.md, and planner-subagent-prompt.md to v2 language</name>
  <files>
    get-shit-done/templates/context.md
    get-shit-done/templates/research.md
    get-shit-done/templates/planner-subagent-prompt.md
  </files>
  <action>
    **context.md:**
    - Title: "Phase Context Template" -> "Feature Context Template"
    - All "phase" -> "feature" or "capability/feature" where contextually appropriate
    - Path patterns: `.planning/phases/XX-name/` -> `.planning/capabilities/{capability}/{feature}/`
    - Consumer refs: `gsd-phase-researcher` -> strip or replace with "research gatherer agents"
    - Examples: Rewrite without "Phase 3: Post Feed" framing — use capability/feature examples instead
    - Keep the overall structure (domain, decisions, specifics, deferred sections)

    **research.md:**
    - Title: "Phase [X]: [Name] - Research" -> "Feature Research" or just "Research"
    - All phase-scoped framing -> feature-scoped
    - Path patterns: update to v2 capability/feature paths
    - Remove any references to `gsd-phase-researcher` agent — research is now done via research-workflow.md with 6 gatherer agents

    **planner-subagent-prompt.md:**
    - Replace `{phase_number}` placeholders -> `{feature}` or make generic
    - Update file path patterns from phase paths to capability/feature paths
    - Update any agent references to reflect v2 agent names
    - Keep the core prompt structure (it defines how subagent planners receive context)
  </action>
  <verify>
    <automated>cd get-shit-done && grep -c "Phase Context Template\|gsd-phase-researcher\|\.planning/phases/" templates/context.md templates/research.md templates/planner-subagent-prompt.md 2>/dev/null | grep -v ":0$" || echo "Zero v1 phase language remains in updated templates"</automated>
  </verify>
  <done>
    context.md, research.md, and planner-subagent-prompt.md all use v2 capability/feature language. Zero references to phase paths, phase-researcher, or phase context remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update phase-prompt.md and all 4 summary templates to v2 language</name>
  <files>
    get-shit-done/templates/phase-prompt.md
    get-shit-done/templates/summary.md
    get-shit-done/templates/summary-complex.md
    get-shit-done/templates/summary-minimal.md
    get-shit-done/templates/summary-standard.md
  </files>
  <action>
    **phase-prompt.md (HIGHEST RISK — most consumed template):**
    - This is the PLAN.md template that gsd-planner produces. It IS the plan format.
    - Keep `phase:` frontmatter field (still used for plan grouping in v2 — phases still exist as execution units)
    - Replace conceptual language: where "phase" means "unit of work within a capability/feature", keep or use neutral language. Where "phase" means "v1 phase model", replace with feature language.
    - Update path examples from `.planning/phases/XX-name/` to `.planning/capabilities/{cap}/{feature}/` where they appear as example output paths
    - Remove line 556 reference to verify-phase.md (deleted in Plan 10-02)
    - Update any remaining agent references to v2 agent names
    - CRITICAL: Do not change the XML task structure (`<task>`, `<files>`, `<action>`, `<verify>`, `<done>`) — this is consumed by execute-plan.md parser
    - The `<output>` section path pattern for SUMMARY.md: update to reflect v2 output path convention

    **summary.md + 3 variants (synchronized updates):**
    - Replace "Phase [X]: [Name] Summary" -> "Plan Summary" in title/header
    - Keep `phase:` frontmatter field (still used for grouping)
    - Replace "Next Phase Readiness" -> "Next Steps" section
    - Update path patterns throughout
    - All 4 variants must have synchronized structure — same sections, same field names, just different detail levels (minimal/standard/complex)
    - summary.md is the "full" variant; summary-standard.md, summary-complex.md, summary-minimal.md are the complexity-selected variants used by template.cjs cmdTemplateFill
  </action>
  <verify>
    <automated>cd get-shit-done && echo "--- phase-prompt.md v1 remnants ---" && grep -cn "verify-phase\|Phase Context\|gsd-phase-researcher" templates/phase-prompt.md && echo "--- summary v1 remnants ---" && grep -cn "Next Phase Readiness" templates/summary.md templates/summary-complex.md templates/summary-minimal.md templates/summary-standard.md 2>/dev/null</automated>
  </verify>
  <done>
    phase-prompt.md updated with v2 language, verify-phase reference removed, XML task structure preserved. All 4 summary variants synchronized with v2 language. Phase frontmatter field retained for plan grouping.
  </done>
</task>

</tasks>

<verification>
- All 8 templates should have zero occurrences of: "Phase Context Template", "gsd-phase-researcher", "Next Phase Readiness", "verify-phase"
- phase-prompt.md should still contain: `<task>`, `<files>`, `<action>`, `<verify>`, `<done>` XML tags (format preserved)
- summary.md and variants should still contain `phase:` in frontmatter (field retained)
</verification>

<success_criteria>
8 highest-priority templates updated to v2 language while preserving their structural contracts with consuming workflows. Phase-prompt.md XML format intact.
</success_criteria>

<output>
After completion, create `.planning/phases/10-remaining-cleanup-polish/10-03-SUMMARY.md`
</output>

---
phase: 10-remaining-cleanup-polish
plan: 04
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - get-shit-done/bin/gsd-tools.cjs
  - get-shit-done/bin/lib/commands.cjs
  - get-shit-done/bin/lib/init.cjs
  - get-shit-done/bin/lib/state.cjs
  - get-shit-done/bin/lib/milestone.cjs
  - get-shit-done/bin/lib/verify.cjs
  - get-shit-done/bin/lib/template.cjs
  - get-shit-done/bin/lib/frontmatter.cjs
  - get-shit-done/bin/lib/phase.cjs
autonomous: true
requirements: [CLN-03]

must_haves:
  truths:
    - "gsd-tools.cjs has no routes for: state resolve-blocker, resolve-model, verify-summary, template select, frontmatter set/merge/validate, verify plan-structure/phase-completeness/references/commits, generate-slug, current-timestamp, verify-path-exists, config-ensure-section, history-digest, phase add/insert/remove, milestone complete, validate consistency, scaffold, init new-project/new-milestone/quick/verify-work/milestone-op/map-codebase, websearch"
    - "init.cjs has no cmdInitNewProject, cmdInitNewMilestone, cmdInitQuick, cmdInitVerifyWork, cmdInitMilestoneOp, cmdInitMapCodebase functions"
    - "milestone.cjs has no cmdMilestoneComplete function (cmdRequirementsMarkComplete stays)"
    - "state.cjs has no cmdStateResolveBlocker function"
  artifacts: []
  key_links:
    - "gsd-tools.cjs routes → only surviving lib functions"
---

<objective>
Remove all dead CLI routes and their handler functions from gsd-tools.cjs and lib/*.cjs modules.

Purpose: 29 CLI routes and their backing functions have zero v2 callers. Removing them makes gsd-tools.cjs lean and removes v1-only concepts (milestone CLI, phase lifecycle commands, dead verify/frontmatter/template subcommands).
Output: ~29 dead routes removed from router, ~15 dead handler functions removed from lib modules.
</objective>

<execution_context>
@/Users/philliphall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philliphall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-remaining-cleanup-polish/10-CONTEXT.md
@.planning/phases/10-remaining-cleanup-polish/research/cli-dead-code-findings.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead routes from gsd-tools.cjs router</name>
  <files>get-shit-done/bin/gsd-tools.cjs</files>
  <action>
Remove these 29 dead route cases from the switch router in gsd-tools.cjs:

1. `state resolve-blocker`
2. `resolve-model`
3. `verify-summary`
4. `template select`
5. `frontmatter set`
6. `frontmatter merge`
7. `frontmatter validate`
8. `verify plan-structure`
9. `verify phase-completeness`
10. `verify references`
11. `verify commits`
12. `generate-slug`
13. `current-timestamp`
14. `verify-path-exists`
15. `config-ensure-section`
16. `history-digest`
17. `phase add`
18. `phase insert`
19. `phase remove`
20. `phase next-decimal`
21. `milestone complete`
22. `validate consistency`
23. `scaffold`
24. `init new-project`
25. `init new-milestone`
26. `init quick`
27. `init verify-work`
28. `init milestone-op`
29. `init map-codebase`
30. `websearch`

**KEEP these routes** (have live v2 callers):
- All `state` routes except `resolve-blocker`
- `find-phase`, `phase complete`, `phase-plan-index`, `phases list`
- `commit`, `template fill`, `frontmatter get`
- `verify artifacts`, `verify key-links`
- `config-get`, `config-set`
- `roadmap get-phase`, `roadmap analyze`, `roadmap update-plan-progress`
- `requirements mark-complete`
- `progress`, `state-snapshot`, `summary-extract`, `plan-validate`
- All `init` routes except the 6 being deleted
- All `capability-*` and `feature-*` routes
- All v2 flagged routes (init plan-feature, init execute-feature, etc.)

Also remove any `require()` statements that become unused after route removal. Do NOT remove the require for milestone.cjs (it still exports cmdRequirementsMarkComplete which is live).
  </action>
  <verify>
    <automated>! grep -q "'resolve-model'\|'verify-summary'\|'template select'\|'frontmatter set'\|'frontmatter merge'\|'frontmatter validate'\|'verify plan-structure'\|'verify phase-completeness'\|'verify references'\|'verify commits'\|'generate-slug'\|'current-timestamp'" get-shit-done/bin/gsd-tools.cjs && echo "PASS"</automated>
  </verify>
  <done>All 29+ dead routes removed from gsd-tools.cjs router. No live routes affected.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead handler functions from lib modules</name>
  <files>
    get-shit-done/bin/lib/commands.cjs
    get-shit-done/bin/lib/init.cjs
    get-shit-done/bin/lib/state.cjs
    get-shit-done/bin/lib/milestone.cjs
    get-shit-done/bin/lib/verify.cjs
    get-shit-done/bin/lib/template.cjs
    get-shit-done/bin/lib/frontmatter.cjs
    get-shit-done/bin/lib/phase.cjs
  </files>
  <action>
Remove dead handler functions from each lib module. Be surgical — remove only the specific functions, not the entire files.

**commands.cjs** — Remove:
- cmdGenerateSlug
- cmdCurrentTimestamp
- cmdVerifyPathExists
- cmdHistoryDigest
- cmdWebsearch
- cmdScaffold
- cmdResolveModel
Remove their module.exports entries too. KEEP: cmdCommit, cmdSummaryExtract, cmdProgressRender.

**init.cjs** — Remove:
- cmdInitNewProject
- cmdInitNewMilestone
- cmdInitQuick
- cmdInitVerifyWork
- cmdInitMilestoneOp
- cmdInitMapCodebase
Remove their module.exports entries. KEEP all other init functions.

**state.cjs** — Remove:
- cmdStateResolveBlocker
Remove its module.exports entry. KEEP all other state functions.

**milestone.cjs** — Remove:
- cmdMilestoneComplete
Remove its module.exports entry. KEEP cmdRequirementsMarkComplete and any shared helpers it uses.

**verify.cjs** — Remove:
- cmdVerifySummary
- cmdVerifyPlanStructure
- cmdVerifyPhaseCompleteness
- cmdVerifyReferences
- cmdVerifyCommits
- cmdValidateConsistency
KEEP: cmdVerifyArtifacts, cmdVerifyKeyLinks, and all internal helper functions they depend on.

**template.cjs** — Remove:
- cmdTemplateSelect
KEEP: cmdTemplateFill and all template parsing internals.

**frontmatter.cjs** — Remove:
- cmdFrontmatterSet
- cmdFrontmatterMerge
- cmdFrontmatterValidate
KEEP: cmdFrontmatterGet and ALL internal parsing functions (extractFrontmatter, reconstructFrontmatter etc.) — these are used extensively by state.cjs, commands.cjs, and other modules.

**phase.cjs** — Remove:
- cmdPhaseAdd
- cmdPhaseInsert
- cmdPhaseRemove
- cmdPhaseNextDecimal
KEEP: cmdFindPhase, cmdPhaseComplete, cmdPhasePlanIndex, cmdPhasesList.

For each file, after removing functions, also clean up any helper functions that were ONLY used by the removed functions (check with grep across the same file). Do NOT remove helpers that are shared with surviving functions.
  </action>
  <verify>
    <automated>! grep -q "cmdInitNewProject\|cmdInitNewMilestone\|cmdInitQuick\|cmdMilestoneComplete\|cmdStateResolveBlocker\|cmdVerifySummary\|cmdTemplateSelect\|cmdPhaseAdd\|cmdPhaseInsert\|cmdPhaseRemove" get-shit-done/bin/lib/init.cjs get-shit-done/bin/lib/milestone.cjs get-shit-done/bin/lib/state.cjs get-shit-done/bin/lib/verify.cjs get-shit-done/bin/lib/template.cjs get-shit-done/bin/lib/phase.cjs 2>/dev/null && echo "PASS"</automated>
  </verify>
  <done>All dead handler functions removed from lib modules. Surviving functions and shared helpers intact.</done>
</task>

</tasks>

<verification>
- `node get-shit-done/bin/gsd-tools.cjs state load 2>&1` runs without require/syntax errors
- `node get-shit-done/bin/gsd-tools.cjs commit --help 2>&1` runs without errors
- No grep hits for removed function names in their respective files
- `node -c get-shit-done/bin/gsd-tools.cjs` syntax check passes
</verification>

<success_criteria>
gsd-tools.cjs router contains only live routes. All lib modules contain only live handler functions. No syntax errors introduced. require() graph is clean.
</success_criteria>

<output>
After completion, create `.planning/phases/10-remaining-cleanup-polish/10-04-SUMMARY.md`
</output>

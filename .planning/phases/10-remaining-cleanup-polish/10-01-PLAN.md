---
phase: 10-remaining-cleanup-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/gsd-tools.cjs
  - get-shit-done/bin/lib/init.cjs
  - get-shit-done/bin/lib/state.cjs
  - get-shit-done/bin/lib/commands.cjs
  - get-shit-done/bin/lib/verify.cjs
  - get-shit-done/bin/lib/template.cjs
  - get-shit-done/bin/lib/frontmatter.cjs
  - get-shit-done/bin/lib/phase.cjs
  - get-shit-done/bin/lib/milestone.cjs
autonomous: true
requirements: [CLN-03]

must_haves:
  truths:
    - "All 29 dead CLI routes are removed from gsd-tools.cjs switch router"
    - "All dead handler functions are removed from their respective lib modules"
    - "All live routes and functions still work (no accidental deletions)"
    - "milestone.cjs still exports cmdRequirementsMarkComplete"
    - "frontmatter.cjs still exports core parsing functions (extractFrontmatter, reconstructFrontmatter)"
  artifacts:
    - path: "get-shit-done/bin/gsd-tools.cjs"
      provides: "CLI router with dead routes removed"
    - path: "get-shit-done/bin/lib/init.cjs"
      provides: "Init module with 6 dead functions removed, 4 v2 flagged functions kept"
    - path: "get-shit-done/bin/lib/commands.cjs"
      provides: "Commands module with dead utility functions removed"
    - path: "get-shit-done/bin/lib/verify.cjs"
      provides: "Verify module with dead subcommands removed, artifacts+key-links kept"
    - path: "get-shit-done/bin/lib/phase.cjs"
      provides: "Phase module with add/insert/remove/next-decimal removed, complete/find/list kept"
  key_links:
    - from: "get-shit-done/bin/gsd-tools.cjs"
      to: "get-shit-done/bin/lib/milestone.cjs"
      via: "require and route dispatch"
      pattern: "cmdRequirementsMarkComplete"
    - from: "get-shit-done/bin/gsd-tools.cjs"
      to: "get-shit-done/bin/lib/frontmatter.cjs"
      via: "require and route dispatch"
      pattern: "cmdFrontmatterGet"
---

<objective>
Remove all 29 dead CLI routes from gsd-tools.cjs and their corresponding handler functions from lib modules.

Purpose: CLN-03 requires gsd-tools.cjs to contain no dead modules. This plan removes routes and functions with zero callers across surviving workflows/commands/agents.
Output: Lean CLI router with only live routes remaining.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-remaining-cleanup-polish/10-CONTEXT.md
@.planning/phases/10-remaining-cleanup-polish/research/cli-dead-code-findings.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove 29 dead routes from gsd-tools.cjs and dead functions from lib modules</name>
  <files>
    get-shit-done/bin/gsd-tools.cjs
    get-shit-done/bin/lib/init.cjs
    get-shit-done/bin/lib/state.cjs
    get-shit-done/bin/lib/commands.cjs
    get-shit-done/bin/lib/verify.cjs
    get-shit-done/bin/lib/template.cjs
    get-shit-done/bin/lib/frontmatter.cjs
    get-shit-done/bin/lib/phase.cjs
    get-shit-done/bin/lib/milestone.cjs
  </files>
  <action>
    Remove 29 dead routes from gsd-tools.cjs switch router. The dead routes are listed in cli-dead-code-findings.md Section 1 "Routes to REMOVE":

    1. `state resolve-blocker`
    2. `resolve-model`
    3. `verify-summary`
    4. `template select`
    5. `frontmatter set`
    6. `frontmatter merge`
    7. `frontmatter validate`
    8. `verify plan-structure`
    9. `verify phase-completeness`
    10. `verify references`
    11. `verify commits`
    12. `generate-slug`
    13. `current-timestamp`
    14. `verify-path-exists`
    15. `config-ensure-section`
    16. `history-digest`
    17. `phase add`
    18. `phase insert`
    19. `phase remove`
    20. `milestone complete`
    21. `validate consistency`
    22. `scaffold`
    23. `init new-project`
    24. `init new-milestone`
    25. `init quick`
    26. `init verify-work`
    27. `init milestone-op`
    28. `init map-codebase`
    29. `websearch`

    KEEP all 8 v2 flagged routes (init plan-feature, init execute-feature, init feature-op, init feature-progress, capability-status, feature-create, feature-list, feature-status) per user decision.

    Then remove the dead handler functions from each lib module:

    **init.cjs** — Remove 6 functions: cmdInitNewProject, cmdInitNewMilestone, cmdInitQuick, cmdInitVerifyWork, cmdInitMilestoneOp, cmdInitMapCodebase. Remove their module.exports entries. KEEP all v2 functions (cmdInitPlanFeature, cmdInitExecuteFeature, cmdInitFeatureOp, cmdInitFeatureProgress).

    **state.cjs** — Remove cmdStateResolveBlocker function and its module.exports entry.

    **commands.cjs** — Remove: cmdResolveModel, cmdGenerateSlug, cmdCurrentTimestamp, cmdVerifyPathExists, cmdHistoryDigest, cmdWebsearch, cmdScaffold, cmdConfigEnsureSection. KEEP: cmdCommit, cmdSummaryExtract, cmdProgressRender, cmdConfigSet, cmdConfigGet.

    **verify.cjs** — Remove: cmdVerifySummary, cmdVerifyPlanStructure, cmdVerifyPhaseCompleteness, cmdVerifyReferences, cmdVerifyCommits, cmdValidateConsistency. KEEP: cmdVerifyArtifacts, cmdVerifyKeyLinks.

    **template.cjs** — Remove cmdTemplateSelect. KEEP cmdTemplateFill.

    **frontmatter.cjs** — Remove CLI route handlers: cmdFrontmatterSet, cmdFrontmatterMerge, cmdFrontmatterValidate. KEEP cmdFrontmatterGet (still called by verify-phase.md). CRITICAL: Do NOT remove core parsing functions (extractFrontmatter, reconstructFrontmatter, parseFrontmatter) — these are used everywhere.

    **phase.cjs** — Remove: cmdPhaseAdd, cmdPhaseInsert, cmdPhaseRemove, cmdPhaseNextDecimal. KEEP: cmdFindPhase, cmdPhaseComplete, cmdPhasePlanIndex, cmdPhasesList.

    **milestone.cjs** — Remove cmdMilestoneComplete function and its module.exports entry. KEEP cmdRequirementsMarkComplete. Do NOT delete the file.

    Also update the help text at the top of gsd-tools.cjs to remove the dead routes from the usage documentation.

    SURGICAL REMOVALS ONLY. For each file:
    1. Read the file first
    2. Identify the exact function boundaries
    3. Remove only the dead function(s)
    4. Update module.exports to remove dead entries
    5. Verify remaining exports are intact
  </action>
  <verify>
    <automated>cd get-shit-done && node bin/gsd-tools.cjs --help 2>&1 | head -5 && echo "---" && node -e "const m = require('./bin/lib/milestone.cjs'); console.log('cmdRequirementsMarkComplete:', typeof m.cmdRequirementsMarkComplete)" && node -e "const f = require('./bin/lib/frontmatter.cjs'); console.log('extractFrontmatter:', typeof f.extractFrontmatter, 'cmdFrontmatterGet:', typeof f.cmdFrontmatterGet)" && node -e "const v = require('./bin/lib/verify.cjs'); console.log('cmdVerifyArtifacts:', typeof v.cmdVerifyArtifacts, 'cmdVerifyKeyLinks:', typeof v.cmdVerifyKeyLinks)" && node -e "const i = require('./bin/lib/init.cjs'); console.log('cmdInitPlanFeature:', typeof i.cmdInitPlanFeature, 'cmdInitExecutePhase:', typeof i.cmdInitExecutePhase)"</automated>
  </verify>
  <done>
    All 29 dead routes removed from gsd-tools.cjs. All corresponding dead handler functions removed from lib modules. All live functions (cmdRequirementsMarkComplete, extractFrontmatter, cmdFrontmatterGet, cmdVerifyArtifacts, cmdVerifyKeyLinks, all v2 init functions) still export correctly. Help text updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove gsd-codebase-mapper from MODEL_PROFILES and add haiku to ROLE_MODEL_MAP</name>
  <files>
    get-shit-done/bin/lib/core.cjs
  </files>
  <action>
    In core.cjs MODEL_PROFILES object:
    - Remove the `'gsd-codebase-mapper'` entry (agent was deleted in Phase 8)
    - Remove entries for other deleted agents: `gsd-roadmapper`, `gsd-debugger`, `gsd-integration-checker`
    - Add a deprecation comment above MODEL_PROFILES: `// v1 MODEL_PROFILES — deprecated. Kept as fallback for agents lacking role_type frontmatter.`
    - Keep entries for: gsd-planner, gsd-executor (still potentially used via v1 fallback path)

    In core.cjs ROLE_MODEL_MAP object:
    - Add `quick: 'haiku'` entry to match v2 standard (Opus/Sonnet/Opus-inherit/Haiku)
    - Result should be: `{ executor: 'sonnet', judge: 'inherit', quick: 'haiku' }`

    Do NOT modify resolveModelInternal or resolveModelFromRole — they are already correct per research findings.
  </action>
  <verify>
    <automated>cd get-shit-done && node -e "const c = require('./bin/lib/core.cjs'); console.log('MODEL_PROFILES keys:', Object.keys(c.MODEL_PROFILES)); console.log('ROLE_MODEL_MAP:', JSON.stringify(c.ROLE_MODEL_MAP))"</automated>
  </verify>
  <done>
    MODEL_PROFILES contains only gsd-planner and gsd-executor. ROLE_MODEL_MAP contains executor, judge, and quick entries matching v2 standard.
  </done>
</task>

</tasks>

<verification>
- Run `node get-shit-done/bin/gsd-tools.cjs state load 2>&1` from a project with .planning/ — should return state JSON without error
- Run `node get-shit-done/bin/gsd-tools.cjs commit --help 2>&1` — should show commit usage
- Grep for any removed function name in gsd-tools.cjs to confirm no stale references: `grep -c "cmdInitNewProject\|cmdMilestoneComplete\|cmdWebsearch\|cmdScaffold" get-shit-done/bin/gsd-tools.cjs` should return 0
</verification>

<success_criteria>
gsd-tools.cjs router has zero dead routes. Every exported function in every lib module is called by at least one surviving route. MODEL_PROFILES cleaned of deleted agents. ROLE_MODEL_MAP has haiku tier.
</success_criteria>

<output>
After completion, create `.planning/phases/10-remaining-cleanup-polish/10-01-SUMMARY.md`
</output>

---
phase: 13-multi-scenario-e2e-testing-cleanup
plan: 04
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - .planning/phases/13-multi-scenario-e2e-testing-cleanup/scenarios/07-mid-pipeline-plan.md
  - .planning/phases/13-multi-scenario-e2e-testing-cleanup/scenarios/08-mid-pipeline-execute.md
  - .planning/phases/13-multi-scenario-e2e-testing-cleanup/scenarios/09-mid-pipeline-review.md
  - .planning/phases/13-multi-scenario-e2e-testing-cleanup/13-FINDINGS.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Mid-pipeline plan entry works with pre-staged FEATURE.md containing EU/FN/TC requirements"
    - "Mid-pipeline execute entry works with pre-staged PLAN files in feature directory"
    - "Mid-pipeline review entry works with pre-staged executed artifacts and SUMMARY files"
    - "Pipeline stages do not require running prior stages -- each accepts pre-staged artifacts"
  artifacts:
    - path: ".planning/phases/13-multi-scenario-e2e-testing-cleanup/scenarios/07-mid-pipeline-plan.md"
      provides: "Scenario report for mid-pipeline plan entry"
    - path: ".planning/phases/13-multi-scenario-e2e-testing-cleanup/scenarios/08-mid-pipeline-execute.md"
      provides: "Scenario report for mid-pipeline execute entry"
    - path: ".planning/phases/13-multi-scenario-e2e-testing-cleanup/scenarios/09-mid-pipeline-review.md"
      provides: "Scenario report for mid-pipeline review entry"
  key_links:
    - from: "get-shit-done/workflows/plan.md"
      to: "bin/gsd-tools.cjs init plan-feature"
      via: "plan workflow reads pre-staged FEATURE.md"
      pattern: "init plan-feature"
    - from: "get-shit-done/workflows/execute.md"
      to: "bin/gsd-tools.cjs init execute-feature"
      via: "execute workflow reads pre-staged PLAN files"
      pattern: "init execute-feature"
    - from: "get-shit-done/workflows/review.md"
      to: "bin/gsd-tools.cjs init feature-op"
      via: "review workflow reads pre-staged artifacts"
      pattern: "init feature-op"
---

<objective>
Test mid-pipeline entry points (S07-S09). Pre-stage artifacts at each pipeline stage and verify that plan, execute, and review can be entered directly without running prior stages.

Purpose: Validates ROADMAP success criterion #8 -- "Can jump into pipeline at any point (not forced to start from discovery)." Real users will resume work, skip stages they've done manually, or enter at specific points.

Output: Three scenario reports for mid-pipeline plan (S07), execute (S08), and review (S09).
</objective>

<execution_context>
@/Users/philliphall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philliphall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-multi-scenario-e2e-testing-cleanup/13-CONTEXT.md
@.planning/phases/13-multi-scenario-e2e-testing-cleanup/13-RESEARCH.md
@.planning/phases/13-multi-scenario-e2e-testing-cleanup/13-02-SUMMARY.md

Read these to understand what each stage expects:
@get-shit-done/workflows/plan.md
@get-shit-done/workflows/execute.md
@get-shit-done/workflows/execute-plan.md
@get-shit-done/workflows/review.md
@get-shit-done/workflows/doc.md
@get-shit-done/templates/feature.md
@get-shit-done/templates/capability.md
@get-shit-done/templates/discovery-brief.md
@get-shit-done/templates/research.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: S07 Mid-pipeline plan entry + S08 Mid-pipeline execute entry</name>
  <files>
    .planning/phases/13-multi-scenario-e2e-testing-cleanup/scenarios/07-mid-pipeline-plan.md
    .planning/phases/13-multi-scenario-e2e-testing-cleanup/scenarios/08-mid-pipeline-execute.md
    .planning/phases/13-multi-scenario-e2e-testing-cleanup/13-FINDINGS.md
  </files>
  <action>
**Run Scenario 07: Mid-pipeline plan entry**

Persona: "I've already discussed the workout app and have requirements. I want to jump straight to planning."

1. **Create pre-staged workspace for plan entry:**
   ```bash
   mkdir -p /tmp/gsd-test-midpipe
   cd /tmp/gsd-test-midpipe && git init
   ```

2. **Pre-stage artifacts using templates:**
   - Create .planning/capabilities/workout-routines/ with CAPABILITY.md (from templates/capability.md, filled with workout domain)
   - Create .planning/capabilities/workout-routines/features/hiit-workouts/ with:
     - FEATURE.md (from templates/feature.md, filled with EU/FN/TC requirements for HIIT workout generation)
     - DISCOVERY-BRIEF.md (from templates/discovery-brief.md, filled with workout discovery notes)
     - RESEARCH.md (from templates/research.md, filled with workout research findings)
   - Create PROJECT.md, STATE.md, ROADMAP.md at .planning/ level
   - Use `node gsd-tools.cjs capability-create workout-routines --cwd=/tmp/gsd-test-midpipe` and `node gsd-tools.cjs feature-create workout-routines hiit-workouts --cwd=/tmp/gsd-test-midpipe` for directory structure, then populate files

3. **Test plan entry point:**
   - `node gsd-tools.cjs init plan-feature workout-routines hiit-workouts --cwd=/tmp/gsd-test-midpipe`
   - Verify JSON returns:
     - feature_found: true
     - feature_dir points to correct path
     - FEATURE.md is readable from feature_dir

4. **Trace plan.md workflow:**
   - Read plan.md to verify it reads FEATURE.md for requirements
   - Confirm it does NOT require DISCOVERY-BRIEF.md or RESEARCH.md to exist (they're helpful but not mandatory)
   - Verify the workflow can operate with just FEATURE.md + CAPABILITY.md present
   - Check that the planner context block gets populated correctly

5. **Record findings** and **write scenario report** to `scenarios/07-mid-pipeline-plan.md`.

---

**Run Scenario 08: Mid-pipeline execute entry**

Persona: "I have a plan file ready. I want to jump straight to execution."

1. **Extend the pre-staged workspace:**
   - Add PLAN files to the hiit-workouts feature directory:
     - Create 01-PLAN.md with realistic frontmatter and 2 tasks (generate HIIT workout sequences, create rest timer)
     - Use the PLAN.md template structure from planner-reference.md

2. **Test execute entry point:**
   - `node gsd-tools.cjs init execute-feature workout-routines hiit-workouts --cwd=/tmp/gsd-test-midpipe`
   - Verify JSON returns valid data including plan count

3. **Trace execute.md workflow:**
   - Read execute.md to verify it scans feature_dir for *-PLAN.md files
   - Confirm it does NOT require discovery or research artifacts
   - Verify wave assignment and plan ordering logic works with the pre-staged plan
   - Check how it tracks progress (SUMMARY files vs PLAN files)

4. **Trace execute-plan.md workflow:**
   - Verify it reads the specific PLAN.md and can extract tasks
   - Check SUMMARY creation path

5. **Record findings** and **write scenario report** to `scenarios/08-mid-pipeline-execute.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-multi-scenario-e2e-testing-cleanup/scenarios/07-mid-pipeline-plan.md && test -f .planning/phases/13-multi-scenario-e2e-testing-cleanup/scenarios/08-mid-pipeline-execute.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Scenarios 07 and 08 reports written. Mid-pipeline plan and execute entries validated with pre-staged artifacts. Findings logged.</done>
</task>

<task type="auto">
  <name>Task 2: S09 Mid-pipeline review entry</name>
  <files>
    .planning/phases/13-multi-scenario-e2e-testing-cleanup/scenarios/09-mid-pipeline-review.md
    .planning/phases/13-multi-scenario-e2e-testing-cleanup/13-FINDINGS.md
  </files>
  <action>
**Run Scenario 09: Mid-pipeline review entry**

Persona: "I've executed the plan manually. I want GSD to review what I built."

1. **Extend the pre-staged workspace:**
   - Add SUMMARY files for each plan (simulating completed execution)
   - Add simulated "built" artifacts (mock files representing what the executor would produce)
   - Ensure FEATURE.md requirements are present for reviewers to trace against

2. **Test review entry point:**
   - `node gsd-tools.cjs init feature-op workout-routines hiit-workouts review --cwd=/tmp/gsd-test-midpipe`
   - Verify JSON returns valid data

3. **Trace review.md workflow:**
   - Read review.md to understand what it expects
   - Verify it reads FEATURE.md EU/FN/TC requirements for reviewer context
   - Verify the 4 parallel specialist reviewers are spawned (end-user, functional, technical, code quality)
   - Check that review-synthesizer consolidation step is wired
   - Confirm @file references in review.md resolve

4. **Trace doc.md workflow (follow-on):**
   - Read doc.md to verify it can follow review
   - Check that it writes to .documentation/ directory
   - Verify @file references resolve

5. **Verify the complete mid-pipeline chain:**
   - Plan entry (S07) -> pre-staged artifacts -> Execute entry (S08) -> SUMMARY files -> Review entry (S09) -> doc follow-on
   - This proves the entire pipeline can be entered at any point

6. **Record findings** and **write scenario report** to `scenarios/09-mid-pipeline-review.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-multi-scenario-e2e-testing-cleanup/scenarios/09-mid-pipeline-review.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Scenario 09 report written. Mid-pipeline review entry validated. Complete mid-pipeline chain (plan -> execute -> review) proven to work with pre-staged artifacts at each entry point.</done>
</task>

</tasks>

<verification>
- All 3 scenario reports exist in scenarios/ directory
- Pre-staged workspace at /tmp/gsd-test-midpipe has complete artifact chain
- 13-FINDINGS.md updated with mid-pipeline findings
- Each pipeline stage proven to accept pre-staged artifacts without requiring prior stages
</verification>

<success_criteria>
All 3 mid-pipeline entry points work. Users can jump into plan, execute, or review with appropriate pre-staged artifacts. No mandatory prior-stage dependency blocks entry. Failures fixed or logged.
</success_criteria>

<output>
After completion, create `.planning/phases/13-multi-scenario-e2e-testing-cleanup/13-04-SUMMARY.md`
</output>

---
phase: 13-multi-scenario-e2e-testing-cleanup
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - .planning/phases/13-multi-scenario-e2e-testing-cleanup/scenarios/06-brownfield-init.md
  - .planning/phases/13-multi-scenario-e2e-testing-cleanup/13-FINDINGS.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "init project detects existing code when .planning/ and .documentation/ are absent but source files exist"
    - "Brownfield init triggers existing-project mode (not new-project mode)"
    - "Capability/feature auto-discovery identifies existing workout-routines structure"
    - "init-project.md workflow handles the brownfield path correctly"
  artifacts:
    - path: ".planning/phases/13-multi-scenario-e2e-testing-cleanup/scenarios/06-brownfield-init.md"
      provides: "Scenario report for brownfield init flow"
  key_links:
    - from: "get-shit-done/workflows/init-project.md"
      to: "bin/gsd-tools.cjs init project"
      via: "init detects existing vs new project"
      pattern: "init project"
---

<objective>
Test the brownfield init flow (S06). Strip .planning/ and .documentation/ from the workout app, re-run init, and verify it detects existing code and triggers capability/feature discovery.

Purpose: Validates INIT-02 -- existing-project mode that discovers existing capabilities/features from code rather than starting from scratch.

Output: One scenario report for brownfield init.
</objective>

<execution_context>
@/Users/philliphall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philliphall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-multi-scenario-e2e-testing-cleanup/13-CONTEXT.md
@.planning/phases/13-multi-scenario-e2e-testing-cleanup/13-RESEARCH.md
@.planning/phases/13-multi-scenario-e2e-testing-cleanup/13-02-SUMMARY.md

Read these to understand the brownfield flow:
@get-shit-done/workflows/init-project.md
@get-shit-done/bin/lib/init.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: S06 Brownfield init -- strip planning docs, re-init, verify discovery</name>
  <files>
    .planning/phases/13-multi-scenario-e2e-testing-cleanup/scenarios/06-brownfield-init.md
    .planning/phases/13-multi-scenario-e2e-testing-cleanup/13-FINDINGS.md
  </files>
  <action>
**Run Scenario 06: Brownfield init**

Persona: "I have an existing workout app codebase. I want to use GSD to manage it."

1. **Create brownfield workspace** (copy from workout app, strip planning):
   ```bash
   cp -r /tmp/gsd-test-workout /tmp/gsd-test-brownfield
   rm -rf /tmp/gsd-test-brownfield/.planning
   rm -rf /tmp/gsd-test-brownfield/.documentation
   ```
   The workspace now has the code artifacts but no GSD scaffolding.

2. **Test init project route on brownfield:**
   ```bash
   node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" init project --cwd=/tmp/gsd-test-brownfield
   ```
   Verify:
   - Returns valid JSON
   - Detects this is an existing project (not a fresh directory)
   - The response indicates existing-project mode or equivalent signal

3. **Read init-project.md workflow:**
   - Trace the brownfield/existing-project path
   - Identify how it branches between new vs existing
   - Verify the existing-project path includes:
     a. Code research/scanning step
     b. Capability/feature discovery
     c. Q&A to confirm discovered capabilities
   - Check that @file references in the brownfield path resolve

4. **Simulate the discovery flow:**
   - If init-project.md calls research-workflow.md or gather-synthesize.md for existing code, trace that path
   - Simulate: the system scans the codebase and discovers workout-routines-related code
   - Verify the workflow creates .planning/capabilities/ structure based on discovery

5. **Test capability creation after brownfield init:**
   - `node gsd-tools.cjs capability-create workout-routines --cwd=/tmp/gsd-test-brownfield`
   - `node gsd-tools.cjs feature-create workout-routines bodyweight-exercises --cwd=/tmp/gsd-test-brownfield`
   - Verify these work the same as in greenfield

6. **Verify STATE.md and ROADMAP.md are created:**
   - init-project.md should bootstrap these as final steps (per 12-03 decision)
   - Check templates/state.md and templates/roadmap.md are used

7. **Compare brownfield vs greenfield outputs:**
   - Same directory structure should result
   - Key difference: brownfield should reference existing code in its discovery
   - Log any differences that seem wrong

8. **Record findings** to 13-FINDINGS.md. Fix simple issues inline.

9. **Write scenario report** to `scenarios/06-brownfield-init.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-multi-scenario-e2e-testing-cleanup/scenarios/06-brownfield-init.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Scenario 06 report written. Brownfield init tested -- either existing-project mode works correctly or specific failures are documented in FINDINGS.md.</done>
</task>

</tasks>

<verification>
- Scenario report exists at scenarios/06-brownfield-init.md
- 13-FINDINGS.md updated with brownfield-specific findings
- Brownfield workspace at /tmp/gsd-test-brownfield has .planning/ structure after init simulation
</verification>

<success_criteria>
Brownfield init flow is tested end-to-end. The init workflow correctly detects an existing project, the discovery path is traced, and the resulting scaffolding matches expectations. Failures are fixed or logged.
</success_criteria>

<output>
After completion, create `.planning/phases/13-multi-scenario-e2e-testing-cleanup/13-03-SUMMARY.md`
</output>

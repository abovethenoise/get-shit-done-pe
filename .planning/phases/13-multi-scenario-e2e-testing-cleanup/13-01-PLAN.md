---
phase: 13-multi-scenario-e2e-testing-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13-multi-scenario-e2e-testing-cleanup/13-FINDINGS.md
  - .planning/phases/13-multi-scenario-e2e-testing-cleanup/scenarios/01-greenfield-new.md
  - .planning/phases/13-multi-scenario-e2e-testing-cleanup/scenarios/02-single-feature.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Greenfield init creates .planning/capabilities/ structure with PROJECT.md, STATE.md, ROADMAP.md"
    - "Capability CRUD (create, list, status) returns valid JSON for workout-routines capability"
    - "Feature CRUD (create, list) returns valid JSON for bodyweight-exercises feature"
    - "init plan-feature and init execute-feature return valid JSON for a created feature"
    - "Single-feature pipeline entry works without requiring full capability discuss"
    - "13-FINDINGS.md exists with structured finding entries for any issues found"
  artifacts:
    - path: ".planning/phases/13-multi-scenario-e2e-testing-cleanup/scenarios/01-greenfield-new.md"
      provides: "Scenario report for greenfield new project flow"
    - path: ".planning/phases/13-multi-scenario-e2e-testing-cleanup/scenarios/02-single-feature.md"
      provides: "Scenario report for single feature pipeline flow"
    - path: ".planning/phases/13-multi-scenario-e2e-testing-cleanup/13-FINDINGS.md"
      provides: "Central findings log for all Phase 13 scenarios"
  key_links:
    - from: "commands/gsd/init.md"
      to: "get-shit-done/workflows/init-project.md"
      via: "/gsd:init invokes init-project workflow"
      pattern: "init-project"
    - from: "get-shit-done/workflows/init-project.md"
      to: "bin/gsd-tools.cjs init project"
      via: "workflow calls CLI route"
      pattern: "init project"
---

<objective>
Test the foundation scenarios: greenfield new project (S01) and single feature pipeline (S02). These build the workout app workspace that all subsequent scenarios reference.

Purpose: S01 validates the full init -> discuss capability -> pipeline flow. S02 validates that a single feature can enter the pipeline without requiring a full capability discussion. Together they establish the workspace artifacts for Plans 02-05.

Output: Two scenario reports, 13-FINDINGS.md initialized, workout app workspace ready for subsequent scenarios.
</objective>

<execution_context>
@/Users/philliphall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philliphall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-multi-scenario-e2e-testing-cleanup/13-CONTEXT.md
@.planning/phases/13-multi-scenario-e2e-testing-cleanup/13-RESEARCH.md

Read these to understand the workflows and CLI routes being tested:
@get-shit-done/workflows/init-project.md
@get-shit-done/workflows/discuss-capability.md
@get-shit-done/workflows/discuss-feature.md
@get-shit-done/workflows/framing-pipeline.md
@get-shit-done/workflows/plan.md
@get-shit-done/workflows/execute.md
@get-shit-done/bin/gsd-tools.cjs (header only -- understand available routes)
</context>

<tasks>

<task type="auto">
  <name>Task 1: S01 Greenfield new project -- init through full pipeline trace</name>
  <files>
    .planning/phases/13-multi-scenario-e2e-testing-cleanup/scenarios/01-greenfield-new.md
    .planning/phases/13-multi-scenario-e2e-testing-cleanup/13-FINDINGS.md
  </files>
  <action>
**Create 13-FINDINGS.md** with the structure from 13-RESEARCH.md (Summary section, From Scenarios table, From Targeted Sweep table, Triage Results section).

**Run Scenario 01: Greenfield new project**

Persona: "I want to build a personal workout app -- balanced cardio and strength training, no gym/weights/machines."

1. **Create workspace:** `mkdir -p /tmp/gsd-test-workout && cd /tmp/gsd-test-workout && git init`

2. **Test init project route:**
   ```bash
   node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" init project --cwd=/tmp/gsd-test-workout
   ```
   Verify: returns valid JSON, `project_found` field present, expected directory suggestions.

3. **Simulate init-project.md workflow:**
   - Read init-project.md to understand the flow
   - Run capability-create: `node gsd-tools.cjs capability-create workout-routines --cwd=/tmp/gsd-test-workout`
   - Run capability-list to verify: `node gsd-tools.cjs capability-list --cwd=/tmp/gsd-test-workout`
   - Verify .planning/capabilities/workout-routines/ directory exists with CAPABILITY.md

4. **Simulate discuss-capability flow:**
   - Read discuss-capability.md workflow
   - Trace the simulated Q&A: user describes workout routines (cardio intervals, bodyweight strength, stretching cooldowns)
   - Create features: `node gsd-tools.cjs feature-create workout-routines bodyweight-exercises --cwd=/tmp/gsd-test-workout`
   - Create second feature: `node gsd-tools.cjs feature-create workout-routines cardio-intervals --cwd=/tmp/gsd-test-workout`
   - Run feature-list: `node gsd-tools.cjs feature-list workout-routines --cwd=/tmp/gsd-test-workout`
   - Verify FEATURE.md exists in each feature directory

5. **Test feature-scoped init routes:**
   - `node gsd-tools.cjs init plan-feature workout-routines bodyweight-exercises --cwd=/tmp/gsd-test-workout`
   - `node gsd-tools.cjs init execute-feature workout-routines bodyweight-exercises --cwd=/tmp/gsd-test-workout`
   - `node gsd-tools.cjs init feature-op workout-routines bodyweight-exercises review --cwd=/tmp/gsd-test-workout`
   - Verify each returns valid JSON with expected fields (feature_found, feature_dir, etc.)

6. **Test slug-resolve:**
   - `node gsd-tools.cjs slug-resolve "workout" --cwd=/tmp/gsd-test-workout`
   - Verify it resolves to workout-routines capability

7. **Simulate framing-pipeline trace:**
   - Read framing-pipeline.md
   - Verify it references research-workflow.md, plan.md, execute.md, review.md, doc.md
   - Confirm all @file references in the pipeline resolve

8. **Record findings:** Log any JSON errors, missing fields, broken routes, or bad @file references to 13-FINDINGS.md with FIXED or OPEN status. Simple fixes (typos, wrong field names) -- fix inline.

9. **Write scenario report** to `scenarios/01-greenfield-new.md` using the template from 13-RESEARCH.md.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-multi-scenario-e2e-testing-cleanup/scenarios/01-greenfield-new.md && test -f .planning/phases/13-multi-scenario-e2e-testing-cleanup/13-FINDINGS.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Scenario 01 report written with PASS/FAIL/PARTIAL verdict. 13-FINDINGS.md initialized. All CLI routes tested with valid JSON responses or findings logged.</done>
</task>

<task type="auto">
  <name>Task 2: S02 Single feature -- pipeline entry without full capability discuss</name>
  <files>
    .planning/phases/13-multi-scenario-e2e-testing-cleanup/scenarios/02-single-feature.md
    .planning/phases/13-multi-scenario-e2e-testing-cleanup/13-FINDINGS.md
  </files>
  <action>
**Run Scenario 02: Single feature pipeline**

Uses the workspace from S01. Tests that a user can work on one specific feature without discussing the full capability first.

Persona: "I just want to build the stretching-cooldown feature. I know what I want."

1. **Create a standalone feature:**
   - `node gsd-tools.cjs feature-create workout-routines stretching-cooldowns --cwd=/tmp/gsd-test-workout`
   - Verify feature directory created

2. **Populate FEATURE.md with workout-domain requirements:**
   - Write realistic EU/FN/TC requirements to the feature's FEATURE.md:
     - EU-01: User can select a stretching routine after workout
     - FN-01: System generates cooldown sequence based on workout type
     - TC-01: Cooldown data stored in JSON format
   - This tests that the pipeline can work with a feature that has requirements but skipped the full discuss-capability discovery

3. **Test feature-scoped init routes for the new feature:**
   - `node gsd-tools.cjs init plan-feature workout-routines stretching-cooldowns --cwd=/tmp/gsd-test-workout`
   - Verify JSON returns feature_found: true and correct feature_dir path

4. **Simulate plan.md workflow trace:**
   - Read plan.md to understand what it does with init plan-feature output
   - Verify the workflow reads FEATURE.md for requirement IDs
   - Verify CAPABILITY_SLUG and FEATURE_SLUG are expected as inputs
   - Check that the planner context block references the feature, not a phase

5. **Simulate discuss-feature.md flow (alternate entry):**
   - Read discuss-feature.md workflow
   - Trace how it handles a single feature discussion
   - Verify it can operate on an existing feature with requirements

6. **Verify feature isolation:**
   - Changes to stretching-cooldowns should not affect bodyweight-exercises or cardio-intervals
   - Run feature-list to confirm all 3 features exist independently

7. **Record findings** to 13-FINDINGS.md. Fix simple issues inline.

8. **Write scenario report** to `scenarios/02-single-feature.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-multi-scenario-e2e-testing-cleanup/scenarios/02-single-feature.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Scenario 02 report written. Single-feature pipeline entry validated or findings logged. Feature isolation confirmed.</done>
</task>

</tasks>

<verification>
- Both scenario reports exist in scenarios/ directory
- 13-FINDINGS.md exists with structured tables
- Workspace at /tmp/gsd-test-workout has .planning/capabilities/workout-routines/ with at least 3 features
- All CLI routes tested return valid JSON or findings are logged
</verification>

<success_criteria>
The greenfield new project flow and single-feature entry work end-to-end through CLI routes. The workout app workspace is established for subsequent scenarios. Any breakages are either fixed inline or logged to 13-FINDINGS.md.
</success_criteria>

<output>
After completion, create `.planning/phases/13-multi-scenario-e2e-testing-cleanup/13-01-SUMMARY.md`
</output>
